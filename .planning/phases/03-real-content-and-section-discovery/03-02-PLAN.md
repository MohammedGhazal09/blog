---
phase: 03-real-content-and-section-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/layouts/SiteLayout.astro
  - src/pages/index.astro
  - src/pages/[section]/index.astro
  - src/pages/عن-أحمد-المنجاوي.astro
  - src/pages/[section]/[slug].astro
autonomous: true
requirements:
  - SITE-03
  - SITE-04
  - SITE-05
  - CONT-03

must_haves:
  truths:
    - "Per D-01 through D-03, every discovery/article route uses one tiny Arabic/RTL shell whose persistent header contains only مدونة أحمد المنجاوي linked to /, while article-only prose/media CSS remains local."
    - "Per D-04 and D-05, / exposes the exact factual Arabic introduction and all registered sections once in registry order, and one dynamic registry-derived route generates all three section roots while unknown sections remain absent."
    - "Per D-06 and D-07, every section index is a semantic text list of approved public articles sorted publishedAt descending then stable slug ascending, with the exact truthful empty sentence when no public entries exist and no hidden section."
    - "Per D-08 through D-10, article section/author facts are ordinary same-tab contextual links, /عن-أحمد-المنجاوي/ renders only the approved name and generic publication purpose plus a home link, and no metadata/schema/entity claim enters this phase."
    - "Per D-18, all new surfaces preserve the approved warm flat one-column Arabic system-font UI, exact four sizes/two weights, logical spacing, visible underlined links/focus, and excluded imagery/cards/motion/dependencies."
  artifacts:
    - path: "src/layouts/SiteLayout.astro"
      provides: "Shared Arabic document, one-link header, main column, and only genuinely shared visual rules"
      contains: "مدونة أحمد المنجاوي"
    - path: "src/pages/index.astro"
      provides: "Registry-ordered Arabic homepage at /"
      contains: "أقسام المدونة"
    - path: "src/pages/[section]/index.astro"
      provides: "One static-path generator for all registered section roots and deterministic public article lists"
      exports: ["getStaticPaths"]
    - path: "src/pages/عن-أحمد-المنجاوي.astro"
      provides: "Truthful verified-field-only Arabic author destination"
      contains: "عن أحمد المنجاوي"
    - path: "src/pages/[section]/[slug].astro"
      provides: "Existing complete article journey wrapped by the shared shell with linked section and author facts"
      contains: "/عن-أحمد-المنجاوي/"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/config/registries.ts"
      via: "Object.entries(sectionRegistry) sorted by order"
      pattern: "sectionRegistry.*order"
    - from: "src/pages/[section]/index.astro"
      to: "src/lib/articles.ts"
      via: "getPublicArticles only; preview records never feed discovery"
      pattern: "getPublicArticles"
    - from: "src/pages/[section]/index.astro"
      to: "src/lib/content-contract.ts"
      via: "articlePath for every linked stable article route"
      pattern: "articlePath"
    - from: "src/pages/[section]/[slug].astro"
      to: "src/layouts/SiteLayout.astro"
      via: "shared document/header/main wrapper around the unchanged article body"
      pattern: "SiteLayout"
    - from: "src/pages/[section]/[slug].astro"
      to: "src/pages/عن-أحمد-المنجاوي.astro"
      via: "ordinary linked byline"
      pattern: "عن-أحمد-المنجاوي"
---

<objective>
Deliver the complete static Arabic discovery graph around the approved-public article boundary.

Purpose: Let a visitor browse the homepage, every registered section, a truthful author destination, and contextual article links with no JavaScript or unsupported claim.

Output: One shared shell, registry-driven homepage and section indexes, one minimal author page, and linked article facts while retaining the verified Phase 2 reader/media behavior.

<acceptance_criteria>
- Given a normal structural build with no public launch article, `/index.html`, all three registry section `index.html` files, and `/عن-أحمد-المنجاوي/index.html` exist, while every section remains visible with the exact empty sentence.
- Given the homepage, each registry label/description appears exactly once in ascending `order`, each label is an ordinary same-tab anchor to its registered section slug, and no latest/featured/card/media block exists.
- Given any approved public article set, each section page lists all and only that section's records once, ordered date-descending then direct code-point slug-ascending, with title link, maintained description, and semantic Arabic date.
- Given a direct development proof article, its section value links to the registered section root and its author value links to `/عن-أحمد-المنجاوي/`; the complete Phase 2 body, provenance, and YouTube journey remain unchanged.
- Given the author page with the current registry, the only personal fact is `أحمد المنجاوي`; the generic purpose paragraph and home link render, while biography/expertise/credential/social/channel/review/schema claims and empty remnants do not.
</acceptance_criteria>
</objective>

<execution_context>
@C:/Users/saieh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/saieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-real-content-and-section-discovery/03-SPEC.md
@.planning/phases/03-real-content-and-section-discovery/03-CONTEXT.md
@.planning/phases/03-real-content-and-section-discovery/03-UI-SPEC.md
@.planning/phases/03-real-content-and-section-discovery/03-RESEARCH.md
@.planning/phases/03-real-content-and-section-discovery/03-VALIDATION.md
@.planning/phases/03-real-content-and-section-discovery/03-PATTERNS.md
@.planning/phases/03-real-content-and-section-discovery/03-01-SUMMARY.md
@.planning/phases/02-complete-arabic-article-journey/02-VERIFICATION.md
@src/config/registries.ts
@src/lib/articles.ts
@src/lib/content-contract.ts
@src/pages/[section]/[slug].astro
@src/components/YouTubePlayer.astro

<interfaces>
Consume these established interfaces directly:
- `sectionRegistry` supplies the only section keys, labels, descriptions, slugs, and order values.
- `authorRegistry.ahmedElMangawy.name` supplies the only approved personal fact currently available.
- `getPublicArticles()` returns collision-checked, dual-approved public entries after Plan 03-01; discovery never calls `getPreviewArticles()`.
- `articlePath(article)` supplies the only stable article href; the index route never reconstructs it.
- `getPreviewArticles()` remains used only by the article route in explicit development mode so draft proofs can demonstrate contextual links.
</interfaces>
</context>

## Phase Goal

**As a** visitor, **I want to** discover substantive, human-reviewed launch content through the homepage, three section indexes, and truthful author context, **so that** I can find relevant material and understand who published it.

## Artifacts this phase produces

- New file and Astro layout symbol `src/layouts/SiteLayout.astro`, with no props beyond the default slot.
- New static route file `src/pages/index.astro` producing `/`.
- New dynamic static route file `src/pages/[section]/index.astro` exporting `getStaticPaths()` and producing every `/{section.slug}/` root.
- New static route file `src/pages/عن-أحمد-المنجاوي.astro` producing `/عن-أحمد-المنجاوي/`.
- New homepage exact copy `أقسام المدونة` and `مقالات عربية في الردود والشبهات والقضايا العامة والعلوم الشرعية، مرتبطة بالمحتوى الموافق لها على يوتيوب.`.
- New author exact copy `عن أحمد المنجاوي`, the locked publication-purpose paragraph, and `العودة إلى الصفحة الرئيسية`.
- New exact empty-state copy `لا توجد مقالات منشورة في هذا القسم بعد.`.
- New contextual article anchors from the section fact to `/{section.slug}/` and from the author fact to `/عن-أحمد-المنجاوي/`.
- No component library, token file, header component, list-row component, optional author schema, metadata helper, or client script is created.

<tasks>

<task type="auto">
  <name>Task 1: Make homepage-to-section discovery work through one shared Arabic shell</name>
  <files>src/layouts/SiteLayout.astro, src/pages/index.astro, src/pages/[section]/index.astro</files>
  <read_first>
    - `src/pages/[section]/[slug].astro` — closest shell/style/static-route analog; extract only document, canvas, column, type, ordinary link, focus, and padding rules.
    - `src/config/registries.ts` — exact section labels, descriptions, slugs, and order; do not duplicate them in route data.
    - `src/lib/articles.ts` — call `getPublicArticles()` only for discovery; Plan 03-01 already enforces approvals/readiness.
    - `src/lib/content-contract.ts` — call `articlePath()` for stable hrefs; preserve title-independent identity.
    - `.planning/phases/03-real-content-and-section-discovery/03-UI-SPEC.md` sections `Design System`, `Shared Shell and Link Graph`, `Homepage`, `Registry-derived section index`, `Responsive`, and `Accessibility` — exact markup/copy/visual contract.
    - `.planning/phases/03-real-content-and-section-discovery/03-CONTEXT.md` D-01 through D-07 and D-18 — shared shell, contextual navigation, registry, list, sort, empty-state, and excluded visual decisions.
    - `.planning/phases/03-real-content-and-section-discovery/03-PATTERNS.md` assignments for `SiteLayout.astro`, `index.astro`, and `[section]/index.astro` — closest analogs and non-generalization rules.
    - `C:/Users/saieh/.agents/skills/astro/SKILL.md` — use Astro file routing/getStaticPaths/static HTML; locked decisions override its Tailwind/metadata suggestions.
    - `C:/Users/saieh/.agents/skills/frontend-responsive-design-standards/SKILL.md` — mobile-first fluid/reflow/44px guidance; the project’s one 48rem breakpoint is authoritative.
    - `C:/Users/saieh/.agents/skills/accessibility/SKILL.md` — native landmarks/lists/links, language, focus, and WCAG 2.2 basics; UI-SPEC's no-skip-link decision is authoritative for this one-link header.
  </read_first>
  <action>
Because this task changes UI, run `npx ui-skills start` before editing and use only the relevant minimalist output; do not install or commit a UI package. Locked D-01 through D-07/D-18 and `03-UI-SPEC.md` override any suggestion for cards, imagery, gradients, motion, webfonts, icons, multiple breakpoints, metadata, or a broader design system.

Create `SiteLayout.astro` with `<!doctype html>`, `<html lang="ar" dir="rtl">`, UTF-8 and viewport declarations only, `body > header + main`, one visible header anchor `مدونة أحمد المنجاوي` to `/`, and the default slot inside `main`. The header contains no menu/nav/sections/logo/icon/footer/breadcrumb. Its link is Body size/700, underlined, at least 44px block size. The layout owns only global box sizing; warm canvas; exact system font `1.125rem/1.9` body; `70ch` centered header/main; common h1/h2 roles; ordinary green underlined links/3px focus; shrink/wrap safety; and mobile-first `16px/32px` padding changing to `24px/64px` at the sole `48rem` breakpoint. Use the exact UI-SPEC colors, four sizes/two weights, relative/logical properties, start alignment, and no `overflow-x:hidden`, card surface, radius, shadow, gradient, animation, image, or new client script (D-01, D-03, D-18).

Create `src/pages/index.astro` wrapped by the layout. Sort `Object.entries(sectionRegistry)` by numeric `order`; render exactly one h1 `أقسام المدونة`, the exact locked introduction, and `ul > li > h2 > a + p`. The anchor text/description/href come from each registry record; expose no internal key/raw slug. Render no latest article, count, author claim, hero, featured item, media, CTA panel, or hardcoded three-item copy (D-04).

Create one `src/pages/[section]/index.astro`. Its `getStaticPaths()` enumerates the sorted registry, not content-derived sections or three hand-written pages; call `getPublicArticles()` once and pass each section's filtered entries. Unknown slugs get no generated path (D-05). Sort each filtered set by canonical `publishedAt` descending and direct code-point `slug` ascending (`<`/`>`, not locale collation). A populated page renders the exact registry h1/description and `ul > li > h2 > a + p + time`; title/description are exact article data, href uses `articlePath`, and `<time datetime>` uses the inherited UTC-stable Arabic long formatter with `<bdi dir="auto">`. An empty page renders exactly `لا توجد مقالات منشورة في هذا القسم بعد.` in the list position, never hides the registered section, and does not make launch readiness pass (D-06, D-07). Keep list rows flat with 24px block padding and 1px adjacent dividers; no card wrapper.
  </action>
  <verify>
    <automated>`npm test && npm run check && npm run build` exits 0. A Node filesystem assertion confirms `dist/index.html`, `dist/الردود-والشبهات/index.html`, `dist/القضايا-العامة/index.html`, and `dist/القسم-العلمي/index.html` exist; each section HTML contains its registry label/description and exact empty sentence while `npm run launch:ready` remains intentionally nonzero.</automated>
  </verify>
  <acceptance_criteria>
    - Given `/`, when built HTML is inspected, then the three registry sections occur once and in exact order with ordinary anchors and exact descriptions.
    - Given zero approved public articles, when each registered section opens, then its h1/description/empty sentence render and the section is not hidden.
    - Given equal-date in-memory/public entries during focused implementation checks, when the index sorts, then direct stable-slug order is deterministic and does not use `localeCompare`.
    - Every new page has one `main`, one `h1`, Arabic/RTL roots, native list/headings/anchors, zero reader-facing English, and zero new client JavaScript.
    - Source contains no duplicated section array, `getPreviewArticles`, card/grid/thumbnail/metric/feed/search/filter/tag UI, physical left/right layout, or Phase 4 metadata.
  </acceptance_criteria>
  <done>A visitor can open the static homepage and follow ordinary Arabic links to every truthful registered section surface, even while launch content is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Complete article-to-section-to-author context without unsupported facts</name>
  <files>src/pages/عن-أحمد-المنجاوي.astro, src/pages/[section]/[slug].astro</files>
  <read_first>
    - `src/pages/[section]/[slug].astro` — preserve the mode boundary, render map, exact article order, dates, summary, references, YouTube player, and article-specific CSS.
    - `src/layouts/SiteLayout.astro` — Task 1 shared shell; use it instead of duplicating document/header/main/global rules.
    - `src/config/registries.ts` — current author input contains only `name: أحمد المنجاوي`; no other personal fact is authoritative.
    - `src/components/YouTubePlayer.astro` — preserve the complete Phase 2 media boundary unchanged.
    - `.planning/phases/03-real-content-and-section-discovery/03-UI-SPEC.md` sections `Updated article context`, `Author page`, `Copywriting Contract`, and `Truthful omission` states — exact links/copy/absence behavior.
    - `.planning/phases/03-real-content-and-section-discovery/03-CONTEXT.md` D-01 through D-03, D-08 through D-10, D-13, and D-18 — shell extraction, author truth, privacy, and metadata boundary.
    - `.planning/phases/03-real-content-and-section-discovery/03-PATTERNS.md` assignments for the author and article routes — exact minimum changes and local article-rule ownership.
    - `.planning/phases/02-complete-arabic-article-journey/02-VERIFICATION.md` — independently verified reader/media behavior that must remain true.
  </read_first>
  <action>
Create `src/pages/عن-أحمد-المنجاوي.astro` wrapped by `SiteLayout`. Resolve `authorRegistry.ahmedElMangawy` and render only: one h1 `عن {author.name}` (therefore currently exact `عن أحمد المنجاوي`); the locked generic purpose paragraph `تنشر المدونة مقالات عربية في الردود والشبهات والقضايا العامة والعلوم الشرعية، وتربط كل مقال بالمحتوى الموافق له على يوتيوب.`; and one ordinary same-tab home link `العودة إلى الصفحة الرئيسية`. Do not create optional author fields/schema, empty elements, unsupported headings/gaps, biography, expertise, affiliation, credential, social/channel/ownership claim, portrait, badge, reviewer/approval statement, Person schema, canonical ID, `قريبًا`, or generated filler (D-08 through D-10, D-13).

Refactor the existing article route to use `SiteLayout` around its existing `article`; delete only the document/body/main/common heading/link/focus rules now owned by the layout. Keep route enumeration, development/public selectors, `render(article)`, registry lookup, UTC formatter, semantic order, summary, authored Markdown/MDX, optional facts/references, media component, and all article-specific prose/summary/reference/player CSS intact (D-01, D-03). In the fact list, keep `القسم:` and `الكاتب:` as plain `dt`; wrap only the section `dd` value in a normal underlined anchor to `/${section.slug}/` and only the author name in an anchor to `/عن-أحمد-المنجاوي/`. Do not make rows clickable or add breadcrumbs, back buttons, reviewer UI, expertise, related content, menus, or share controls (D-02, D-08).

Run the complete structural verification, which now exercises both draft proof routes against `astro dev`; confirm the shared header precedes main content but the article's internal order/media behaviors and exact four-size/two-weight set remain unchanged.
  </action>
  <verify>
    <automated>`npm run verify` exits 0 under exact Node/npm. A Node built-output assertion confirms `dist/عن-أحمد-المنجاوي/index.html` exists and contains only the registered name, locked purpose, and home link; the development Playwright matrix confirms both proof articles still render and their section/author fact values are anchors to existing targets.</automated>
  </verify>
  <acceptance_criteria>
    - Given the current author registry, when the author page renders, then no personal claim beyond the name is present and no absent optional unit leaves markup or spacing.
    - Given either development proof article, when facts are inspected, only the section/author values are clickable and point to the correct section/author routes.
    - Given JavaScript disabled, the shared header, contextual links, full article, author page, and return link remain ordinary usable HTML.
    - Phase 2's summary, content order, references, player intent/fallback, direct YouTube action, bidi, keyboard, and reflow tests remain green.
    - No sidecar/reviewer value, approval claim, schema/canonical metadata, footer/menu/breadcrumb, or new runtime dependency reaches public output.
  </acceptance_criteria>
  <done>A visitor can move between article context, its section, the truthful author destination, and home through static Arabic links without losing the verified reader journey.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registries/approved public entries → static discovery HTML | Templates must use authoritative display values and stable path helpers without revalidating, repairing, or inventing facts. |
| Shared layout → route-specific content | Common CSS/document structure must not absorb article-only media/prose logic or introduce a client/runtime surface. |
| Internal approval evidence → public pages | Routes consume approved article data only; review JSON, identities, and approval claims stay outside render props. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information disclosure | Homepage/index/article/author output | mitigate | Import no review module/data; render no reviewer field/value/badge/claim; Plan 03-03 scans DOM/dist. |
| T-03-08 | Spoofing | Section/article/author links | mitigate | Derive destinations from registry slugs, `articlePath`, and one locked author path; use descriptive same-tab anchors. |
| T-03-09 | Tampering / XSS | Dynamic titles/descriptions/registry facts | mitigate | Consume validated Astro data and normal escaped interpolation; add no raw HTML or client injection. |
| T-03-10 | Information integrity | Author page | mitigate | Render the registered name and locked publication-purpose copy only; omit every unsupported optional claim. |
| T-03-11 | Denial of service / usability | Long Arabic/bidi content | mitigate | Logical fluid 70ch layout, shrinkable children, wrapping, one breakpoint, no fixed heights or overflow concealment. |
| T-03-SC | Tampering | Package supply chain | accept | No package or external block is added; native Astro/HTML/CSS and installed dependencies suffice. |
</threat_model>

<verification>
- Before UI edits, run `npx ui-skills start`; do not add its output or a package to the repository.
- After Task 1: `npm test && npm run check && npm run build`; inspect all four discovery output roots.
- After Task 2: full `npm run verify`; expected-red `npm run launch:ready` remains the truth gate rather than a failure of this structural plan.
- Confirm one h1/main per route, static same-tab link graph, exact Arabic copy, registry parity, deterministic sort source, no client discovery script, and no proof/reviewer output.
- `git diff --check` passes; no `.env` file is read; no browser artifact is written outside `.artifacts/`.
</verification>

<success_criteria>
- `/` links to all three registry section roots through ordinary Arabic anchors.
- Every section root exists even when empty and can render every approved public article once in deterministic order.
- Every development/public article fact links to its section and one truthful author route.
- The shared layout exactly preserves the approved simple Arabic UI and Phase 2 article behavior without expanding scope.
- Ordinary structural verification is green; launch readiness remains honestly blocked on real content.
</success_criteria>

<scope_guard>
Do not add optional personal claims, content packages/sidecars, browser-server split, cards/images/thumbnails/icons/gradients/shadows/motion/webfonts, search/tags/feeds/related content, footer/menu/breadcrumbs, metadata/canonical/schema/sitemap/robots/404, analytics, hosting, deployment, or production claims.
</scope_guard>

<output>
Create `.planning/phases/03-real-content-and-section-discovery/03-02-SUMMARY.md` when done. Do not mark Phase 3 complete while real inputs/readiness are missing.
</output>
