---
phase: 03-real-content-and-section-discovery
plan: 04
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - .planning/phases/03-real-content-and-section-discovery/03-CONTENT-INPUTS.md
autonomous: false
requirements:
  - SITE-03
  - SITE-04
  - SITE-05
  - CONT-01
  - CONT-02
  - CONT-03

must_haves:
  truths:
    - "Per D-14, one internal four-row checklist records approved author facts and one genuine package for each registered section; every unknown starts blank and no example, proof value, or prechecked approval is seeded."
    - "Before Task 2 resumes, each section package has an owner-supplied or explicitly owner-approved literal source path, explicit article ID/public slug, and literal encoded sidecar path recorded in the checklist; Task 3 rejects unresolved, title-derived, traversing, duplicate, or colliding paths."
    - "Per D-11 through D-13, each public launch source is byte-for-byte the human-reviewed revision and has one strict internal sidecar whose SHA-256, source identity, editorial pass, and religious-accuracy pass match that exact revision without exposing reviewer data publicly."
    - "Per D-12 and D-15 through D-17, genuine sources enter the repository as drafts first, Phase 2 proof records never satisfy the corpus gate, and only the final exact approved bytes become public and discoverable."
    - "Per D-01 through D-09 and D-18 through D-19, the real corpus completes the ordinary Arabic homepage-to-section-to-article-to-author graph and passes production discovery, no-JavaScript, keyboard, reflow, accessibility, and final rendered QA."
    - "Phase 3 remains incomplete and Phase 4 does not begin unless npm run verify, npm run launch:ready, every manual truth/consent audit, Hercules QA, and Phase 3 UAT all pass against the genuine final corpus."
  artifacts:
    - path: ".planning/phases/03-real-content-and-section-discovery/03-CONTENT-INPUTS.md"
      provides: "Initially blank four-row internal intake, then the owner-approved literal source/article-ID/public-slug/encoded-sidecar path manifest and evidence ledger for the three section packages"
      contains: "Approved author facts"
    - path: ".artifacts/hercules-visual-qa/phase-03-final/"
      provides: "Ignored real-corpus visual, responsive, interaction, console/network, and final UAT evidence"
  key_links:
    - from: ".planning/phases/03-real-content-and-section-discovery/03-CONTENT-INPUTS.md"
      to: "the three literal article source paths and three literal review-sidecar paths recorded and owner-approved at Task 2"
      via: "per-package article ID/public slug/source equality plus SHA-256 of the exact final raw source bytes; filenames are never inferred from titles"
      pattern: "Literal source path|Literal review-sidecar path"
    - from: "src/pages/[section]/index.astro"
      to: "the three approved launch sources at the literal paths recorded in 03-CONTENT-INPUTS.md"
      via: "getPublicArticles() after central approval validation of those recorded sources and sidecars"
      pattern: "getPublicArticles"
    - from: "each launch article direct YouTube action"
      to: "its owner-supplied matching video"
      via: "validated YouTube ID plus recorded human video-match judgment and manual direct-link audit"
      pattern: "videoMatchesArticle"
---

<objective>
Publish the truthful three-article launch corpus and close the human trust gate without inventing a single content, identity, review, date, reference, or video fact.

Purpose: Turn the already-verified structural discovery graph into a real visitor journey with one substantive, independently reviewed article/video pair in every registered section.

Output: One blank-then-completed internal intake/path manifest, three owner-supplied Arabic Markdown/MDX sources at owner-approved literal repository paths, three exact-byte dual-approval sidecars at the literal encoded paths recorded before resume, green structural/readiness/browser gates, and recorded final human content and UI acceptance.

<acceptance_criteria>
- Given the repository before owner input, `03-CONTENT-INPUTS.md` contains exactly four named rows with every evidence/status cell blank, and `npm run launch:ready` remains nonzero.
- Given missing source text, matching video, truthful date/reference facts, either reviewer record, or repository-visibility/consent confirmation, execution stops at the human-action gate and neither a launch article nor sidecar is created.
- Given any of the three packages lacks an owner-supplied or explicitly owner-approved literal repository source filename, explicit article ID, public slug, or resulting literal encoded review-sidecar path, the checkpoint remains blocked; no filename or identity is inferred from a title.
- Given a complete package, its repository source is first exercised as `draft: true`; only the separately supplied, reviewed final byte stream with `draft: false` is then written publicly, and its recomputed digest must equal the supplied review evidence before the sidecar is accepted.
- Given the final corpus, every registered section has at least one approved public article, every section index lists the correct real article once, every article reaches its matching YouTube video and truthful author context, and all proof/reviewer traces stay absent from public output.
- Given Phase 3 completion, `npm run verify` and `npm run launch:ready` both exit 0, all manual substance/video/religious/fact/consent checks are explicitly recorded as passed by real humans, production discovery coverage is green, and final Hercules/Phase 3 UAT has no unresolved finding.
</acceptance_criteria>
</objective>

<execution_context>
@C:/Users/saieh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/saieh/.codex/get-shit-done/templates/summary.md
@C:/Users/saieh/.agents/skills/hercules-visual-qa/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-real-content-and-section-discovery/03-SPEC.md
@.planning/phases/03-real-content-and-section-discovery/03-CONTEXT.md
@.planning/phases/03-real-content-and-section-discovery/03-UI-SPEC.md
@.planning/phases/03-real-content-and-section-discovery/03-RESEARCH.md
@.planning/phases/03-real-content-and-section-discovery/03-VALIDATION.md
@.planning/phases/03-real-content-and-section-discovery/03-PATTERNS.md
@.planning/phases/03-real-content-and-section-discovery/03-01-SUMMARY.md
@.planning/phases/03-real-content-and-section-discovery/03-02-SUMMARY.md
@.planning/phases/03-real-content-and-section-discovery/03-03-SUMMARY.md
@src/config/registries.ts
@src/content.config.ts
@src/lib/approval-contract.ts
@src/lib/articles.ts
@tests/content-contract.test.ts
@tests/discovery.spec.ts

<interfaces>
Use these completed Phase 3 boundaries without widening them:
- No launch source filename, article ID, public slug, or sidecar path is selected by the planner. Task 2 must obtain or explicitly confirm each literal repository source path, explicit article ID, public slug, and corresponding literal encoded review-sidecar path, then record that six-path manifest in `03-CONTENT-INPUTS.md` before execution resumes.
- `assertPublicArticleApprovals()` is the only approval sidecar reader and hashes the exact raw source bytes; routes receive no reviewer data.
- `getPublicArticles()` supplies every production route/index and `getPreviewArticles()` supplies direct draft preview only in explicit development mode.
- `npm run verify` proves structural correctness; `npm run launch:ready` additionally proves genuine approved coverage of every registered section.
- `tests/discovery.spec.ts` derives the expected public corpus independently from validated source files plus current strict sidecars, then separately compares it with generated routes and rendered index links without hardcoded article facts.
</interfaces>
</context>

## Phase Goal

**As a** visitor, **I want to** discover substantive, human-reviewed launch content through the homepage, three section indexes, and truthful author context, **so that** I can find relevant material and understand who published it.

## Artifacts this phase produces

- One internal `.planning/phases/03-real-content-and-section-discovery/03-CONTENT-INPUTS.md` intake/evidence file. Its initial table has exactly four rows: `Approved author facts`, `Refutations package`, `General issues package`, and `Scholarship package`; all other cells begin blank.
- Three literal source destinations under `src/content/articles/` supplied or explicitly approved by the owner at Task 2, with their explicit article IDs and public slugs recorded in the checklist before resume.
- Three literal encoded sidecar destinations under `src/content/reviews/`, computed from the explicitly approved article IDs, shown to the owner, and recorded as literal paths in the checklist before Task 3 writes them.
- Real human evidence for editorial substance, article/video correspondence, religious accuracy/reference adequacy, truthful author/date/reference facts, reviewer identity/date/pass, and repository visibility/consent.
- Ignored final rendered evidence under `.artifacts/hercules-visual-qa/phase-03-final/` only.
- No sample launch source, fake reviewer, precomputed approval, reused proof fact, CMS/database/auth path, dependency, or public review badge.

<tasks>

<task type="auto">
  <name>Task 1: Create the blank four-package truth intake and preserve the blocking gate</name>
  <files>.planning/phases/03-real-content-and-section-discovery/03-CONTENT-INPUTS.md</files>
  <read_first>
    - `.planning/phases/03-real-content-and-section-discovery/03-SPEC.md` requirements 3 through 7 — real corpus, exact approvals, truthful facts, and proof exclusion.
    - `.planning/phases/03-real-content-and-section-discovery/03-CONTEXT.md` D-11 through D-17 — internal sidecars, blank input record, proof isolation, and distinct readiness semantics.
    - `.planning/phases/03-real-content-and-section-discovery/03-RESEARCH.md` sections `Truthful External Inputs and Readiness Boundary` and `Content-Input Checklist Recommendation` — all locally missing facts and the four-row format.
    - `.planning/phases/03-real-content-and-section-discovery/03-VALIDATION.md` `Known External Blocker` and `Manual-Only Verifications` — evidence automation cannot manufacture.
    - `.planning/phases/03-real-content-and-section-discovery/03-PATTERNS.md` `Plan 03-04 input checkpoint and real corpus` — fixed no-analog truth gate and no-example rule.
    - `.planning/STATE.md` `Blockers/Concerns` — current evidence that content/profile/review inputs are absent.
  </read_first>
  <action>
Create one internal Markdown checklist at the exact path above before requesting any human input (D-14). Its single table has the row labels `Approved author facts`, `Refutations package`, `General issues package`, and `Scholarship package`. Columns record: literal repository source path; explicit article ID; explicit public slug; literal encoded review-sidecar path; approved visible author facts or exact article source package; matching YouTube URL and ID; real publication/update dates; required references; editorial reviewer identity/date/pass plus `substantive` and `videoMatchesArticle`; religious-accuracy reviewer identity/date/pass; repository visibility and reviewer consent; and final status. Leave every cell other than the four row labels blank. Use no checkbox, example, demonstration value, expected answer, reviewer alias, date, URL, video ID, reference, digest, path, status, `TBD`, or approval-like wording that could be mistaken for evidence.

Keep `03-CONTENT-INPUTS.md` outside `src/` and never render/import it. Run ordinary verification and the wrapped readiness check: structural verification must remain green, while readiness must still fail for all registered sections. Do not create the six production content/review files, change current author facts, reuse a Phase 2 proof, or imply that the blank intake satisfies any requirement.
  </action>
  <verify>
    <automated>`npm run verify` exits 0. PowerShell then asserts exactly four data rows in `03-CONTENT-INPUTS.md`, no non-label cell contains non-whitespace, and the wrapped `npm run launch:ready` exits nonzero naming `refutations`, `generalIssues`, and `scholarship`.</automated>
  </verify>
  <acceptance_criteria>
    - The file exists once, has exactly four required row labels, and every evidence/status cell is blank.
    - No prechecked state, illustrative person/date/URL/reference/digest, proof title, `example.com`, or `dQw4w9WgXcQ` appears.
    - The file is not imported by source or tests and is absent from `dist`.
    - Structural verification is green and launch readiness is still specifically blocked by all missing section packages.
  </acceptance_criteria>
  <done>The repository has one honest intake boundary and no fabricated launch evidence.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Obtain genuine byte-exact content, dual review, and consent packages</name>
  <files>.planning/phases/03-real-content-and-section-discovery/03-CONTENT-INPUTS.md</files>
  <read_first>
    - `.planning/phases/03-real-content-and-section-discovery/03-CONTENT-INPUTS.md` — every blank cell is a required unresolved fact, not permission to infer one.
    - `.planning/phases/03-real-content-and-section-discovery/03-SPEC.md` requirements 3 through 6 — human truth and substantive-content acceptance.
    - `.planning/phases/03-real-content-and-section-discovery/03-CONTEXT.md` D-09 and D-11 through D-17 — author omission, exact-byte sidecars, privacy, and proof prohibition.
    - `.planning/phases/03-real-content-and-section-discovery/03-VALIDATION.md` `Manual-Only Verifications` — required human judgments and consent confirmation.
    - `src/content.config.ts` — exact repository-ready frontmatter/content schema each supplied final byte stream must satisfy.
    - `src/lib/approval-contract.ts` — exact strict sidecar fields, source identity, digest, dates, and human pass literals the supplied records must match.
  </read_first>
  <action>
Stop execution here until the owner supplies all four packages; do not draft a substitute. The author-facts package must explicitly approve every visible Phase 3 author-page fact, including confirmation that the current name and publication-purpose text are accurate; optional biography/expertise/credential/social/channel claims remain absent unless separately supplied and approved.

Each of the three article packages must contain: an owner-supplied or explicitly owner-approved literal repository source filename under `src/content/articles/`; an explicit article ID; an explicit Arabic public slug; a repository-ready final Markdown/MDX byte stream for that literal source path; genuine Arabic authored text that independently answers its topic; the correct registered section; truthful byline and real publication/update date; required references; and the real matching YouTube URL/ID. It must also contain the lowercase SHA-256 of that exact final byte stream with `draft: false`, a real editorial reviewer identity/date/literal pass with explicit substance and video-match pass, and a real religious-accuracy reviewer identity/date/literal pass for the same digest. The owner must confirm repository visibility and each reviewer's informed consent to store their identity/date in the Git-tracked internal sidecar. A reachable/valid video alone is not evidence that it matches; a word count alone is not evidence of substance; automation is not a reviewer.

Before this checkpoint may resume, run a read-only path/identity preflight over the supplied candidates. Reject any empty/unresolved token; absolute, rooted, drive-qualified, dot-segment, or path-traversing source/sidecar path; source outside `src/content/articles/`; source extension other than `.md` or `.mdx`; sidecar outside `src/content/reviews/`; duplicate source, article ID, public slug, sidecar, or public route; collision with an existing source/sidecar/registered public route; or mismatch between the explicit article ID and the encoded sidecar filename required by `assertPublicArticleApprovals()`. Never derive a filename, article ID, or slug from the title. Compute the exact encoded sidecar path from each explicitly approved article ID, present all three literal source paths, IDs/slugs, and literal sidecar paths to the owner, and obtain explicit approval. Record those literal resolved values in the corresponding checklist rows before resume; no formula, angle-bracket token, sample path, or planner-selected filename may stand in for the literal manifest.

Reject any package that copies/relabels either Phase 2 proof, uses an example/reference/demo value, lacks one exact final byte stream or one approved literal path/identity set, gives approvals for draft bytes or a different digest, asks the agent to write/review religious claims, supplies a future/fictional date or reviewer, leaves repository visibility/consent unresolved, or collides with another/current source, sidecar, ID, slug, or route. Keep the related checklist cells blank and remain at this checkpoint. Do not proceed because most inputs are present.
  </action>
  <verify>
    <automated>`npm run launch:ready` remains nonzero before Task 3; no command may turn this checkpoint green. After input arrives, a read-only Node preflight must prove each supplied final byte stream equals its supplied SHA-256; every literal source/sidecar path is contained, normalized, unique, non-traversing, non-colliding, and fully resolved; every article ID/public slug/section/public route is explicit and unique; and each literal sidecar path exactly matches the encoded approved article ID before any repository content write.</automated>
    <human-check>The owner confirms all visible author facts, three literal repository source filenames, three explicit article IDs/public slugs, the three resulting literal encoded sidecar paths recorded in `03-CONTENT-INPUTS.md`, three genuine article/video/date/reference packages, six real reviewer identity/date/pass records tied to the exact final bytes, repository visibility, and reviewer consent. Editorial and religious-accuracy reviewers—not the agent—confirm the required judgments.</human-check>
  </verify>
  <acceptance_criteria>
    - All four packages are complete, internally consistent, and mapped to owner-supplied or explicitly owner-approved literal source filenames, explicit article IDs/public slugs, and literal encoded sidecar paths recorded in the checklist before resume.
    - Every review record binds to the supplied final `draft: false` byte stream, not a draft/body-only/normalized/re-serialized variant.
    - One genuine package maps to each and only one registered section; no proof fixture or invented fact is repurposed.
    - No path is unresolved, title-derived, absolute, traversing, duplicated, or colliding; every literal sidecar path is exactly the encoded path for its approved article ID.
    - Reviewer identity/date/pass and repository visibility/consent are explicit and real; nothing relies on an implied checkbox.
    - If any item is missing or unsafe, this task and Phase 3 remain blocked with the blank checklist showing the unresolved fields.
  </acceptance_criteria>
  <resume-signal>Supply the four complete packages, approve the literal source/article-ID/public-slug/encoded-sidecar manifest recorded in `03-CONTENT-INPUTS.md`, and explicitly confirm reviewer identity storage/consent; otherwise Phase 3 remains at this checkpoint.</resume-signal>
  <done>All real-world inputs needed for byte-exact publication and human truth verification exist without agent inference.</done>
</task>

<task type="auto">
  <name>Task 3: Preview as drafts, publish only exact approved bytes, and close every final gate</name>
  <files>.planning/phases/03-real-content-and-section-discovery/03-CONTENT-INPUTS.md is the path manifest; after Task 2, modify only the three literal article source paths and three literal review-sidecar paths recorded and owner-approved there</files>
  <read_first>
    - All four owner-supplied packages accepted at Task 2 — these are the only authority for article, author, date, reference, video, reviewer, approval, and consent facts.
    - `.planning/phases/03-real-content-and-section-discovery/03-CONTENT-INPUTS.md` — record actual evidence only after each corresponding check succeeds.
    - `src/content.config.ts` — final sources must pass the existing Markdown contract without repair in a route.
    - `src/lib/approval-contract.ts` and `src/lib/articles.ts` — exact final bytes/sidecars must pass the central fail-closed boundary before selection.
    - `tests/content-contract.test.ts` — do not weaken strict sidecar, proof-exclusion, or readiness tests for supplied data.
    - `tests/discovery.spec.ts` — the production suite must discover and validate these articles dynamically, not through new hardcoded facts.
    - `.planning/phases/03-real-content-and-section-discovery/03-VALIDATION.md` `Manual-Only Verifications` and `Validation Sign-Off` — complete every automated and human gate.
    - `C:/Users/saieh/.agents/skills/hercules-visual-qa/SKILL.md` plus its `workflows/test-and-fix.md`, `references/coverage-contract.md`, `references/visual-ux-checklist.md`, and `references/report-format.md` — final real-corpus rendered audit and evidence contract.
  </read_first>
  <action>
Read the literal path/identity manifest from `03-CONTENT-INPUTS.md`; do not reconstruct any path from a title or choose a filename during execution. Before writing, repeat the containment/normalization/uniqueness/collision checks from Task 2 and reject any blank/formula/sample token, absolute/rooted/drive-qualified/dot-segment/traversing path, source outside `src/content/articles/`, invalid source extension, sidecar outside `src/content/reviews/`, duplicate/colliding source/sidecar/article ID/public slug/public route, or sidecar path that is not exactly the encoded destination for its recorded article ID. Stop and return to Task 2 on any mismatch; never repair or infer a path silently.

For each accepted final source, first create only its recorded literal repository source path as a draft preview copy: preserve every supplied metadata/body byte except the single `draft` value, which is `true`. Create no review sidecar yet. Run the exact-runtime native/check/build gates and direct `astro dev` preview for all three recorded final-route identities. The owner, editorial reviewer, and religious-accuracy reviewer compare rendered text, references, dates, article/video subject, and author facts with the supplied package; any content change invalidates the supplied final digest and returns execution to Task 2 for review of a new exact byte stream (D-12, D-16).

After draft preview is accepted, replace each draft copy at its recorded literal source path with its supplied final `draft: false` byte stream exactly; do not normalize, reformat, rewrite, spell-correct, or otherwise alter it. Recompute SHA-256 from the actual repository file and require exact equality with the supplied reviewed digest before writing the matching strict sidecar at its recorded literal sidecar path. Write only the supplied real reviewer identity/date/pass values and required human flags. Confirm sidecar article/source identities match the loader records and the recorded explicit ID/slug/path manifest. If any byte/digest/field/path differs, leave the source non-public or restore `draft: true`, create no passing sidecar, and return to Task 2. Never copy a proof title/body/reference/video/date, let the agent attest substance/religious accuracy/video correspondence, or show reviewer/approval data in public HTML (D-11 through D-17).

Run `npm run verify` and `npm run launch:ready`; both must exit 0. Run the focused production suite `npx playwright test --project=production-discovery tests/discovery.spec.ts` and confirm homepage/three-section set equality, date/slug ordering, every article/author/context link, proof/reviewer absence, no-JavaScript traversal, keyboard focus, five-width reflow, and axe coverage against the real built corpus (D-01 through D-09, D-17 through D-19). Open every permanent YouTube action and have the owner/editorial reviewer record that it reaches the supplied matching video; technical URL success is insufficient. Have the religious-accuracy reviewer record that the exact final article and references pass, and have the owner compare every visible author/byline/date/reference/review claim with the supplied facts.

Run `npx ui-skills start` before any UI adjustment and then execute the Hercules `test-and-fix` workflow against the final production discovery graph, using visible-browser evidence when available. Keep screenshots, profiles, reports, console/network logs, ledgers, and any before/after evidence only under `.artifacts/hercules-visual-qa/phase-03-final/`; redact internal reviewer identities. Cover homepage, all populated indexes, all three articles, author page, no-JS, keyboard/focus, 320/390/768/1024/1440, and native 200% zoom. Any real finding blocks completion and must be fixed through an explicit GSD gap/fix step, then fully reverified; do not waive it or edit beyond this plan's owned files silently.

Only after every automated and manual gate passes, replace the checklist blanks with the actual supplied artifact paths/evidence and a truthful passed status. Record Phase 3 UAT acceptance in the execution summary with the human identities redacted from public/browser evidence. Phase 3 remains incomplete and Phase 4 continuation remains blocked if either command fails, any manual audit is missing, any reviewer/consent fact is unresolved, or any final Hercules/UAT finding remains.
  </action>
  <verify>
    <automated>`npm run verify && npm run launch:ready && npx playwright test --project=production-discovery tests/discovery.spec.ts` exits 0 under Node `v24.19.0` and npm `11.17.0`; `git diff --check` passes; a recursive path check proves every browser/visual artifact is beneath `.artifacts/`.</automated>
    <human-check>Editorial substance and video match, religious accuracy and reference adequacy, author/byline/date/reference truth, reviewer identity/date, repository visibility/consent, native 200% zoom, final Arabic visual quality, and complete Phase 3 UAT are each explicitly passed against the exact final corpus.</human-check>
  </verify>
  <acceptance_criteria>
    - Each source at its owner-approved literal path was previewed as a draft before publication, and its final public bytes exactly match the digest approved by both real reviewers.
    - Task 3 touched only the checklist plus the six literal post-checkpoint paths recorded there; no source/sidecar filename or identity was inferred from a title, formula, or planner placeholder.
    - Every recorded path is resolved, contained, normalized, unique, non-traversing, non-colliding, and consistent with the explicit article ID/public slug before and after publication.
    - All three strict sidecars pass central validation; a one-byte edit makes the affected public build fail until a new human-reviewed package is supplied.
    - Every registered section contains at least one genuine approved article/video pair, and the production browser suite discovers each without hardcoded article facts.
    - Public output contains no Phase 2 proof trace, reviewer identity/field, sidecar data, approval badge, or unsupported author claim.
    - Structural verify, launch readiness, real-corpus browser coverage, every human truth/consent audit, final Hercules review, and Phase 3 UAT all pass.
    - The checklist contains only actual supplied evidence after verification; no blank is converted to a pass by inference.
    - Phase 4 is not started unless every acceptance item is proven.
  </acceptance_criteria>
  <done>The public Arabic discovery graph contains a genuine, byte-exact, dual-reviewed article/video pair in every primary section and has complete automated plus human acceptance evidence.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan(s) | Status |
|--------|----|-----------------------|---------|--------|
| GOAL | — | Visitor discovers substantive human-reviewed launch content through homepage, three indexes, and truthful author context | 03-02, 03-03, 03-04 | COVERED |
| REQ | SITE-03 | Homepage ordinary links to all three primary sections | 03-02, 03-03, 03-04 | COVERED |
| REQ | SITE-04 | Crawlable per-section indexes list every published article usefully | 03-02, 03-03, 03-04 | COVERED |
| REQ | SITE-05 | Truthful Arabic author page and linked bylines | 03-02, 03-03, 03-04 | COVERED |
| REQ | CONT-01 | One substantive real article/video pair per primary section | 03-01, 03-04 | COVERED |
| REQ | CONT-02 | Exact-revision editorial and religious-accuracy sign-off | 03-01, 03-04 | COVERED |
| REQ | CONT-03 | Real byline/expertise/date/reference/review facts and no proof stand-ins | 03-01, 03-02, 03-03, 03-04 | COVERED |
| RESEARCH | — | Strict Node-built-in raw-byte SHA-256 sidecar and fail-closed public selection | 03-01 | COVERED |
| RESEARCH | — | Separate green structural verification and explicit launch-readiness coverage gate | 03-01, 03-04 | COVERED |
| RESEARCH | — | Registry-driven static discovery graph with truthful empty states | 03-02 | COVERED |
| RESEARCH | — | Separate development proof and production discovery browser identities | 03-01, 03-03 | COVERED |
| RESEARCH | — | Proof demotion plus production proof/reviewer absence | 03-01, 03-03, 03-04 | COVERED |
| RESEARCH | — | Owner-supplied real corpus, manual truth checks, and consent boundary | 03-04 | COVERED |
| RESEARCH | — | No dependency, CMS, database, auth, runtime service, or Phase 4–6 claim | 03-01 through 03-04 | COVERED |
| CONTEXT | D-01 | One small Arabic/RTL shell and one-link header | 03-02, 03-03 | COVERED |
| CONTEXT | D-02 | Contextual homepage/index/section/author same-tab anchors | 03-02, 03-03, 03-04 | COVERED |
| CONTEXT | D-03 | Share only document/type/color/focus/measure rules | 03-02, 03-03 | COVERED |
| CONTEXT | D-04 | Factual registry-driven homepage with no feed/promotion | 03-02, 03-03 | COVERED |
| CONTEXT | D-05 | One registry-derived section-index route | 03-02, 03-03 | COVERED |
| CONTEXT | D-06 | Semantic text lists sorted date-descending then slug-ascending | 03-02, 03-03, 03-04 | COVERED |
| CONTEXT | D-07 | Truthful development empty state; launch fails on empty section | 03-01, 03-02, 03-03, 03-04 | COVERED |
| CONTEXT | D-08 | One author route, all bylines linked, home return | 03-02, 03-03, 03-04 | COVERED |
| CONTEXT | D-09 | Render verified author fields only; omit unsupported claims | 03-02, 03-03, 03-04 | COVERED |
| CONTEXT | D-10 | Person/Article schema and canonical identity remain outside Phase 3 | 03-02 | COVERED |
| CONTEXT | D-11 | External exact-byte SHA-256 dual-review sidecars using Node built-ins | 03-01, 03-04 | COVERED |
| CONTEXT | D-12 | Every public article fails closed on missing/malformed/stale approval | 03-01, 03-04 | COVERED |
| CONTEXT | D-13 | Reviewer identity/approval evidence remains out of public UI | 03-01, 03-02, 03-03, 03-04 | COVERED |
| CONTEXT | D-14 | One blank four-row internal content-input checklist | 03-01, 03-04 | COVERED |
| CONTEXT | D-15 | Phase 2 proofs are draft/test-only through the development boundary | 03-01, 03-03, 03-04 | COVERED |
| CONTEXT | D-16 | Structural verify remains runnable; launch gate blocks later work | 03-01, 03-02, 03-03, 03-04 | COVERED |
| CONTEXT | D-17 | Production checks use genuine approved content and reject all proof traces | 03-01, 03-03, 03-04 | COVERED |
| CONTEXT | D-18 | Preserve approved warm flat Arabic visual system and exclusions | 03-02, 03-03, 03-04 | COVERED |
| CONTEXT | D-19 | Five-width/keyboard/no-JS/axe/browser evidence stays under `.artifacts/` | 03-03, 03-04 | COVERED |

No source item is missing. Deferred Phase 4 metadata/discovery files, Phase 5 deployment/measurement, Phase 6 production certification, and deferred search/catalog features are deliberately excluded rather than unplanned.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner/reviewer packages → repository content | External facts and approvals are accepted only when complete, consented, and bound to an exact supplied byte stream. |
| Owner-approved literal path manifest → repository writes | Source and sidecar paths remain untrusted until resolved, contained, unique, non-traversing, non-colliding, and explicitly approved; filenames are never inferred from titles. |
| Draft preview → final public source | Preview may change only `draft`; any other change requires a newly reviewed final byte stream before publication. |
| Internal review sidecar/Git visibility → public build | Reviewer data is visible to authorized repository readers but must never enter rendered HTML, browser evidence, logs, or screenshots. |
| Automated gates → human truth claims | Tests prove structure/integrity; only humans can prove substance, religious accuracy, reference adequacy, identity, consent, and semantic video match. |
| Phase 3 evidence → Phase 4 continuation | A partial or structurally green result cannot authorize the next phase; all automated and manual gates are blocking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Final article bytes / sidecar digest | mitigate | Recompute raw SHA-256 from each actual final repository file and require exact equality with both human reviews. |
| T-03-06 | Information disclosure | Reviewer identity and sidecar fields | mitigate | Never render/import sidecars, scan production DOM/dist, redact visual evidence, and keep artifacts ignored. |
| T-03-07 | Spoofing / Repudiation | Substance, religious accuracy, facts, and video match | transfer | Require real owner/editorial/religious review records; automation must not issue these judgments. |
| T-03-13 | Information disclosure / Repudiation | Git-tracked reviewer identity | mitigate | Block until repository visibility and each reviewer's informed consent are explicitly confirmed. |
| T-03-14 | Information integrity | Phase 2 proof data reused as launch content | mitigate | Owner-approved unique literal source identities, human package audit, production proof scans, and no copy/relabel path. |
| T-03-15 | Elevation of privilege | Phase continuation with partial evidence | mitigate | Require both commands, browser coverage, every manual audit, Hercules, and UAT before Phase 3 completion or Phase 4 start. |
| T-03-17 | Tampering / Elevation of privilege | Unresolved, traversing, duplicated, or colliding content/review path | mitigate | Task 2 records an owner-approved literal manifest; Task 3 repeats containment, encoding, identity, uniqueness, route, and collision checks before any write. |
| T-03-SC | Tampering | Package supply chain | accept | No install, dependency, runtime service, or external content tool is added. |
</threat_model>

<verification>
- Select exact Node `v24.19.0` and npm `11.17.0`; stop on mismatch and never read `.env` files.
- Before owner input: `npm run verify` is green and wrapped `npm run launch:ready` is specifically nonzero for all three sections.
- Before any repository write: read-only SHA-256 preflight proves each supplied final byte stream and digest agree, and the recorded literal path/ID/slug manifest passes containment, encoding, uniqueness, traversal, and collision checks.
- Draft stage: all three final-route identities preview successfully with `draft: true`; production discovery remains unchanged.
- Public stage: `npm run verify && npm run launch:ready` plus the focused production-discovery project are green.
- Manual stage: editorial substance/video match, religious accuracy/references, author/fact/date truth, reviewer identity/date, repository visibility/consent, native zoom, Hercules, and Phase 3 UAT are explicitly recorded.
- `git diff --check` passes; proof/reviewer traces are absent from public output; every browser/visual artifact remains below `.artifacts/`.
</verification>

<success_criteria>
- One genuine, independently useful Arabic article and real matching YouTube video is public in each registered section.
- Every final public source has two real human pass records bound to its exact raw bytes and becomes invalid after any edit.
- Homepage, three populated section indexes, articles, contextual links, and truthful author page form one fully crawlable Arabic/no-JS graph.
- No proof, reviewer identity, approval record, unsupported author fact, or example value reaches production output.
- Structural, readiness, production browser, manual truth/consent, Hercules, and UAT gates all pass; otherwise Phase 3 and Phase 4 continuation remain blocked.
</success_criteria>

<scope_guard>
Do not invent or translate article claims; reuse proof data; approve religious/content/video truth as an agent; add optional author claims without approval; create public reviewer UI; add metadata/canonical/schema/sitemap/robots/404; add CMS/database/auth/search/analytics/hosting/deployment; install a package; or store browser evidence outside `.artifacts/`.
</scope_guard>

<output>
Create `.planning/phases/03-real-content-and-section-discovery/03-04-SUMMARY.md` only after every automated and human gate passes. If Task 2 inputs or any final gate are missing, return the blocking checkpoint and do not mark Phase 3 complete or start Phase 4.
</output>
