---
phase: 03-real-content-and-section-discovery
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - playwright.config.ts
  - tests/article-journey.spec.ts
  - tests/discovery.spec.ts
  - src/layouts/SiteLayout.astro
  - src/pages/index.astro
  - src/pages/[section]/index.astro
  - src/pages/عن-أحمد-المنجاوي.astro
  - src/pages/[section]/[slug].astro
autonomous: true
requirements:
  - SITE-03
  - SITE-04
  - SITE-05
  - CONT-03

must_haves:
  truths:
    - "Per D-15 and D-17, one explicit development project proves both draft proof article formats while a separate production-preview project proves proof routes/content/references/video mapping are absent and discovery uses only approved public entries."
    - "Per D-05 through D-07 and D-17, an independent test-only oracle derives the expected approved public corpus from validated article source files plus current strict sidecars, then separately proves equality with generated article routes and section-index links before also asserting route-to-index wiring equality."
    - "Per D-01 through D-10, browser assertions prove the complete static Arabic graph: shared home link, registry homepage, all section roots, deterministic article lists when populated, contextual article links, and verified-field-only author page."
    - "Per D-13, production DOM/dist scans reject review-sidecar keys, reviewer identities, approval badges, and generic approval claims."
    - "Per D-18 and D-19, representative routes pass Arabic/RTL semantics, native list/link/heading structure, keyboard focus/order, disabled-JavaScript navigation, 320–1440 reflow, serious/critical axe checks, and evidence-first visual QA with all artifacts under .artifacts/."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Distinct development-proof and production-discovery Chromium projects with distinct servers/ports and one artifact root"
      contains: "production-discovery"
    - path: "tests/article-journey.spec.ts"
      provides: "Unweakened Phase 2 Markdown/MDX journey against draft proof routes in development plus contextual-link/shared-shell assertions"
      contains: "development"
    - path: "tests/discovery.spec.ts"
      provides: "Production static discovery contract with an independent raw-source/current-sidecar public-corpus oracle, separate route and index comparisons, proof/reviewer isolation, no-JS, keyboard, reflow, and accessibility coverage"
      contains: "أقسام المدونة"
    - path: ".artifacts/hercules-visual-qa/phase-03-structural/"
      provides: "Ignored identity evidence, coverage ledger, screenshots, console/network review, findings, and post-fix verification"
  key_links:
    - from: "playwright.config.ts development-proof"
      to: "tests/article-journey.spec.ts"
      via: "astro dev on port 4321 and exact draft proof readiness route"
      pattern: "development-proof"
    - from: "playwright.config.ts production-discovery"
      to: "tests/discovery.spec.ts"
      via: "astro preview on port 4322 after the structural production build"
      pattern: "production-discovery"
    - from: "tests/discovery.spec.ts expectedPublicCorpus"
      to: "src/content/articles and src/content/reviews"
      via: "Node-built-in raw-source/current-strict-sidecar scan after Astro schema/build validation; no route, index, dist, or getPublicArticles input"
      pattern: "createHash.*sha256"
    - from: "tests/discovery.spec.ts expectedPublicCorpus"
      to: "generated dist article routes"
      via: "independent expected-route set equality"
      pattern: "expectedPublicCorpus"
    - from: "tests/discovery.spec.ts expectedPublicCorpus"
      to: "rendered section-index article anchors"
      via: "independent per-section expected-link set equality, followed by a separate built-route-to-index wiring assertion"
      pattern: "expectedPublicCorpus"
    - from: "tests/discovery.spec.ts"
      to: "src/content/reviews/*.json"
      via: "extract any real internal reviewer values only for negative public-output assertions"
      pattern: "reviewer"
    - from: "Hercules coverage ledger"
      to: "all Phase 3 structural routes/states"
      via: "visible Chromium viewport/full-page/responsive/interaction evidence and root-cause rechecks"
      pattern: "tested|fixed|blocked"
---

<objective>
Prove the structural Phase 3 implementation in distinct development and production browser modes, then close every phase-scoped visual or logic finding.

Purpose: Prevent a false green caused by draft-serving development mode, while demonstrating the entire Arabic discovery graph is semantic, no-JS, responsive, accessible, visually consistent, and free of proof/reviewer leakage.

Output: Two unambiguous Playwright projects/servers, a production discovery test matrix that adapts from truthful empty state to real approved content, updated development article coverage, and evidence-first Hercules QA under `.artifacts/`.

<acceptance_criteria>
- Given the development project, both draft proof URLs return 200 and retain all Phase 2 Markdown/MDX/media behavior plus shared header and contextual section/author links.
- Given the production project before real inputs, `/`, all three empty section roots, and `/عن-أحمد-المنجاوي/` return 200, while every proof route returns 404 and no proof/reviewer trace occurs in HTML or `dist`.
- Given the same production suite before or after Plan 03-04, it derives an independent expected public corpus from validated source files plus current strict approval sidecars without calling `getPublicArticles()` or reading route/index output, separately proves that corpus equals generated article routes and section-index links, retains route-to-index equality as a third wiring assertion, proves date-desc/slug-asc order, follows each contextual link, and requires no hardcoded real article fact.
- Given JavaScript disabled and keyboard-only use, the static graph remains traversable with native anchors, logical focus order, and the exact visible 3px focus style.
- Given routes at 320, 390, 768, 1024, and 1440 CSS pixels plus native 200% zoom review, content reflows without clipping/overlap/two-dimensional scrolling and axe reports zero serious/critical in-scope findings.
- Given rendered visual QA, every route/state/control is accounted for in a coverage ledger and no phase-scoped finding remains unfixed or silently untested.
</acceptance_criteria>
</objective>

<execution_context>
@C:/Users/saieh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/saieh/.codex/get-shit-done/templates/summary.md
@C:/Users/saieh/.agents/skills/hercules-visual-qa/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-real-content-and-section-discovery/03-SPEC.md
@.planning/phases/03-real-content-and-section-discovery/03-CONTEXT.md
@.planning/phases/03-real-content-and-section-discovery/03-UI-SPEC.md
@.planning/phases/03-real-content-and-section-discovery/03-RESEARCH.md
@.planning/phases/03-real-content-and-section-discovery/03-VALIDATION.md
@.planning/phases/03-real-content-and-section-discovery/03-PATTERNS.md
@.planning/phases/03-real-content-and-section-discovery/03-01-SUMMARY.md
@.planning/phases/03-real-content-and-section-discovery/03-02-SUMMARY.md
@.planning/phases/02-complete-arabic-article-journey/02-VERIFICATION.md
@playwright.config.ts
@tests/article-journey.spec.ts
@src/layouts/SiteLayout.astro
@src/pages/index.astro
@src/pages/[section]/index.astro
@src/pages/عن-أحمد-المنجاوي.astro
@src/pages/[section]/[slug].astro

<interfaces>
Preserve these test/runtime boundaries:
- `npm run verify` builds production output before `npm run test:browser`; production preview therefore serves the same `dist` inspected by discovery tests.
- Development proof project base URL is `http://127.0.0.1:4321`; production discovery project base URL is `http://127.0.0.1:4322`.
- `tests/article-journey.spec.ts` owns the two known draft proof fixtures and their full Phase 2 reader/media matrix.
- `tests/discovery.spec.ts` must derive its expected public corpus independently from validated raw article sources plus current strict review sidecars, using `sectionRegistry` only for registered route mapping. It must not use `getPublicArticles()`, generated routes, section-index output, or duplicated future real content facts as the expected-data source.
</interfaces>
</context>

## Phase Goal

**As a** visitor, **I want to** discover substantive, human-reviewed launch content through the homepage, three section indexes, and truthful author context, **so that** I can find relevant material and understand who published it.

## Artifacts this phase produces

- New Playwright project identifier `development-proof` using `article-journey.spec.ts`, Astro dev, port 4321, and the Markdown proof readiness URL.
- New Playwright project identifier `production-discovery` using `discovery.spec.ts`, Astro preview, port 4322, and `/` as readiness URL.
- New file `tests/discovery.spec.ts`.
- New test helpers `orderedSections`, `expectedPublicCorpus()`, `builtArticlePathsForSection(sectionSlug)`, `renderedArticlePathsForSection(page)`, `expectArabicDocument(page)`, `expectNoHorizontalOverflow(page)`, and `internalReviewerValues()`; these remain test-local and are not production abstractions.
- New production browser scenarios for homepage registry parity, section route/set/sort parity, truthful author omission, article contextual links when populated, proof/reviewer absence, no JavaScript, keyboard/focus, five-width reflow, zero eager discovery requests, visual-token exclusions, and serious/critical axe checks.
- Updated development article scenarios for the shared site header and section/author fact anchors.
- New ignored artifact root `.artifacts/hercules-visual-qa/phase-03-structural/` with identity evidence, coverage ledger, viewport/full-page/focus screenshots, console/network records, report, and any before/after fix evidence.
- No screenshot, report, trace, browser profile, generated payload, or QA ledger is created under `src/`, `tests/`, or `.planning/`.

<tasks>

<task type="auto">
  <name>Task 1: Separate development proof and production discovery browser identities</name>
  <files>playwright.config.ts, tests/article-journey.spec.ts</files>
  <read_first>
    - `playwright.config.ts` — preserve the exact installed Playwright configuration style, non-reused server lifecycle, Chromium device, and all `.artifacts/` output paths.
    - `tests/article-journey.spec.ts` — preserve every Phase 2 assertion and both known proof fixtures; change only mode/shared-shell/context assumptions made obsolete by Plans 03-01/03-02.
    - `package.json` — `verify` already builds before `test:browser`; do not add a package or redundant process manager.
    - `src/pages/[section]/[slug].astro` — current direct-development mode branch, shared shell, and contextual links the dev suite must prove.
    - `src/content/articles/contract-markdown.md` and `src/content/articles/contract-mdx.mdx` — both are now draft-only and still have exact known paths/copy.
    - `.planning/phases/03-real-content-and-section-discovery/03-CONTEXT.md` D-15, D-17, and D-19 — mode separation, proof isolation, and browser verification.
    - `.planning/phases/03-real-content-and-section-discovery/03-PATTERNS.md` `tests/article-journey.spec.ts` and `playwright.config.ts` — server/project ownership and assertions to retain.
    - `.planning/phases/03-real-content-and-section-discovery/03-VALIDATION.md` tasks 03-03-01/02 and threat T-03-04/T-03-06 — Nyquist and privacy requirements.
  </read_first>
  <action>
Replace the interim single development setup with one Playwright config containing two explicitly named Chromium projects and two Playwright-managed web servers. Preserve `testDir`, exact Desktop Chrome device, trace/screenshot/video retention, HTML report, output, and snapshot paths under `.artifacts/`; keep `reuseExistingServer: false` and `ASTRO_PREVIEW_BACKGROUND: "0"`. The `development-proof` project matches only `article-journey.spec.ts`, sets `baseURL` to port 4321, and uses `npm run dev -- --host 127.0.0.1 --port 4321` with the Markdown proof readiness URL. The `production-discovery` project matches only `discovery.spec.ts`, sets `baseURL` to port 4322, and uses `npm run preview -- --host 127.0.0.1 --port 4322` with `/` as readiness URL. Do not let either project inherit the other's base URL/test file or use an unrelated existing process (D-17).

Update the existing article matrix without reducing its 26-case behavior: assert the shared header has exactly one visible `مدونة أحمد المنجاوي` home anchor before `main`; section/author fact values are same-tab anchors to their registered section root and `/عن-أحمد-المنجاوي/`; no positive tabindex/client router exists; the final media/article order remains intact. Keep both proof routes 200 only in this development project. Keep the `dist` assertions introduced by Plan 03-01 negative for both proofs and their example/reference/video traces. Remove/move no Phase 2 no-JS, player, degraded, keyboard, provenance, summary, bidi, reflow, axe, or exact visual-token check (D-15).

Run each project independently and inspect Playwright's project/server logs to prove which app/port/route served it. A test reaching the wrong server mode is a failure, even if assertions happen to pass.
  </action>
  <verify>
    <automated>`npm run build && npx playwright test --project=development-proof tests/article-journey.spec.ts` exits 0, while `npx playwright test --project=production-discovery tests/article-journey.spec.ts` selects zero tests by project matching. After Task 2 exists, the converse project/file selection must also hold.</automated>
  </verify>
  <acceptance_criteria>
    - Given development-proof, both draft proof URLs return 200 and all unchanged Phase 2 behaviors plus shared/context links pass.
    - Given production output, both proof paths remain absent from `dist`; no development selector is used by the production server.
    - Project names, ports, base URLs, test matches, and readiness URLs are distinct and unambiguous.
    - All generated browser outputs remain beneath `.artifacts/playwright`; no new package/script/process manager is added.
    - No test is skipped, focused, converted to warning-only, or weakened to accommodate draft demotion/shared layout.
  </acceptance_criteria>
  <done>The same browser command can prove draft preview and production isolation without confusing their server identities.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the production discovery graph, empty/populated states, and accessibility</name>
  <files>tests/discovery.spec.ts</files>
  <read_first>
    - `tests/article-journey.spec.ts` — copy only the established Playwright/axe/helper patterns, not proof-specific facts.
    - `playwright.config.ts` — target only the production-discovery project/base URL.
    - `src/config/registries.ts` — import exact section labels/descriptions/slugs/order and author name; never duplicate future content facts.
    - `src/pages/index.astro`, `src/pages/[section]/index.astro`, `src/pages/عن-أحمد-المنجاوي.astro`, and `src/pages/[section]/[slug].astro` — routes/semantics under test.
    - `src/content/articles/contract-markdown.md` and `src/content/articles/contract-mdx.mdx` — exact proof titles/paths/example/video traces that production must reject.
    - `.planning/phases/03-real-content-and-section-discovery/03-UI-SPEC.md` `Verification and Test Matrix`, `State Matrix`, `Responsive`, `RTL`, `Interaction`, and `Accessibility` — exhaustive expected states.
    - `.planning/phases/03-real-content-and-section-discovery/03-VALIDATION.md` `Wave 0 Requirements` and `Manual-Only Verifications` — required automated/manual boundaries.
    - `.planning/phases/03-real-content-and-section-discovery/03-PATTERNS.md` `tests/discovery.spec.ts` — dynamic empty/populated and artifact-safe pattern.
  </read_first>
  <action>
Create `tests/discovery.spec.ts` with the local helpers named under phase artifacts. Import `sectionRegistry` for exact registry facts/order. Make `builtArticlePathsForSection()` enumerate only direct article subdirectories under `dist/{section.slug}/` that contain `index.html`, excluding the section root itself; this is the generated-route observation and must never serve as the expected public corpus. Make `renderedArticlePathsForSection()` read only the section index's article anchors. Keep both observations separate from the oracle below.

Replace built output as the expected-data source with a test-only `expectedPublicCorpus()` oracle that runs only after `astro check` and `astro build` have validated the collection. Using Node built-ins only, recursively enumerate article `.md`/`.mdx` source files, inspect the already-schema-validated frontmatter to identify every literal `draft: false` record, and extract only its validated section key and public slug. For every such source, require exactly one matching review sidecar; strictly validate the sidecar's exact known fields/literals/types, source path, article slug, `classification: "launch"`, both pass records/human flags/current non-future dates, and lowercase SHA-256 against the raw source bytes. Derive the stable expected route from the registered section slug plus public article slug. Reject duplicate sources, sidecars, IDs, slugs, section/routes, missing or extra approval fields, stale bytes, and unregistered sections. Never call/import `getPublicArticles()`, article-route generation, section-index output, or `dist` while constructing the oracle, and never include reviewer identity values in returned data, diagnostics, snapshots, logs, or assertion output.

Compute three distinct assertions for every registered section and for the zero-corpus state: (1) oracle routes equal generated public article routes in `dist`; (2) oracle routes equal rendered section-index article hrefs; and (3) generated article routes equal rendered section-index hrefs as a separate wiring check. A valid approved source omitted by the shared selector must therefore fail both independent comparisons instead of disappearing from both sides. Keep the linked h2/description/time anatomy and rendered date-descending/direct-slug-ascending checks, then follow every generated internal link and require a successful response (D-04 through D-07, D-17).

Cover: homepage one-h1/one-main/Arabic-RTL and exact three registry labels/descriptions/anchors in order; all three registered section roots and unknown-section absence; author exact h1/purpose/home link plus complete omission of biography/expertise/affiliation/credential/social/channel/review remnants; shared header exactly once on every route; and, conditionally for every built public article, section/author fact anchors plus successful section/author targets (D-01 through D-10). Do not hardcode any future article title, slug, date, reference, reviewer, or video.

Assert both proof paths return 404 from production preview and `dist` contains neither proof title, `example.com`, nor `dQw4w9WgXcQ`. Recursively scan built text for sidecar field names (`articleId`, `sha256`, `classification`, `editorial`, `religiousAccuracy`, `reviewer`, `approvedAt`) and, when review JSON files later exist, compare reviewer strings through a non-echoing boolean/custom assertion whose failure message never includes the value. Never return reviewer values from the corpus oracle or log/snapshot/expose those internal identities (D-13, D-17).

Add a JavaScript-disabled context that follows header/home/section/author static anchors and, when a public article exists, completes homepage → section → article → author → homepage while retaining the permanent YouTube link. Add keyboard tests for native anchors, DOM order, no positive tabindex, Enter activation, underlines, and computed 3px green focus/offset; responsive loops over 320/390/768/1024/1440 for homepage, all sections, author, and every built article, requiring document scroll width <= client width and visible non-clipped content; request capture proving discovery/author pages load no remote assets/runtime fetch; source/computed checks for exact four sizes/two weights/palette/70ch/one 48rem padding breakpoint and absence of cards/images/icons/shadows/gradients/motion/webfonts/line clamp/global overflow concealment; and axe WCAG A/AA/2.2 with only the out-of-phase document-title rule disabled, requiring zero serious/critical findings (D-18, D-19).

The suite must pass now with truthful empty sections and later with approved populated sections; never branch on a fabricated environment flag or make empty production output count as launch ready.
  </action>
  <verify>
    <automated>`npm run check && npm run build && npx playwright test --project=production-discovery tests/discovery.spec.ts` exits 0 with the independent source/sidecar oracle separately equal to generated routes and rendered index links, the additional route-to-index wiring equality, three truthful empty indexes, exact homepage/author surfaces, proof/reviewer absence, no-JS, keyboard, five-width reflow, and zero serious/critical in-scope axe findings. Then `npm run test:browser` exits 0 across both projects.</automated>
  </verify>
  <acceptance_criteria>
    - Given the current empty public corpus, the independent oracle, generated-route set, and rendered-index set are all empty and all structural routes/states pass without a fake populated fixture or launch-ready claim.
    - Given any later approved source and current strict sidecar, the same suite independently derives its expected section/route from source evidence, separately detects omission from generated routes or index links, retains route-to-index equality, and checks order, contextual links, no-JS full journey, and route success.
    - The oracle uses no public selector, route/index output, `dist`, hardcoded real fact, or reviewer value in its expected-data derivation or diagnostics.
    - Proof routes/traces, reviewer values/fields, and generic approval statements are absent from production DOM and `dist`.
    - All routes use Arabic/RTL, one main/h1, native headings/lists/time/anchors, meaningful names, visible focus, and no positive tabindex/redundant ARIA.
    - All locked widths reflow without concealment; discovery/author pages issue no remote/runtime request; axe has zero serious/critical in-scope findings.
    - No browser output escapes `.artifacts/`, and no test reads `.env` or exposes reviewer values in output.
  </acceptance_criteria>
  <done>Production discovery has an executable, corpus-adaptive browser contract independent from the development proof journey.</done>
</task>

<task type="auto">
  <name>Task 3: Run evidence-first visual and logic QA, fix root causes, and re-verify</name>
  <files>src/layouts/SiteLayout.astro, src/pages/index.astro, src/pages/[section]/index.astro, src/pages/عن-أحمد-المنجاوي.astro, src/pages/[section]/[slug].astro, tests/article-journey.spec.ts, tests/discovery.spec.ts</files>
  <read_first>
    - `C:/Users/saieh/.agents/skills/hercules-visual-qa/SKILL.md` and `workflows/test-and-fix.md` — required visible-browser, identity, ledger, root-cause, evidence, and report workflow.
    - `C:/Users/saieh/.agents/skills/hercules-visual-qa/references/coverage-contract.md` — account for every route/control/state as tested/fixed/failed/untested/blocked/out_of_scope.
    - `C:/Users/saieh/.agents/skills/hercules-visual-qa/references/visual-ux-checklist.md` — viewport/full-page/responsive/interaction quadracheck.
    - `C:/Users/saieh/.agents/skills/hercules-visual-qa/references/hercules-agent-runner.md` — Chrome DevTools first and honestly labelled Playwright/Hercules fallback.
    - `C:/Users/saieh/.agents/skills/hercules-visual-qa/references/report-format.md` — findings-first evidence and redaction contract.
    - `.planning/phases/03-real-content-and-section-discovery/03-UI-SPEC.md` — exact visual/copy/state source of truth; do not substitute generic taste.
    - `playwright.config.ts`, `tests/article-journey.spec.ts`, and `tests/discovery.spec.ts` — know what automation covers before manual visual/logic review.
    - Every source file listed in `<files>` — inspect the root cause/callers before any fix; preserve unrelated/user changes.
  </read_first>
  <action>
Run the Hercules `test-and-fix` workflow in the current session, with no subagent. Verify repository/branch/commit, exact runtime, server process/ports, resolved URLs, Arabic app marker, and test-data identity before accepting evidence. Use visible Chrome DevTools MCP when available; otherwise follow the documented runner order and label the actual backend. Store the complete run only under `.artifacts/hercules-visual-qa/phase-03-structural/`; redact internal reviewer values, cookies, tokens, private URLs, and irrelevant identifiers (D-19).

Inventory and ledger: homepage; each of three section roots in empty state; author page; both direct development proof article routes; unknown section/proof production absence; header, every section link, author/home link, contextual fact links, video controls/direct action on proof routes; normal/no-JS/keyboard/focus states; and any error/blocked state. Capture viewport and full-page screenshots at 320, 390, 768, 1024, and 1440 CSS pixels for the important structural surfaces, focus/interaction screenshots, and native 200% zoom evidence on homepage, longest/empty section, author, and one proof article. Check Arabic joining/diacritics/bidi, hierarchy, exact flat list rhythm, 44px standalone home target, underlines/focus, no clipping/overflow, console/page errors, HTTP failures, and unexpected network/third-party requests (D-18, D-19).

Treat logic as equal priority: actually follow every ordinary link; confirm server-mode separation; test no-JS navigation; ensure empty sections remain visible but launch readiness stays nonzero; confirm proof/article contextual links and permanent media fallback; and compare DOM/accessibility tree landmarks/headings/lists/names/order. Maintain the required coverage ledger; no item may disappear because it is inconvenient. Real populated production content is marked `blocked` with the truthful missing-input reason and is owned by Plan 03-04, not simulated.

For every confirmed phase-scoped issue, reproduce first, grep every caller of the shared helper/layout before editing, fix the smallest root cause, add/adjust the focused regression assertion when nontrivial, and capture before/after plus adjacent-viewport evidence. Run `npx ui-skills start` before any UI fix, but do not add a dependency or accept conflicting visual suggestions. Re-run the exact scenario, console/network checks, focused Playwright project, and the full structural gate. Finish only with a findings-first report whose ledger has no untested structural item and no unresolved phase-scoped failed finding.
  </action>
  <verify>
    <automated>`npm run verify` exits 0 after all fixes; the wrapped expected-red `npm run launch:ready` still proves all missing sections rather than being waived. `git diff --check` passes, and all Playwright/Hercules paths resolve beneath ignored `.artifacts/`.</automated>
    <human-check>Visible-browser inspection records native 200% zoom, Arabic joining/bidi/diacritics, hierarchy, focus, route/control logic, console/network state, and screenshot review in the Hercules ledger/report; every structural item is tested/fixed or explicitly blocked only by absent real content.</human-check>
  </verify>
  <acceptance_criteria>
    - The report records target identity, branch/commit, ports, resolved URLs, backend, viewports, redaction, and test-data boundary.
    - Viewport/full-page/responsive/interaction evidence is visually reviewed, not merely captured.
    - Every structural route/control/state is `tested` or `fixed`; real-populated content alone may be `blocked` for Plan 03-04, and no item is silently untested.
    - Every confirmed source defect has root cause, before evidence, smallest fix, after evidence, and runnable regression verification.
    - Full structural automation is green and launch readiness remains intentionally nonzero for missing truthful inputs.
    - No evidence, profile, trace, screenshot, log, or report is tracked outside `.artifacts/`.
  </acceptance_criteria>
  <done>The empty-state discovery graph and development article context are both automated and visibly verified with no unresolved Phase 3 structural defect or false production claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright project → local Astro server | Each project must prove its own mode/port/app identity or tests can pass against the wrong visibility boundary. |
| Internal source/review data → test output/artifacts | Tests may inspect sidecar identities only to prove absence; they must never print or screenshot them. |
| Validated source/current sidecar corpus → generated routes and section indexes | Expected public membership must be derived independently so a shared-selector omission cannot disappear from both outputs and still pass. |
| Automated assertions → visual/accessibility claim | DOM checks alone cannot prove Arabic rendering, native zoom, focus visibility, or screenshot quality; visible evidence and ledger close the gap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information integrity | Development/production proof boundary | mitigate | Distinct ports/projects/testMatch/readiness URLs plus positive dev and negative production proof assertions. |
| T-03-05 | Spoofing | Launch status during browser QA | mitigate | Keep expected-red readiness assertion; empty structural pages never count as a launch pass. |
| T-03-06 | Information disclosure | Reviewer values in dist/DOM/report | mitigate | Negative scans, no logging/snapshots of values, report redaction, artifacts ignored. |
| T-03-08 | Spoofing | Internal link graph | mitigate | Follow every registered/internal anchor against the intended server and require successful, correctly named destinations. |
| T-03-11 | Denial of service / accessibility | Narrow/zoomed Arabic layout | mitigate | Five-width automation, native 200% visual evidence, no overflow concealment, keyboard/axe/tree checks. |
| T-03-12 | Repudiation | Shallow visual QA claim | mitigate | Required identity evidence, coverage ledger, findings-first report, before/after paths, and blocked/untested accounting. |
| T-03-16 | Information integrity | Shared public selector feeding both article routes and indexes | mitigate | Build an independent raw-source/current-sidecar oracle, compare it separately with generated routes and rendered index links, then retain route-to-index equality as a third assertion. |
| T-03-SC | Tampering | Browser/package supply chain | accept | Use the existing exact Playwright/axe/Chromium lock; no install, update, or new runner package occurs. |
</threat_model>

<verification>
- Focused dev gate: `npm run build && npx playwright test --project=development-proof tests/article-journey.spec.ts`.
- Focused production gate: `npm run check && npm run build && npx playwright test --project=production-discovery tests/discovery.spec.ts`, including independent oracle-to-route, oracle-to-index, and route-to-index equality.
- Composite gate: exact-runtime `npm run verify`.
- Truth guard: wrapped `npm run launch:ready` must remain nonzero until Plan 03-04.
- Visual/logic gate: Hercules test-and-fix report and complete ledger under `.artifacts/hercules-visual-qa/phase-03-structural/`.
- No `.env` file is read; no artifact leaves `.artifacts/`; no skipped/focused/warning-only test remains.
</verification>

<success_criteria>
- Development and production browser modes cannot be confused.
- The structural graph passes static, semantic, no-JS, keyboard, responsive, accessibility, proof-isolation, privacy, and visible QA checks.
- The production suite is already able to validate genuine populated indexes/article links after Plan 03-04 from an independent source/current-sidecar oracle, without hardcoded future facts or common-mode selector omissions.
- No valid Phase 3 structural finding remains unresolved; real-content states are truthfully blocked rather than fabricated.
</success_criteria>

<scope_guard>
Do not invent populated screenshots/data; add a fake public article/approval; expose reviewer values; implement metadata/schema/sitemap/robots/404; add hosting/analytics/production checks; install a runner/UI/a11y dependency; or broaden UI beyond the approved flat text surfaces. Plan 03-04 owns truthful populated/final evidence.
</scope_guard>

<output>
Create `.planning/phases/03-real-content-and-section-discovery/03-03-SUMMARY.md` with commands/results and the ignored Hercules report path. Do not mark Phase 3 complete while Plan 03-04 inputs/readiness remain blocked.
</output>
