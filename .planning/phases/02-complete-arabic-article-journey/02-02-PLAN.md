---
phase: 02-complete-arabic-article-journey
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/lib/content-contract.ts
  - src/content.config.ts
  - tests/content-contract.test.ts
  - src/content/articles/contract-markdown.md
  - src/pages/[section]/[slug].astro
  - src/content/articles/contract-mdx.mdx
  - tests/article-journey.spec.ts
  - README.md
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - ART-01
  - ART-02
  - ART-03
  - ART-05
  - ART-06
  - ART-07
  - QUAL-01
  - QUAL-03
  - QUAL-04

must_haves:
  truths:
    - "Per D-03, malformed, non-Arabic-facing, relative, non-HTTPS, or credential-bearing references fail at the shared content boundary with source/index/field diagnostics."
    - "Per D-02, D-04, and D-05, one public proof renders its registry facts, UTC publication/update dates, and semantic Arabic references exactly once while the other renders no empty optional unit."
    - "Per D-01, D-06, D-09 through D-11, and D-21, Markdown and approved MDX share the same complete Arabic document order, typography, semantics, restricted component map, and final route family."
    - "Per D-19 and D-20, reference support extends the existing ArticleData/schema/validator only; it creates no second model, route, registry, MDX permission, UI framework, or production dependency."
    - "Per D-22, executable Node and browser checks cover references, provenance presence/absence, Markdown/MDX parity, Arabic-only UI, headings, bidi isolation, responsive widths, axe, and Phase 1 regressions."
  artifacts:
    - path: "src/lib/content-contract.ts"
      provides: "ArticleReference type plus fail-closed structured-reference semantic policy"
      exports: ["ArticleReference", "ArticleData", "validateArticleData"]
    - path: "src/content.config.ts"
      provides: "Optional strict references array at the Astro collection shape boundary"
      contains: "references"
    - path: "src/pages/[section]/[slug].astro"
      provides: "Conditional semantic reference rendering and clean optional absence"
      contains: "المراجع"
    - path: "src/content/articles/contract-mdx.mdx"
      provides: "Complete approved-MDX reader proof without optional update/reference facts"
      contains: "## الخاتمة"
    - path: "tests/article-journey.spec.ts"
      provides: "Markdown/MDX parity, provenance, bidi, reflow, quality, and accessibility regression checks"
      contains: "accessibility"
    - path: "README.md"
      provides: "Arabic authoring contract for complete structure, structured references, and full browser verification"
      contains: "references"
  key_links:
    - from: "src/content.config.ts"
      to: "src/lib/content-contract.ts"
      via: "strict reference shape followed by validateArticleData semantic policy"
      pattern: "references.*validateArticleData"
    - from: "src/pages/[section]/[slug].astro"
      to: "article.data.references"
      via: "complete-section conditional before the media continuation"
      pattern: "references\\?\\.length|references.length"
    - from: "src/pages/[section]/[slug].astro"
      to: "src/config/registries.ts"
      via: "unchanged registry-backed author/section facts"
      pattern: "sectionRegistry|authorRegistry"
    - from: "tests/content-contract.test.ts"
      to: "validateArticleData"
      via: "table-driven source/index/field diagnostics for reference policy"
      pattern: "references"
    - from: "tests/article-journey.spec.ts"
      to: "both public Markdown and MDX routes"
      via: "one shared route matrix"
      pattern: "contract-markdown|اختبار-مكون-ام-دي-اكس"
---

<objective>
Complete the truthful provenance and format-parity slice: validated structured references, clean optional rendering, and equivalent full Arabic reading behavior for Markdown and approved MDX.

Purpose: Make every textual/provenance requirement true before media enhancement, while keeping article identity citable and truthful without pulling Phase 3 author pages or Phase 4 metadata/schema into scope.

Output: One reference type/schema/validator extension, a present/absent proof matrix, full approved-MDX content, expanded Node/browser coverage, and synchronized Arabic authoring instructions.

<acceptance_criteria>
- `npm test` passes every Phase 1 case plus reference cases for absent/empty/valid data, invalid shapes/labels, and unsafe destinations.
- `npm run check && npm run build` emits the same two public routes and no draft route; one public route contains update/references and the other contains neither unit.
- `npx playwright test tests/article-journey.spec.ts --project=chromium -g "Arabic surface|Arabic document semantics|complete text-first|provenance|summary|bidi|reflow|accessibility|quality"` exits 0.
- Axe reports zero serious/critical violations on both routes before media activation, and all browser artifacts remain under `.artifacts/`.
</acceptance_criteria>
</objective>

<execution_context>
@C:/Users/saieh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/saieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-complete-arabic-article-journey/02-SPEC.md
@.planning/phases/02-complete-arabic-article-journey/02-CONTEXT.md
@.planning/phases/02-complete-arabic-article-journey/02-UI-SPEC.md
@.planning/phases/02-complete-arabic-article-journey/02-RESEARCH.md
@.planning/phases/02-complete-arabic-article-journey/02-VALIDATION.md
@.planning/phases/02-complete-arabic-article-journey/02-PATTERNS.md
@.planning/phases/02-complete-arabic-article-journey/02-01-SUMMARY.md
@src/lib/content-contract.ts
@src/content.config.ts
@src/pages/[section]/[slug].astro
@src/content/articles/contract-markdown.md
@src/content/articles/contract-mdx.mdx
@tests/content-contract.test.ts
@tests/article-journey.spec.ts

<interfaces>
Interfaces produced by Plan 02-01 and existing Phase 1 contracts:

From `src/components/YouTubePlayer.astro`:
- Local props are `{ youtubeId: string; title: string }`; the component owns the permanent direct action and remains outside authored MDX.

From `src/lib/content-contract.ts` before this plan:
- `ArticleData` is the single article fact model consumed by schema, route helpers, selectors, and tests.
- `validateArticleData(data, source, options)` is the shared fail-closed semantic boundary.
- Existing helpers `assertNonEmpty`, `isArabicLetter`, and `fail` already provide the required validation/diagnostic pattern; extend in place rather than creating another validator module.

From `src/content.config.ts`:
- The collection uses built-in `astro/zod`, a local `nonEmpty` primitive, a strict object shape, and one `.superRefine()` call to `validateArticleData`.
</interfaces>
</context>

## Phase Goal

**As an** Arabic reader, **I want to** read a complete accessible article and reach its matching YouTube video, **so that** I can learn even when media or JavaScript is unavailable.

## Artifacts this phase produces

- New exported `ArticleReference` type with exact `{ label: string; url: string }` fields.
- New optional `ArticleData.references?: readonly ArticleReference[]` contract and corresponding strict Astro schema field.
- New `validateArticleData` reference branches for array/object shape, Arabic-facing non-empty labels, absolute `https:` URL, hostname, and empty credentials.
- New named browser scenarios `Arabic surface`, `Arabic document semantics`, `complete text-first`, `provenance`, `summary`, `bidi`, `reflow`, `accessibility`, and `quality` in the existing Phase 2 browser spec.
- No new file outside the files listed in frontmatter and no new production dependency.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-closed references and render truthful optional provenance end to end</name>
  <files>src/lib/content-contract.ts, src/content.config.ts, tests/content-contract.test.ts, src/content/articles/contract-markdown.md, src/pages/[section]/[slug].astro</files>
  <read_first>
    - `src/lib/content-contract.ts` — reuse `ArticleData`, `assertNonEmpty`, `isArabicLetter`, `fail`, and `validateArticleData`; preserve all slug/date/video/query contracts.
    - `src/content.config.ts` — extend the single built-in Astro Zod object and existing `.superRefine()` only.
    - `tests/content-contract.test.ts` — retain all Phase 1 cases and the valid-data/table-driven diagnostic style.
    - `src/pages/[section]/[slug].astro` — retain Plan 02-01 document order/styles/component boundary; references belong after authored conclusion and before media.
    - `.planning/phases/02-complete-arabic-article-journey/02-CONTEXT.md` decisions D-02 through D-05, D-19 through D-22.
    - `.planning/phases/02-complete-arabic-article-journey/02-RESEARCH.md` sections `Pattern 1`, `Pattern 2`, `Conditional References`, pitfalls 1/5/8/9, and threats T2-01/T2-05/T2-06/T2-09.
    - `.planning/phases/02-complete-arabic-article-journey/02-UI-SPEC.md` `Copywriting Contract`, `Component Inventory`, `RTL and Bidirectional Contract`, and `State Matrix`.
  </read_first>
  <behavior>
    - `references` omitted or `[]` validates and emits no references section, separator, or reserved gap.
    - A non-empty list with Arabic-facing labels and absolute credential-free HTTPS URLs validates and renders one semantic `section > h2 + ul`.
    - Blank/non-Arabic-facing labels and malformed/relative/HTTP/JavaScript/credential-bearing URLs fail with `source.references.{index}.{field}` diagnostics.
    - Publication/update dates are stable Arabic long dates in `<time>`; no update row exists when `updatedAt` is absent.
  </behavior>
  <action>
Before changing production code, extend `tests/content-contract.test.ts` with table-driven RED cases that call the real `validateArticleData`: valid `undefined`, empty, and non-empty references; non-array and wrong-entry shapes via deliberately malformed runtime values; blank and Latin-only visible labels; malformed, relative, `http:`, `javascript:`, and credential-bearing URLs; and one absolute credential-free HTTPS URL. Require diagnostic matches for article source, zero-based reference index where applicable, field, and rule. Keep every existing slug/path/date/draft/MDX case unchanged (D-03/D-22, T2-01).

Add exported `ArticleReference = { label: string; url: string }` and optional readonly `references` to the existing `ArticleData`; create no model file. In `validateArticleData`, reject non-array/wrong-entry runtime values with location-rich errors, reuse `assertNonEmpty`, require at least one Arabic-script letter in the visible label, parse destinations with native `URL` without a base, and require `protocol === "https:"`, non-empty hostname, and empty username/password. Do not repair input, allow relative URLs, accept a content-authored embed URL, or use an ad-hoc URL regex.

In `src/content.config.ts`, add only `references: z.array(z.object({ label: nonEmpty, url: nonEmpty }).strict()).optional()` and keep semantic URL/Arabic policy delegated to `validateArticleData` (D-19/D-20). Run the RED tests, implement until GREEN, then run the entire Node suite.

Because the task also changes rendered UI, run `npx ui-skills start` before editing the route. Put a truthful `updatedAt` and at least one descriptive Arabic-labelled HTTPS reference on only `contract-markdown.md`; keep its contract-fixture disclosure and every identity field unchanged. In the route, guard the entire references unit with `article.data.references?.length`: render a `section` after `<Content>` and before `YouTubePlayer`, an `h2` with exact text `المراجع`, and a semantic list of ordinary underlined same-tab links using the validated Arabic label. Emit no wrapper/heading/border/gap for an empty/absent list; update facts remain a complete conditional `dl` row. Do not expose raw URLs as link labels and do not add `target` (D-02/D-04/D-05, T2-04/T2-09).

After editing, run Hercules visual QA on the Markdown route at mobile and desktop widths, placing all evidence only under `.artifacts/hercules-visual-qa/phase-02-plan-02-provenance/`. Verify the references section follows the conclusion, precedes media, and does not disturb the single-column measure or spacing. Fix every provenance/order/spacing/contrast defect before completing the task.
  </action>
  <verify>
    <automated>`npm test && npm run check && npm run build && npx playwright test tests/article-journey.spec.ts --project=chromium -g "complete Markdown reader|provenance"` exits 0.</automated>
  </verify>
  <acceptance_criteria>
    - Every new invalid reference case fails through production `validateArticleData` with the expected source/index/field/rule; all prior tests stay green.
    - The collection schema is strict at each reference entry and imports no new Zod/package abstraction.
    - Markdown built output has exactly one update fact and one `المراجع` section; each reference link is descriptive Arabic, HTTPS, same-tab, and free of credentials.
    - No template-side URL repair/try-catch, duplicated author/section fact, empty optional node, weakened YouTube ID rule, MDX permission, or draft-query bypass is introduced.
    - Hercules evidence is ignored beneath `.artifacts` and records no unresolved reader defect.
  </acceptance_criteria>
  <done>A reader receives truthful registry-backed provenance and conditional references, and malformed authoring input fails before public rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove complete Markdown/MDX parity, Arabic bidi readability, reflow, and accessibility</name>
  <files>src/content/articles/contract-mdx.mdx, tests/article-journey.spec.ts, README.md</files>
  <read_first>
    - `src/content/articles/contract-mdx.mdx` — preserve title/section/author/slug/date/draft/video ID and the sole approved `<ContractNote>` use.
    - `src/components/mdx-components.ts` and `src/lib/mdx-policy.ts` — the allowlist is an invariant; do not add media/reference/bidi components or article imports.
    - `tests/article-journey.spec.ts` — extend the working Plan 02-01 harness rather than adding another spec/helper framework.
    - `README.md` — keep all owner-facing documentation Arabic and synchronized with executable scripts/fields.
    - `.planning/phases/02-complete-arabic-article-journey/02-UI-SPEC.md` — exact copy/state matrix, style values, responsive widths, semantic order, bidi, focus, and registry safety.
    - `.planning/phases/02-complete-arabic-article-journey/02-VALIDATION.md` — per-requirement commands, required scenarios, and manual-only boundary.
    - `C:/Users/saieh/.agents/skills/entity-seo/SKILL.md` — consistent visible author/section identity and citable self-contained prose; exclude metadata/schema/About.
    - `C:/Users/saieh/.agents/skills/fixing-accessibility/SKILL.md` — native structure, Arabic accessible names, focus, keyboard, contrast, and minimal ARIA.
    - `C:/Users/saieh/.agents/skills/better-typography/SKILL.md` — exact restrained hierarchy, measure, wrap, system font, and RTL logical properties.
  </read_first>
  <behavior>
    - Markdown and approved MDX render the identical shell/order/type/spacing/semantics while retaining their authored bodies.
    - Markdown shows update/references; MDX emits neither optional unit nor an empty remnant.
    - URLs/IDs/code-like fragments compute as isolated LTR, dates are isolated auto direction, and Arabic prose/diacritics remain RTL with normal letter spacing.
    - Both routes reflow at 320/390/768/1024/1440, meet computed type/target/color rules, and have zero serious/critical axe violations.
  </behavior>
  <action>
Run `npx ui-skills start` before changing rendered MDX content. Expand `contract-mdx.mdx` into the same complete text-first structure as Markdown: introductory paragraph, sequential `h2` then nested `h3`, substantive Arabic prose, list/quotation/link samples, representative Arabic/ASCII numbers and punctuation, diacritics, inline-code HTTPS URL and 11-character video ID, and final `## الخاتمة`. Keep it explicitly a contract fixture, keep `ContractNote` without import/attributes/expressions, and deliberately omit `updatedAt` and `references` so conditional absence is testable (D-01/D-04/D-21).

Extend the single browser spec before any corrective implementation with named tests matching `02-VALIDATION.md`: `Arabic surface`, `Arabic document semantics`, `complete text-first`, `provenance`, `summary`, `bidi`, `reflow`, `accessibility`, and `quality`. Use a two-route table rather than duplicate suites. Assert exact Arabic chrome/copy; root `lang/dir`; one `h1`; no skipped headings; title → facts → `الخلاصة` → authored introduction/headings/body/conclusion → conditional `المراجع` → media order; optionals present only on Markdown; same registry-backed author/section facts; and no raw English control/status label.

For bidi, assert `<time>` contains `bdi[dir=auto]`, code/raw URL/ID fragments compute `direction:ltr` plus `unicode-bidi:isolate` or use explicit equivalent native isolation, visible Arabic remains RTL, `letter-spacing` is normal, and strings are not truncated. For each exact width 320/390/768/1024/1440, assert `scrollWidth <= clientWidth`, the article cap is 70ch, body font is 18px (and never below 16px), controls are at least 44px, all layout children can shrink, the same DOM order remains one column, and no authored/optional text is hidden. Assert exact four font sizes/two weights, line heights, palette/contrast roles, `3px` focus outline/offset, underlines, spacing/breakpoint values, and absence of `overflow-x:hidden`, decorative effects, webfonts, assets, icons, cards, multi-column UI, or registry code.

Run `AxeBuilder` with WCAG 2 A/AA, 2.1 AA, and 2.2 AA tags on both local routes before any iframe exists; fail only if serious or critical violations are present, then keep explicit semantic/accessible-name assertions because axe is not complete. Verify the JavaScript-disabled context still exposes the complete text and permanent link with no dead activation button (D-06 through D-14, D-17/D-18, D-22).

Update `README.md` in Arabic: show the optional `references` frontmatter array with descriptive Arabic `label` and absolute credential-free HTTPS `url`; document the complete introduction → headings/body → conclusion authoring order, omission behavior for update/references, the exact `preview`/`test:browser`/`verify` commands, and `.artifacts`-only browser evidence. Do not document metadata/schema, final corpus, deployment, analytics, or production certification.

After editing, run Hercules visual QA on both routes at all five widths, saving evidence only under `.artifacts/hercules-visual-qa/phase-02-plan-02-parity/`. Compare the two routes for the same hierarchy/measure/spacing, visually inspect Arabic joining/diacritics/bidi samples, and fix every accessible-name, overflow, typography, contrast, optional-state, or copy defect before completion.
  </action>
  <verify>
    <automated>`npm test && npm run check && npm run build && npx playwright test tests/article-journey.spec.ts --project=chromium -g "Arabic surface|Arabic document semantics|complete text-first|provenance|summary|bidi|reflow|accessibility|quality"` exits 0.</automated>
  </verify>
  <acceptance_criteria>
    - Both route rows pass the exact DOM/order/copy/computed-style/reflow/axe matrix.
    - Markdown alone renders update/references; MDX has no matching heading, wrapper, separator, or reserved spacing.
    - The MDX source contains only the approved component and no raw HTML, script, iframe, import/export, event handler, expression, or unsafe protocol.
    - README reference rules and verification commands match executable schema/scripts exactly and remain Arabic.
    - Hercules reports for both routes and all five widths remain only under `.artifacts` with no unresolved defect.
  </acceptance_criteria>
  <done>A reader receives the same complete, readable, semantic, accessible Arabic journey from either approved content format, including clean optional absence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown/MDX frontmatter → Astro schema/shared validator | Git-authored reference labels and destinations are untrusted until strict shape and semantic validation succeed. |
| Registry keys → visible identity facts | Stable keys select public Arabic labels/names; article bodies must not spoof or duplicate those facts. |
| Restricted authored MDX → shared Astro render map | The existing preflight/allowlist remains the only permitted executable component surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T2-01 | Spoofing / Tampering | `references[].url` and label | mitigate | Strict object shape, Arabic-facing non-empty label, native URL parse, absolute HTTPS/hostname, empty credentials, source/index/field diagnostics, descriptive same-tab anchor. |
| T2-04 | Spoofing / Elevation | Rendered reference links | mitigate | Omit `target` on every reference link and assert it; no new browsing context/opener is created. |
| T2-05 | Tampering / Elevation | MDX proof and render path | mitigate | Keep structural preflight and exact `ContractNote` map unchanged; references and media remain trusted route/component output, never authored JSX. |
| T2-06 | Information Disclosure | Production collection/route | mitigate | Preserve `getPublicArticles()` and assert two public outputs plus absent draft after schema/fixture changes. |
| T2-09 | Spoofing | Optional update/reference provenance | mitigate | Source author/section from registries and guard the complete semantic optional unit so missing values cannot create misleading empty UI. |
| T2-SC | Tampering | Package surface | accept | This plan installs no package; `npm ci` must continue to resolve the exact audited lock from Plan 02-01. |
</threat_model>

<verification>
- Per-task: `npm test`; for rendered changes run the named focused browser group after `npm run build`.
- Wave gate: `npm run check && npm run build && npm run test:browser`.
- Inspect built output for exactly two public routes, no draft, one present optional matrix, one absent optional matrix, and no unapproved MDX/runtime surface.
- Confirm Hercules/Playwright output paths stay beneath ignored `.artifacts`; no evidence file belongs in `src`, `tests`, or `.planning`.
- Do not read or create any `.env` file.
</verification>

<success_criteria>
- Structured references fail closed and render safely only when valid.
- Visible provenance is truthful, registry-backed, UTC-stable, and free of empty optional UI.
- Markdown and approved MDX deliver the same exact UI contract and complete text-first journey.
- Automated accessibility/reflow/bidi/browser checks and Phase 1 regressions are green before media enhancement begins.
</success_criteria>

<scope_guard>
Keep Phase 3 homepage/index/navigation/About/real content, Phase 4 metadata/schema/discovery files/404, Phase 5 deployment/analytics, Phase 6 production certification, and all deferred CMS/search/community/video features out of this plan.
</scope_guard>

<output>
Create `.planning/phases/02-complete-arabic-article-journey/02-02-SUMMARY.md` when done.
</output>
