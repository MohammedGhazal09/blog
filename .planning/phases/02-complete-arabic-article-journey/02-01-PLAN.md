---
phase: 02-complete-arabic-article-journey
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.ts
  - tests/article-journey.spec.ts
  - src/pages/[section]/[slug].astro
  - src/components/YouTubePlayer.astro
  - src/content/articles/contract-markdown.md
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - ART-01
  - ART-02
  - ART-03
  - ART-05
  - ART-06
  - ART-07
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04

must_haves:
  truths:
    - "Per D-01, D-06, and D-18, a reader can consume the complete Arabic text-first Markdown article and use its permanent YouTube action without video playback or client JavaScript."
    - "Per D-02 and D-05, the route resolves the visible Arabic section and author from the central registries and emits a UTC-stable Arabic publication date."
    - "Per D-07, D-08, D-09, and D-10, the reader is a restrained RTL single column that reflows from 320px through 1440px, uses the exact four-size/two-weight system-font typography, and isolates mixed-direction values without hiding overflow."
    - "Per D-11, D-12, D-13, D-14, and D-17, native semantic structure, descriptive Arabic controls, 44px standalone targets, visible focus, no autoplay, and a same-tab permanent direct-video link exist in static HTML."
    - "Per D-19 through D-21, the existing static route, public/preview query split, title-independent paths, draft exclusion, registries, and restricted MDX map remain the only production flow."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Chromium browser-test configuration with every generated artifact beneath .artifacts"
      contains: ".artifacts"
    - path: "tests/article-journey.spec.ts"
      provides: "Executable browser contract beginning with the complete Markdown/direct-link happy path"
      contains: "complete Markdown reader"
    - path: "src/pages/[section]/[slug].astro"
      provides: "Static Arabic article shell, registry facts, labelled summary, authored content, and exact reader styling"
      contains: "<html lang=\"ar\" dir=\"rtl\">"
    - path: "src/components/YouTubePlayer.astro"
      provides: "Permanent Arabic YouTube continuation owned by the media boundary"
      contains: "مشاهدة الفيديو على يوتيوب"
    - path: "src/content/articles/contract-markdown.md"
      provides: "Complete public Markdown reader proof with introduction, ordered headings, body, conclusion, and bidi samples"
      contains: "## الخاتمة"
  key_links:
    - from: "src/pages/[section]/[slug].astro"
      to: "src/lib/articles.ts"
      via: "unchanged getPreviewArticles/getPublicArticles getStaticPaths branch"
      pattern: "getPreviewArticles.*getPublicArticles"
    - from: "src/pages/[section]/[slug].astro"
      to: "src/config/registries.ts"
      via: "registry-key lookup for public section and author facts"
      pattern: "sectionRegistry|authorRegistry"
    - from: "src/pages/[section]/[slug].astro"
      to: "src/components/YouTubePlayer.astro"
      via: "validated youtubeId/title props"
      pattern: "YouTubePlayer"
    - from: "src/components/YouTubePlayer.astro"
      to: "https://www.youtube.com/watch?v={youtubeId}"
      via: "static same-tab href outside any client mutation"
      pattern: "youtube\\.com/watch\\?v="
    - from: "package.json"
      to: "tests/article-journey.spec.ts"
      via: "test:browser and verify scripts"
      pattern: "test:browser"
---

<objective>
Deliver the first usable Phase 2 vertical slice: an exact-runtime browser harness and a complete static Arabic Markdown article whose permanent YouTube action works without JavaScript.

Purpose: Prove the hardest product invariant first—the text and direct continuation remain useful when all media enhancement is absent—while retaining every Phase 1 trust boundary.

Output: Exact browser dev tooling, one RED-to-GREEN end-to-end browser path, the shared Arabic reader route, a cohesive media component boundary, and a complete Markdown proof article.

<acceptance_criteria>
- `node --version` is exactly `v24.19.0` and `npm --version` is exactly `11.17.0` before any lockfile mutation.
- `npm run build && npx playwright test tests/article-journey.spec.ts --project=chromium -g "complete Markdown reader"` exits 0 after Task 2.
- Built Markdown HTML contains `lang="ar"`, `dir="rtl"`, exactly one `h1`, the labelled `الخلاصة`, registry-backed section/author/date facts, authored introduction/body/conclusion, and the exact direct link `https://www.youtube.com/watch?v=dQw4w9WgXcQ`.
- The direct link has no `target` attribute, the production draft route is absent, and initial HTML contains no iframe, remote poster, preconnect, or YouTube script.
</acceptance_criteria>
</objective>

<execution_context>
@C:/Users/saieh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/saieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-complete-arabic-article-journey/02-SPEC.md
@.planning/phases/02-complete-arabic-article-journey/02-CONTEXT.md
@.planning/phases/02-complete-arabic-article-journey/02-UI-SPEC.md
@.planning/phases/02-complete-arabic-article-journey/02-RESEARCH.md
@.planning/phases/02-complete-arabic-article-journey/02-VALIDATION.md
@.planning/phases/02-complete-arabic-article-journey/02-PATTERNS.md
@.planning/phases/01-content-and-url-contract/01-01-SUMMARY.md
@.planning/phases/01-content-and-url-contract/01-02-SUMMARY.md
@.planning/phases/01-content-and-url-contract/01-03-SUMMARY.md
@package.json
@src/pages/[section]/[slug].astro
@src/lib/articles.ts
@src/lib/content-contract.ts
@src/config/registries.ts
@src/components/mdx-components.ts

<interfaces>
Existing interfaces to preserve and consume directly:

From `src/lib/articles.ts`:
- `getPublicArticles()` returns the collision-checked published collection.
- `getPreviewArticles()` returns the complete validated collection only under explicit development mode.

From `src/lib/content-contract.ts`:
- `pathParamsFor(article): { section: string; slug: string }` is the sole route-parameter derivation helper.
- `ArticleData` already supplies `title`, `summary`, registry keys, validated dates, `draft`, and the validated 11-character `youtubeId`.

From `src/config/registries.ts`:
- `sectionRegistry[article.data.section].label` is the public Arabic section fact.
- `authorRegistry[article.data.author].name` is the public Arabic author fact.

From `src/components/mdx-components.ts`:
- `mdxComponents` remains the exact approved render map passed to `<Content components={mdxComponents} />`.
</interfaces>
</context>

## Phase Goal

**As an** Arabic reader, **I want to** read a complete accessible article and reach its matching YouTube video, **so that** I can learn even when media or JavaScript is unavailable.

## Artifacts this phase produces

- New `playwright.config.ts` default configuration: one Chromium project, preview `webServer`, base URL `http://127.0.0.1:4321`, and `.artifacts`-only reports/output/snapshots.
- New `tests/article-journey.spec.ts` browser contract and its named vertical scenarios, beginning with `complete Markdown reader`.
- New `src/components/YouTubePlayer.astro` with local `Props { youtubeId: string; title: string }`; this plan creates the final permanent direct-action portion of that media boundary.
- New package scripts `preview` and `test:browser`; `verify` becomes unit tests → Astro diagnostics → production build → Chromium suite.
- Exact dev-only package entries `@playwright/test@1.62.1` and `@axe-core/playwright@4.13.0`; no production package is created.

<tasks>

<task type="auto">
  <name>Task 1: Make the static Markdown journey fail in a real Chromium harness</name>
  <files>package.json, package-lock.json, playwright.config.ts, tests/article-journey.spec.ts</files>
  <read_first>
    - `.nvmrc` — exact Node selection source.
    - `package.json` — preserve `preinstall`, built-in Node `test`, and exact engine/package-manager policy.
    - `.gitignore` — `.artifacts/` is the sole allowed browser-output root.
    - `.planning/phases/02-complete-arabic-article-journey/02-RESEARCH.md` sections `Package Legitimacy Audit`, `Validation Architecture`, and `Security Domain` — approved exact packages and T2-10 controls.
    - `.planning/phases/02-complete-arabic-article-journey/02-VALIDATION.md` sections `Test Infrastructure`, `Wave 0 Requirements`, and `Required Automated Scenarios` — Nyquist contract.
    - `tests/content-contract.test.ts` — preserve the working Node-test script and direct-test style.
  </read_first>
  <action>
First select Node `v24.19.0` and npm `11.17.0`; run `node --version` and `npm --version`, and do not mutate dependencies unless both outputs match exactly. Install only exact dev dependencies `@playwright/test@1.62.1` and `@axe-core/playwright@4.13.0` with `npm install --save-dev --save-exact`, then run the locally resolved `npx playwright install chromium`. The research Package Legitimacy Audit marks both packages `OK`, so no package checkpoint is required and no other dependency is permitted (D-20, T2-10).

Preserve the existing `test` script byte-for-byte in behavior. Add `preview: astro preview`, `test:browser: playwright test`, and extend `verify` to `npm test && npm run check && npm run build && npm run test:browser`, keeping the browser gate after the production build.

Create `playwright.config.ts` using Playwright `defineConfig`: `testDir: "./tests"`, match only `article-journey.spec.ts`, `baseURL: "http://127.0.0.1:4321"`, one desktop Chromium project, and a `webServer` that runs `npm run preview -- --host 127.0.0.1` without building or reusing an unrelated server. Put `outputDir`, HTML report output, trace, screenshot, video, and snapshot paths beneath `.artifacts/`; do not create `test-results`, `playwright-report`, browser profiles, screenshots, or traces in `src/`, `tests/`, or `.planning`.

Create the first executable test in `tests/article-journey.spec.ts`, named `complete Markdown reader`, against `/القضايا-العامة/اختبار-عقد-المحتوى/`. Assert `html[lang=ar][dir=rtl]`, exactly one `h1`, the labelled `الخلاصة`, section/author/publication facts, introduction and `## الخاتمة` content in document order, and the exact descriptive same-tab link `مشاهدة الفيديو على يوتيوب` with `href=https://www.youtube.com/watch?v=dQw4w9WgXcQ`. Run it against the current build and retain the expected RED evidence: it must fail because the reader facts/summary label/direct action are missing, not because configuration, navigation, Chromium, or the preview server failed. This is the first MVP end-to-end test per D-22.
  </action>
  <verify>
    <automated>`node --version; npm --version; npm test; npm run build; npx playwright test tests/article-journey.spec.ts --project=chromium -g "complete Markdown reader"` reports exact runtime, keeps the Phase 1 Node suite green, and ends RED only on the newly asserted missing reader behavior.</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` retains the existing `test` command and lists exactly the two approved browser packages as exact dev dependencies.
    - `playwright.config.ts` contains no output/report/snapshot path outside `.artifacts/`.
    - Chromium can launch the built Astro preview; the named test reaches the existing Markdown route and fails on an assertion for locked reader behavior.
    - `git status --short` shows no generated browser artifact because `.artifacts/` is ignored.
  </acceptance_criteria>
  <done>The exact browser stack is reproducible and a genuine end-to-end reader contract is RED for product behavior only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the complete static Arabic Markdown and direct-video journey GREEN</name>
  <files>src/pages/[section]/[slug].astro, src/components/YouTubePlayer.astro, src/content/articles/contract-markdown.md, tests/article-journey.spec.ts</files>
  <read_first>
    - `tests/article-journey.spec.ts` — the RED behavior from Task 1; do not relax its assertions.
    - `src/pages/[section]/[slug].astro` — preserve `getStaticPaths()`, public/preview selection, `pathParamsFor()`, root semantics, and restricted MDX render map.
    - `src/config/registries.ts` — only source for visible section and author identity per D-05.
    - `src/content/articles/contract-markdown.md` — preserve the title-independent public slug and fixture status.
    - `.planning/phases/02-complete-arabic-article-journey/02-UI-SPEC.md` — exact document order, copy, 4px spacing scale, four sizes/two weights, palette, responsive rules, RTL/bidi rules, target sizes, and exclusions.
    - `.planning/phases/02-complete-arabic-article-journey/02-CONTEXT.md` decisions D-01, D-02, D-05 through D-14, and D-17 through D-21.
    - `C:/Users/saieh/.agents/skills/entity-seo/SKILL.md` — keep visible Ahmed El-Mangawy/section identity and self-contained citable prose; do not add schema or Phase 4 metadata.
    - `C:/Users/saieh/.agents/skills/fixing-accessibility/SKILL.md` — native names, semantics, keyboard, focus, and minimal ARIA.
    - `C:/Users/saieh/.agents/skills/better-typography/SKILL.md` — system Arabic font, semantic scale, 70ch measure, logical properties, and wrapping.
  </read_first>
  <behavior>
    - Markdown route: static HTML exposes Arabic/RTL, one h1, registry facts, labelled summary, complete authored article, and the exact permanent YouTube link.
    - No-JavaScript path: all text and the direct link remain present because neither depends on a client handler.
    - Responsive path: the same DOM order remains one column and does not overflow at 320, 390, 768, 1024, or 1440 CSS pixels.
    - Registry path: changing a registry display fact would change the rendered fact; frontmatter/body contains no duplicate author or section label.
  </behavior>
  <action>
Because this task changes rendered UI, run `npx ui-skills start` before editing. Keep the existing route family and query/render frontmatter intact per D-19/D-21; do not introduce a layout route, direct `getCollection` call, metadata/canonical work, navigation, or a second CSS system.

In route frontmatter, import `sectionRegistry`, `authorRegistry`, and `YouTubePlayer`; resolve the already validated registry keys with no fallback string. Define one route-local `Intl.DateTimeFormat("ar", { dateStyle: "long", timeZone: "UTC" })`; parse date-only values with explicit `T00:00:00.000Z`. Render `main > article` in this exact order: `h1`; a semantic `dl` with exact Arabic copy `القسم:`, `الكاتب:`, `نُشر في:` and optional `حُدّثت المادة في:`; a labelled summary `section` with `h2` text `الخلاصة`; `<Content components={mdxComponents} />`; then `YouTubePlayer`. Render dates as `<time datetime="YYYY-MM-DD"><bdi dir="auto">{Arabic long date}</bdi></time>` (D-01/D-02/D-05/D-06/D-11).

Create `src/components/YouTubePlayer.astro` with typed props `youtubeId` and `title`. In this task it must render the final `section`/`h2` label `الفيديو المرتبط بالمقال` and the permanent filled CTA `مشاهدة الفيديو على يوتيوب` with exact same-tab href `https://www.youtube.com/watch?v={encodeURIComponent(youtubeId)}` and no `target`; no iframe, remote poster, preconnect, or remote script belongs in static output. The direct action is final guaranteed functionality under D-17/D-18, not a player substitute.

Apply the approved UI contract with one route-owned style block using intentional `:global(...)` descendants for rendered Markdown/MDX. Declare only spacing `4/8/16/24/32/48/64px`; body `1.125rem/1.9` weight 400, label `0.875rem/1.6` weight 700, heading `1.5rem/1.4` weight 700, display `2rem/1.3` weight 700; the exact system font stack; `font-synthesis:none`; exact colors `#FFFDF8`, `#F5F1E8`, `#166534`, `#14532D`, `#1C1917`, `#57534E`, and `#78716C`; `70ch`; `16px/32px` mobile page/block padding and `24px/64px` from `48rem`; `32px` substantive gaps and `48px` before media; underlined links; a `3px #166534` focus-visible outline with `3px` offset; and 44px minimum standalone controls. Use logical properties/start alignment, `min-inline-size:0`, `overflow-wrap:anywhere` on links/code/bdi, heading-only `text-wrap:balance`, no justification/letter spacing/truncation, and no `overflow-x:hidden` (D-07 through D-14).

Expand the Markdown proof body into a clearly labelled contract fixture with an introductory paragraph, sequential `h2`/`h3` sections, substantive Arabic prose, list/quotation/link examples, representative Arabic and ASCII numbers/punctuation/diacritics, inline-code HTTPS URL and 11-character YouTube ID for bidi testing, and a final `## الخاتمة`. Preserve its title, section key, author key, slug, publication date, draft state, and YouTube ID; Phase 3 owns the real corpus.

Make the Task 1 test GREEN and add source assertions for no `target`, no iframe/preconnect/remote poster/script, one `h1`, and production draft absence. After editing, run the project-required Hercules visual QA against the Markdown route at 320, 390, 768, 1024, and 1440 CSS pixels, saving every report/screenshot only below `.artifacts/hercules-visual-qa/phase-02-plan-01/`. Fix any spacing, typography, RTL, focus, overflow, Arabic-copy, or logic defect it finds before completing the task.
  </action>
  <verify>
    <automated>`npm test && npm run check && npm run build && npx playwright test tests/article-journey.spec.ts --project=chromium -g "complete Markdown reader"` exits 0.</automated>
  </verify>
  <acceptance_criteria>
    - The named Playwright happy path is GREEN without weakening any Task 1 assertion.
    - Both public route identities remain title-independent; the production draft HTML path remains absent.
    - Computed Markdown styles use the exact UI-SPEC scale/palette/type roles, `70ch`, logical layout, visible focus, and 44px direct CTA.
    - Source/built-output inspection finds no UI framework, design-system abstraction, webfont, icon, animation, eager YouTube resource, raw English reader control, second route, or global overflow concealment.
    - Hercules evidence exists only under ignored `.artifacts/hercules-visual-qa/phase-02-plan-01/` and records no unresolved visual or interaction defect.
  </acceptance_criteria>
  <done>A real reader can open the static Markdown route, read the complete Arabic article in a responsive semantic layout, and continue through the matching YouTube link with JavaScript unavailable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry/Chromium distribution → local dev toolchain | External test code and browser binary enter only the development/test environment under exact pins and a committed lockfile. |
| Validated collection/registries → static Astro HTML | Trusted templates consume validated IDs and registry keys; they must not bypass public queries, MDX policy, or Astro escaping. |
| Static article → youtube.com navigation | The generated same-tab anchor uses only the validated 11-character ID and performs no request until the reader follows it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T2-02 | Tampering / XSS | `YouTubePlayer.astro` direct URL | mitigate | Keep Phase 1's 11-character allowlist, encode the ID when composing the hardcoded watch URL, and assert the exact href. |
| T2-04 | Spoofing / Elevation | Reference/direct anchors | mitigate | All Phase 2 links are same-tab and omit `target`; assert no target attribute so no opener relationship exists. |
| T2-05 | Tampering / Elevation | Restricted MDX render path | mitigate | Preserve `preflightArticleSources()` and the exact `mdxComponents` map; add no media/reference component permission. |
| T2-06 | Information Disclosure | `getStaticPaths()` | mitigate | Preserve production `getPublicArticles()` and assert exactly two public outputs with the draft path absent. |
| T2-07 | Information Disclosure | Initial article/media HTML | mitigate | Static output contains no iframe, remote poster, preconnect, YouTube script, or remote media request; only the inert direct href exists. |
| T2-10 | Tampering | Playwright/axe/Chromium dev tooling | mitigate | Exact versions, committed lockfile, devDependencies only, locally resolved `npx`, matching Chromium, `npm ci`, and audit/lock inspection. |
| T2-SC | Tampering | npm install gate | mitigate | Research Package Legitimacy Audit marks both packages `OK`; install no `[ASSUMED]`, `[SUS]`, or `[SLOP]` package and add no production dependency. |
</threat_model>

<verification>
- Exact runtime probe: `node --version` = `v24.19.0`; `npm --version` = `11.17.0`.
- Per-task quick gate: `npm test`; after the UI task also run the focused Playwright happy path.
- Wave gate: `npm run check && npm run build && npm run test:browser` after Task 2.
- Inspect `git status --short --ignored` only as needed to confirm every generated browser/Hercules file is ignored beneath `.artifacts/`; do not move evidence into source or planning paths.
- No `.env` file is read, created, or referenced.
</verification>

<success_criteria>
- The first implemented slice gives a reader a complete static Arabic article and exact direct YouTube continuation.
- Phase 1 tests, query boundaries, draft exclusion, route identity, and restricted MDX remain green.
- Browser tooling is exact, dev-only, executable, and artifact-safe.
- The Markdown happy path is GREEN in Chromium and visually passes the project-required Hercules checks at all five locked widths.
</success_criteria>

<scope_guard>
Do not add homepage/index/navigation/About/real launch corpus; metadata/canonicals/schema/sitemap/robots/404; hosting/Search Console/analytics; production CWV/crawl certification; CMS/database/auth/search/comments/related content; autoplay/sticky/popover player/transcripts/timestamps; webfonts, UI frameworks, icon libraries, animation systems, or any production dependency.
</scope_guard>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID/group | Plan coverage | Status |
|--------|----------|---------------|--------|
| GOAL | Complete accessible Arabic article → matching YouTube even without media/JS | 02-01, 02-02, 02-03, 02-04 | COVERED |
| REQ | SITE-01 | 02-01, 02-02, 02-03, 02-04 | COVERED |
| REQ | SITE-02 | 02-01, 02-02, 02-04 | COVERED |
| REQ | ART-01 | 02-01, 02-02, 02-04 | COVERED |
| REQ | ART-02 | 02-01, 02-02, 02-03, 02-04 | COVERED |
| REQ | ART-03 | 02-01, 02-02, 02-03, 02-04 | COVERED |
| REQ | ART-04 | 02-03, 02-04 | COVERED |
| REQ | ART-05 | 02-01, 02-02, 02-03, 02-04 | COVERED |
| REQ | ART-06 | 02-01, 02-02, 02-04 | COVERED |
| REQ | ART-07 | 02-01, 02-02, 02-04 | COVERED |
| REQ | QUAL-01 | 02-01, 02-02, 02-03, 02-04 | COVERED |
| REQ | QUAL-02 | 02-01, 02-03, 02-04 | COVERED |
| REQ | QUAL-03 | 02-01, 02-02, 02-03, 02-04 | COVERED |
| REQ | QUAL-04 | 02-01, 02-02, 02-03, 02-04 | COVERED |
| RESEARCH | Exact Node/npm, exact dev-only Playwright/axe/Chromium, `.artifacts`-only outputs, and composite gate | 02-01 | COVERED |
| RESEARCH | Structured Arabic-labelled absolute-HTTPS references at build-time boundary | 02-02 | COVERED |
| RESEARCH | Registry-backed UTC facts, single route, restricted MDX, public/draft split | 02-01, 02-02 | COVERED |
| RESEARCH | Native RTL reader, exact UI contract, bidi isolation, accessibility, responsive/zoom/manual gates | 02-01, 02-02, 02-03, 02-04 | COVERED |
| RESEARCH | Hidden-until-wired no-request/no-autoplay nocookie iframe with permanent fallback | 02-03, 02-04 | COVERED |
| RESEARCH | Threats T2-01 through T2-10 and package legitimacy | 02-01 through 02-04 | COVERED |
| CONTEXT | D-01 | 02-01, 02-02 | COVERED |
| CONTEXT | D-02 | 02-01, 02-02 | COVERED |
| CONTEXT | D-03 | 02-02 | COVERED |
| CONTEXT | D-04 | 02-02 | COVERED |
| CONTEXT | D-05 | 02-01, 02-02 | COVERED |
| CONTEXT | D-06 | 02-01, 02-02 | COVERED |
| CONTEXT | D-07 | 02-01, 02-02, 02-04 | COVERED |
| CONTEXT | D-08 | 02-01, 02-02 | COVERED |
| CONTEXT | D-09 | 02-01, 02-02, 02-04 | COVERED |
| CONTEXT | D-10 | 02-01, 02-02, 02-04 | COVERED |
| CONTEXT | D-11 | 02-01, 02-02, 02-04 | COVERED |
| CONTEXT | D-12 | 02-01, 02-03, 02-04 | COVERED |
| CONTEXT | D-13 | 02-01, 02-03, 02-04 | COVERED |
| CONTEXT | D-14 | 02-01, 02-03, 02-04 | COVERED |
| CONTEXT | D-15 | 02-03, 02-04 | COVERED |
| CONTEXT | D-16 | 02-03, 02-04 | COVERED |
| CONTEXT | D-17 | 02-01, 02-03, 02-04 | COVERED |
| CONTEXT | D-18 | 02-01, 02-03, 02-04 | COVERED |
| CONTEXT | D-19 | 02-01, 02-02, 02-03 | COVERED |
| CONTEXT | D-20 | 02-01, 02-02, 02-03 | COVERED |
| CONTEXT | D-21 | 02-01, 02-02, 02-03 | COVERED |
| CONTEXT | D-22 | 02-01, 02-02, 02-03, 02-04 | COVERED |
| CONTEXT | Deferred Ideas (Phases 3–6) | Excluded by scope guard | EXCLUDED — NOT A GAP |
</source_audit>

<output>
Create `.planning/phases/02-complete-arabic-article-journey/02-01-SUMMARY.md` when done.
</output>
