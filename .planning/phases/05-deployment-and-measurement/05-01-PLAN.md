---
phase: 05-deployment-and-measurement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/measurement.ts
  - scripts/launch-ready.mjs
  - src/layouts/SiteLayout.astro
  - tests/content-contract.test.ts
  - tests/deployment-measurement.test.ts
  - package.json
autonomous: true
requirements: [MEAS-01, MEAS-02]
must_haves:
  truths:
    - "A controlled launch-readiness build fails closed unless PLAUSIBLE_SCRIPT_SRC is the explicit current https://plausible.io/js/pa-….js shape."
    - "Every launch HTML document contains exactly one deferred Plausible loader, while every ordinary build remains analytics-free even when an ambient Plausible value exists."
    - "One permanent direct YouTube CTA activation creates exactly one controlled project-wiring attempt named `Outbound Link: Click` whose `url` property equals the direct destination, while player activation creates zero outbound-link attempts and source inspection finds no project-owned listener or custom event path."
    - "Blocking analytics cannot alter Arabic/RTL content, focus, layout, player behavior, or native same-tab YouTube navigation."
    - "Astro.site remains the sole canonical origin authority and no analytics code enters YouTubePlayer.astro."
  artifacts:
    - path: "src/lib/measurement.ts"
      provides: "Fail-closed public Plausible script URL validator"
      exports: ["plausibleScriptSource"]
    - path: "scripts/launch-ready.mjs"
      provides: "Preflight validation for SITE_ORIGIN and PLAUSIBLE_SCRIPT_SRC before the launch-readiness build"
    - path: "src/layouts/SiteLayout.astro"
      provides: "One launch-only non-rendering analytics loader in the shared head"
    - path: "tests/content-contract.test.ts"
      provides: "Validator, source, build-output, body-invariance, omission, and restoration contracts"
    - path: "tests/deployment-measurement.test.ts"
      provides: "Isolated browser proof of one project wiring seam and analytics-failure independence"
    - path: "package.json"
      provides: "Registration of the focused measurement test plus serialized build-mutating native test execution"
  key_links:
    - from: "scripts/launch-ready.mjs"
      to: "src/lib/measurement.ts"
      via: "plausibleScriptSource(process.env.PLAUSIBLE_SCRIPT_SRC) before Astro build"
      pattern: "plausibleScriptSource.*PLAUSIBLE_SCRIPT_SRC"
    - from: "src/layouts/SiteLayout.astro"
      to: "PLAUSIBLE_SCRIPT_SRC"
      via: "exact import.meta.env.MODE launch-readiness gate plus shared validator"
      pattern: "MODE.*launch-readiness"
    - from: "src/layouts/SiteLayout.astro"
      to: "every emitted HTML document head"
      via: "single deferred external script element"
      pattern: "script.*defer.*src"
    - from: "package.json"
      to: "tests/deployment-measurement.test.ts"
      via: "existing explicit node:test file list"
      pattern: "deployment-measurement\\.test\\.ts"
---

## Phase Goal

**As a** site owner, **I want to** operate the canonical production site and measure the intended discovery-to-YouTube journey, **so that** I can observe aggregate use without identifying individual readers.

<objective>
Deliver the complete local measurement slice: an explicit current Plausible asset is validated at the launch boundary, rendered once in every launch document, absent everywhere else, and exercised through the unchanged permanent YouTube anchor.

Purpose: MEAS-01 and MEAS-02 need a real project-side path before any owner-controlled dashboard can report pageviews or outbound clicks. This plan implements only that path and labels deterministic interception as wiring evidence, per D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-16, D-17, D-18, and D-19.
Output: One validator, one shared-head loader, the extended launch wrapper, native output contracts, and one isolated browser test with zero visible UI change.
</objective>

<execution_context>
@C:/Users/saieh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/saieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-deployment-and-measurement/05-SPEC.md
@.planning/phases/05-deployment-and-measurement/05-CONTEXT.md
@.planning/phases/05-deployment-and-measurement/05-RESEARCH.md
@.planning/phases/05-deployment-and-measurement/05-PATTERNS.md
@.planning/phases/05-deployment-and-measurement/05-VALIDATION.md
@.planning/phases/05-deployment-and-measurement/05-UI-SPEC.md
@.planning/phases/04-search-discovery-integrity/04-03-SUMMARY.md
@src/lib/site-origin.ts
@scripts/launch-ready.mjs
@src/layouts/SiteLayout.astro
@src/components/YouTubePlayer.astro
@tests/content-contract.test.ts
@tests/site-origin.test.ts
@tests/search-discovery.spec.ts
@playwright.config.ts
@package.json
</context>

## Artifacts this phase produces

- New exported symbol `plausibleScriptSource(raw: unknown): string` in `src/lib/measurement.ts`.
- New required public launch build input `PLAUSIBLE_SCRIPT_SRC`; it is validated but never treated as a canonical origin or secret.
- Existing public launch build input `SITE_ORIGIN` remains the only source for `Astro.site`.
- Existing build mode `launch-readiness` remains the only analytics inclusion gate; no second flag or environment mode is added.
- Layout-local symbols `isLaunchReadiness` and `plausibleScriptSrc` connect the mode and validated public input to one deferred script tag.
- New test-only constant `CONTROLLED_PLAUSIBLE_SCRIPT_SRC` uses the unmistakably fake `https://plausible.io/js/pa-FAKE_TEST_FIXTURE_DO_NOT_DEPLOY.js` value.
- New file `src/lib/measurement.ts`.
- Modified files `scripts/launch-ready.mjs`, `src/layouts/SiteLayout.astro`, `tests/content-contract.test.ts`, and `package.json`.
- New file `tests/deployment-measurement.test.ts`.
- Phase documentation artifacts owned by Plan 05-02: the existing Arabic `README.md` extension and new `.planning/phases/05-deployment-and-measurement/05-LAUNCH-EVIDENCE.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing launch-to-YouTube measurement contract</name>
  <files>tests/content-contract.test.ts, tests/deployment-measurement.test.ts, package.json</files>
  <read_first>
    @.planning/phases/05-deployment-and-measurement/05-SPEC.md
    @.planning/phases/05-deployment-and-measurement/05-CONTEXT.md
    @.planning/phases/05-deployment-and-measurement/05-VALIDATION.md
    @.planning/phases/05-deployment-and-measurement/05-UI-SPEC.md
    @.planning/phases/05-deployment-and-measurement/05-PATTERNS.md
    @tests/content-contract.test.ts
    @tests/site-origin.test.ts
    @tests/search-discovery.spec.ts
    @playwright.config.ts
    @src/layouts/SiteLayout.astro
    @src/components/YouTubePlayer.astro
    @scripts/launch-ready.mjs
    @package.json
  </read_first>
  <behavior>
    - "Valid input: https://plausible.io/js/pa-FAKE_TEST_FIXTURE_DO_NOT_DEPLOY.js returns unchanged."
    - "Invalid input: missing, non-string, padded, HTTP, credentialed, alternate-host, explicit-port, query, fragment, legacy script.outbound-links.js, generic script.js, missing pa token, encoded-path, and non-js values throw."
    - "Ordinary build: every emitted HTML document has zero plausible.io script markup, including when an ambient PLAUSIBLE_SCRIPT_SRC exists."
    - "Launch build: every emitted HTML document, including 404.html, has exactly one deferred loader whose src equals the controlled fixture."
    - "Launch and ordinary bodies: body HTML, Arabic text, landmarks, link topology, and YouTube CTA remain identical; finally restores the ordinary local canonical and zero-analytics output."
    - "Browser seam: a controlled vendor stub observes one pageview attempt; after one direct CTA activation it observes exactly one `Outbound Link: Click` attempt whose `props.url` equals the direct destination, while player activation creates zero outbound-link attempts."
    - "Failure seam: aborting the analytics asset/event request leaves the static Arabic page, focus, player control, and native direct-link request usable with no project retry or error UI."
  </behavior>
  <action>
Create the RED contract before production code. Extend the existing launch lifecycle in `tests/content-contract.test.ts` rather than building a parallel harness: import the not-yet-created `plausibleScriptSource`, add table-driven acceptance/rejection cases, build and capture ordinary output, launch with the controlled Phase 4 hostname plus `CONTROLLED_PLAUSIBLE_SCRIPT_SRC`, recursively inspect every `dist/**/*.html`, compare extracted bodies, and delete both public inputs in the existing `finally` restoration path. Assert the exact `launch-readiness` source gate, one loader per launch document, zero loader per ordinary document, no legacy URL, and no analytics listener/attribute/call in `YouTubePlayer.astro`, per D-06, D-07, D-08, D-10, D-16, D-18, and D-19.

Create `tests/deployment-measurement.test.ts` as one Node `node:test` lifecycle using the installed Playwright browser. Build the controlled launch, serve it only on a local transport, fulfill the fake `pa-…` asset with a clearly test-only vendor-behavior stub, intercept the event endpoint and YouTube navigation, and distinguish pageview, direct-link outbound, and player activation. After one permanent direct CTA activation, require exactly one controlled project-wiring attempt named `Outbound Link: Click` whose property `url` equals the intercepted direct destination; in a separate player-activation assertion, require zero outbound-link attempts. Keep source inspection independently responsible for forbidding project-owned outbound click listeners and custom analytics event calls throughout `src/`, so a duplicate path cannot be hidden behind the controlled count. Label all controlled observations as project wiring rather than Plausible receipt/reporting per D-09, D-11, D-16, and D-17. Give this lifecycle its own `try/finally` ordinary-build restoration. Add the file to the existing explicit `npm test` list and set Node's existing runner to `--test-concurrency=1`, because both build-contract files mutate the shared `dist/` directory and parallel execution would make the oracle racy. Add no new script, dependency, fixture framework, screenshot default, or application listener. Any failure-only browser artifact must go under `.artifacts/`.

Run the focused native command and preserve the expected RED failure. It must fail because measurement validation/launch markup does not exist yet; an unrelated runtime, browser, or fixture failure is not an acceptable RED state.
</action>
<verify>
<automated>PowerShell with Node v24.19.0 and npm 11.17.0: `node -e "if(process.version!=='v24.19.0')process.exit(1)"; if ((npm --version) -ne '11.17.0') { exit 1 }; npm test` — expected RED: non-zero only on the missing Phase 5 measurement contract, including the exact-one controlled outbound attempt and zero player-attempt assertions.</automated>
</verify>
<acceptance_criteria> - `package.json` includes `tests/deployment-measurement.test.ts` in the existing `node --test` file list, serializes build-mutating files with `--test-concurrency=1`, and adds no dependency or new package script. - The controlled fixture is unmistakably fake, structurally valid, and appears only in tests or controlled command evidence. - The tests cover all emitted HTML, ordinary omission, launch inclusion, body invariance, exactly one controlled `Outbound Link: Click` attempt with `url` equal to the direct CTA destination after one CTA activation, zero outbound attempts after player activation, source-level absence of project listeners/custom calls, vendor blocking, and ordinary restoration. - The RED run fails for the absent validator/loader contract and not because of a missing browser, wrong runtime, or malformed test harness.
</acceptance_criteria>
<done>The complete executable RED contract exists and fails for the intended missing measurement slice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the fail-closed launch-only loader and make the full slice green</name>
  <files>src/lib/measurement.ts, scripts/launch-ready.mjs, src/layouts/SiteLayout.astro</files>
  <read_first>
    @AGENTS.md
    @.planning/phases/05-deployment-and-measurement/05-CONTEXT.md
    @.planning/phases/05-deployment-and-measurement/05-RESEARCH.md
    @.planning/phases/05-deployment-and-measurement/05-PATTERNS.md
    @.planning/phases/05-deployment-and-measurement/05-UI-SPEC.md
    @src/lib/site-origin.ts
    @scripts/launch-ready.mjs
    @src/layouts/SiteLayout.astro
    @src/components/YouTubePlayer.astro
    @tests/content-contract.test.ts
    @tests/deployment-measurement.test.ts
  </read_first>
  <behavior>
    - "The platform URL parser plus an exact allowlist accepts only a clean https://plausible.io/js/pa-[A-Za-z0-9_-]+.js asset."
    - "launch-ready validates SITE_ORIGIN and PLAUSIBLE_SCRIPT_SRC before invoking Astro with mode launch-readiness."
    - "SiteLayout renders one deferred, compiler-unprocessed external loader only when import.meta.env.MODE equals launch-readiness."
    - "Ordinary builds do not validate, render, or request analytics, regardless of an ambient PLAUSIBLE_SCRIPT_SRC."
    - "The controlled browser seam records exactly one `Outbound Link: Click` attempt with the direct destination `url` after one permanent CTA activation, zero outbound attempts after player activation, and no project-owned listener or custom event call."
    - "No body/style/Arabic/RTL/CTA/player source changes are introduced."
  </behavior>
  <action>
Before editing the shared layout, run `npx ui-skills start` under the pinned Node/npm runtime and apply the approved zero-visible-change contract. Create `src/lib/measurement.ts` in the exact small-function style of `productionSiteOrigin()`: export only `plausibleScriptSource(raw: unknown): string`, parse with the native `URL`, reject every value outside the clean exact `https://plausible.io/js/pa-[A-Za-z0-9_-]+.js` form, and return the normalized accepted source. Do not add a schema library, class, second origin abstraction, compatibility path, legacy filename, token store, or environment-file loader per D-05, D-07, D-08, and D-18.

Update `scripts/launch-ready.mjs` to validate and normalize `process.env.PLAUSIBLE_SCRIPT_SRC` before `build()`, while retaining `productionSiteOrigin(process.env.SITE_ORIGIN)` and the exact `build({ site, mode: "launch-readiness" })` authority. Update only `SiteLayout.astro` frontmatter and head: derive `isLaunchReadiness` solely from `import.meta.env.MODE`, obtain the validated source only inside that branch, and render one `is:inline` deferred external script in `<head>`. Keep `Astro.site` as the sole canonical authority. Do not change `<body>`, styles, page props, visible or accessible text, `YouTubePlayer.astro`, or native navigation per D-06, D-07, D-08, D-09, D-10, D-11, and D-19. The GREEN result must prove exactly one controlled `Outbound Link: Click` attempt with the matching direct destination `url` after one CTA activation, zero outbound attempts after player activation, and no project-owned listener or custom event call.

Make the RED suite green, then run the ordinary browser baseline. After code verification, perform Hercules zero-visible-delta QA across the existing public route families, 404, approved widths, focus, no-JavaScript, successful controlled loader, and blocked loader. If the named Hercules integration is unavailable, use its documented headed Playwright fallback. Store all screenshots, request ledgers, and accessibility output under `.artifacts/hercules-visual-qa/phase-05-zero-visible-delta/`; no artifact belongs in source or `.planning/`. The expected body visual difference is zero pixels and zero accessible-tree additions.
</action>
<verify>
<automated>PowerShell with Node v24.19.0 and npm 11.17.0: `node -e "if(process.version!=='v24.19.0')process.exit(1)"; if ((npm --version) -ne '11.17.0') { exit 1 }; npm test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run test:browser` — must pass the exact-one controlled CTA attempt, matching `url`, zero player attempts, and no custom project event path.</automated>
</verify>
<acceptance_criteria> - `plausibleScriptSource` accepts only the current official host/path shape and rejects missing, legacy, generic, credentialed, alternate-host, query, fragment, and malformed inputs. - A controlled launch produces exactly one deferred fake fixture loader in every HTML document; ordinary output produces zero loaders and is restored after tests. - One permanent direct CTA activation yields exactly one controlled `Outbound Link: Click` attempt whose `url` equals the direct destination; player activation yields zero outbound-link attempts, and source inspection finds no project-owned listener or custom event call. - `SiteLayout.astro` body/style output and `YouTubePlayer.astro` are unchanged; Arabic/RTL, focus, responsive layout, no-JavaScript, and navigation baselines remain green. - No new dependency, adapter, server, CMS, API, tag manager, consent UI, identifier, or browser artifact outside `.artifacts/` exists.
</acceptance_criteria>
<done>The launch-only aggregate measurement path is fail-closed, invisible, single-wired, and fully verified locally without making a live-service claim.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                                                             |
| ------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| Provider build values → launch wrapper      | Public but untrusted `SITE_ORIGIN` and `PLAUSIBLE_SCRIPT_SRC` cross into the static build.                              |
| Launch wrapper → Astro shared head          | Validated values and exact build mode decide canonical identity and analytics inclusion.                                |
| Browser → Plausible asset/event endpoint    | A third-party script observes aggregate page and outbound-link actions; site behavior must survive blocking or failure. |
| Native YouTube anchor → external navigation | Measurement may observe but must never intercept, delay, duplicate, or relabel the reader action.                       |

## STRIDE Threat Register

| Threat ID       | Category                | Component                                   | Disposition | Mitigation Plan                                                                                                                                                                                                                              |
| --------------- | ----------------------- | ------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-01         | Spoofing / Tampering    | `plausibleScriptSource`, `launch-ready.mjs` | mitigate    | Accept only clean `https://plausible.io/js/pa-[A-Za-z0-9_-]+.js`, reuse the existing production-origin validator, and fail before Astro build on either unsafe input.                                                                        |
| T-05-03         | Tampering               | Third-party Plausible loader                | mitigate    | Restrict host/path to the owner-generated current asset, load it deferred only in launch mode, forbid legacy/generic/wrapped code, and keep all static behavior independent when it is blocked.                                              |
| T-05-04         | Repudiation / Integrity | Shared head and outbound anchor             | mitigate    | Assert one loader per document, exactly one controlled `Outbound Link: Click` attempt with destination `url` after one direct CTA activation, zero outbound attempts after player activation, and no custom project listener/call/attribute. |
| T-05-05         | Integrity               | Ordinary and preview output                 | mitigate    | Use exact `import.meta.env.MODE === "launch-readiness"` as the only gate and prove ordinary output has zero analytics even with an ambient value.                                                                                            |
| T-05-06         | Denial of Service       | Analytics network failure                   | mitigate    | Use deferred loading and the native same-tab anchor with no preventDefault, retry, wait, error UI, or content dependency; browser-test an aborted asset/event endpoint.                                                                      |
| T-05-02         | Information Disclosure  | Source, tests, and built output             | mitigate    | Commit only an unmistakable fake test fixture, never a real `pa-…` value or provider credential, and scan source/output without reading or creating environment files.                                                                       |
| </threat_model> |

<verification>
- The RED task fails on the absent Phase 5 measurement contract, then the GREEN task passes `npm test`, `npm run check`, and `npm run test:browser` under Node v24.19.0/npm 11.17.0.
- Controlled launch inspection covers every HTML document and exact loader count; ordinary restoration proves local canonical identity and zero analytics.
- The isolated browser seam proves project wiring only: one pageview attempt, exactly one `Outbound Link: Click` attempt whose `url` equals the direct CTA destination after one CTA activation, zero outbound attempts after player activation, and full navigation/content behavior under blocking.
- Hercules or its documented headed Playwright fallback records a zero-visible/body/accessibility delta under ignored `.artifacts/` only.
- `git diff --check` is clean and no dependency, environment file, adapter, server, or YouTube component change is present.
</verification>

<success_criteria>

- MEAS-01 has a launch-only, aggregate, non-rendering project integration seam without reader identifiers or ordinary-build analytics.
- MEAS-02 has a controlled proof that one permanent direct CTA activation produces exactly one `Outbound Link: Click` wiring attempt with the matching destination `url`; player activation produces zero attempts and project code contains no second event path.
- The existing Arabic/RTL reader journey and all Phase 4 canonical/discovery gates remain unchanged and green.
- Local results are described only as wiring/readiness evidence; real Plausible receipt and dashboard reporting remain external facts for Plan 05-02.
  </success_criteria>

<output>
Create `.planning/phases/05-deployment-and-measurement/05-01-SUMMARY.md` when done.
</output>
