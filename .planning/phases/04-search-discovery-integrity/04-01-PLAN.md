---
phase: 04-search-discovery-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/site-origin.ts
  - tests/site-origin.test.ts
  - scripts/launch-ready.mjs
  - package.json
  - package-lock.json
  - astro.config.mjs
  - src/pages/robots.txt.ts
autonomous: true
requirements:
  - SEO-03
  - SEO-05

must_haves:
  truths:
    - "Ordinary builds always use http://127.0.0.1:4322, while launch readiness accepts only an explicit normalized safe HTTPS SITE_ORIGIN and never reads .env files per D-05 through D-08."
    - "The exact official @astrojs/sitemap@3.7.3 dependency is verified and installed autonomously, and plain sitemap() derives URLs only from Astro's generated routes per D-12 and D-14."
    - "Robots exposes the minimal allow policy and exactly one sitemap-index URL derived from the same Astro.site origin per D-13."
    - "Both local and controlled launch builds prove sitemap/robots host agreement before ordinary local output is restored per D-17 through D-19."
  artifacts:
    - path: "src/lib/site-origin.ts"
      provides: "One local-origin constant and pure production-origin validator"
      exports: ["LOCAL_SITE_ORIGIN", "productionSiteOrigin"]
    - path: "tests/site-origin.test.ts"
      provides: "Native acceptance, normalization, and rejection matrix"
    - path: "scripts/launch-ready.mjs"
      provides: "Explicit launch-only programmatic Astro build wrapper"
    - path: "astro.config.mjs"
      provides: "Fixed local site plus plain official sitemap integration"
      contains: "sitemap()"
    - path: "src/pages/robots.txt.ts"
      provides: "Static robots endpoint derived only from Astro.site"
      exports: ["GET"]
  key_links:
    - from: "astro.config.mjs"
      to: "src/lib/site-origin.ts"
      via: "site: LOCAL_SITE_ORIGIN"
      pattern: "site:\\s*LOCAL_SITE_ORIGIN"
    - from: "scripts/launch-ready.mjs"
      to: "src/lib/site-origin.ts"
      via: "productionSiteOrigin(process.env.SITE_ORIGIN) before build({ site })"
      pattern: "productionSiteOrigin.*SITE_ORIGIN"
    - from: "astro.config.mjs"
      to: "@astrojs/sitemap@3.7.3"
      via: "plain sitemap() with no custom pages, filter, or serializer"
      pattern: "sitemap\\(\\)"
    - from: "src/pages/robots.txt.ts"
      to: "dist/sitemap-index.xml"
      via: "new URL('sitemap-index.xml', site).href"
      pattern: "sitemap-index\\.xml"
---

## Phase Goal

**As an** Arabic search reader, **I want** every public page and crawler discovery file to expose one accurate identity and published route graph, **so that** I can find and navigate the site's public content reliably.

<objective>
Establish the one safe build-origin boundary and use it to emit the official route-derived sitemap plus matching robots policy.

Purpose: Absolute crawler identity must be normalized once before page metadata and browser-facing surfaces consume it.
Output: Native origin contract, explicit launch wrapper, exact official sitemap dependency/integration, same-origin robots endpoint, and local/controlled-launch build proof.
</objective>

<execution_context>
@C:/Users/saieh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/saieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-search-discovery-integrity/04-SPEC.md
@.planning/phases/04-search-discovery-integrity/04-CONTEXT.md
@.planning/phases/04-search-discovery-integrity/04-RESEARCH.md
@.planning/phases/04-search-discovery-integrity/04-VALIDATION.md
@package.json
@package-lock.json
@astro.config.mjs
@src/lib/articles.ts
@src/pages/[section]/[slug].astro

<interfaces>
From the current build contract:
- Astro remains static with `trailingSlash: "always"`, MDX preflight, and the existing MDX integration.
- The production preview origin is `http://127.0.0.1:4322`.
- `getPublicArticles()` remains the production article route selector; this plan must not change it or create a second sitemap route list.

From the pinned stack and research:
- Astro 7.2.7 exports `build({ site })` for the explicit launch wrapper.
- `@astrojs/sitemap@3.7.3` is the locked official package; its pinned implementation excludes status pages and writes `sitemap-index.xml` plus `sitemap-0.xml`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the single safe origin and launch-build boundary</name>
  <files>src/lib/site-origin.ts, tests/site-origin.test.ts, scripts/launch-ready.mjs, package.json, astro.config.mjs</files>
  <read_first>
    - `.planning/phases/04-search-discovery-integrity/04-CONTEXT.md` — D-05 through D-08 are non-negotiable.
    - `.planning/phases/04-search-discovery-integrity/04-RESEARCH.md` — use Pattern 1 and the complete native origin matrix; do not use an Astro/Vite config factory.
    - `.planning/phases/04-search-discovery-integrity/04-VALIDATION.md` — implement validation row 04-01-01 and the launch-boundary portion of 04-01-02.
    - `package.json` — preserve exact runtime gates and extend the existing native test command.
    - `astro.config.mjs` — preserve static output, trailing slashes, MDX preflight, and existing integrations.
  </read_first>
  <behavior>
    - Valid normalized root HTTPS origins, including uppercase host, trailing slash, default port removal, and explicit non-default HTTPS port, return one normalized `.origin`.
    - Missing, empty, surrounding whitespace, non-HTTPS schemes, credentials or raw `@`, query/raw `?`, fragment/raw `#`, non-root paths, trailing-dot hosts, localhost/subdomains, every IPv4/IPv6 literal, reserved/example roots/subdomains, and malformed values throw.
    - Ordinary `npm run build` always uses `http://127.0.0.1:4322` even when an ambient `SITE_ORIGIN` exists.
    - `npm run launch:ready` fails when input is absent/unsafe and succeeds with a controlled safe value without loading `.env` files.
  </behavior>
  <action>Create `src/lib/site-origin.ts` with only `LOCAL_SITE_ORIGIN = "http://127.0.0.1:4322"`, a small reserved-root list, and `productionSiteOrigin(raw: unknown)` implemented with WHATWG `URL` plus `node:net.isIP`; validate every D-07 branch before returning `url.origin`. Write the native table tests first in `tests/site-origin.test.ts`, run them red, then implement the helper. Create `scripts/launch-ready.mjs` to read only `process.env.SITE_ORIGIN`, validate it, and call Astro 7's exported `build({ site })`; never call `loadEnv`, dotenv, or read/create an `.env` file. Replace the ineffective `launch:ready` script with this wrapper and include the origin test in `npm test`. Set `astro.config.mjs` `site` to `LOCAL_SITE_ORIGIN` so ordinary builds are deterministic and ignore ambient production input. Preserve all runtime/version, MDX preflight, static, trailing-slash, and integration behavior. This implements D-05, D-06, D-07, and D-08 at the single build-time trust boundary.</action>
  <verify>
    <automated>npm test; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run check; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run build; if ($LASTEXITCODE) { exit $LASTEXITCODE }; Remove-Item Env:SITE_ORIGIN -ErrorAction SilentlyContinue; npm run launch:ready; if ($LASTEXITCODE -eq 0) { throw 'Launch unexpectedly accepted missing SITE_ORIGIN' }; $env:SITE_ORIGIN='http://blog.ahmed-mangawy.org'; try { npm run launch:ready; if ($LASTEXITCODE -eq 0) { throw 'Launch unexpectedly accepted unsafe SITE_ORIGIN' } } finally { Remove-Item Env:SITE_ORIGIN -ErrorAction SilentlyContinue }; $env:SITE_ORIGIN='https://blog.ahmed-mangawy.org'; try { npm run launch:ready; if ($LASTEXITCODE) { throw 'Controlled launch build failed' } } finally { Remove-Item Env:SITE_ORIGIN -ErrorAction SilentlyContinue }; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `tests/site-origin.test.ts` covers every listed valid and invalid class and passes under Node 24.19.0.
    - Missing or unsafe launch input exits nonzero before accepted output; controlled safe HTTPS input builds successfully.
    - A final ordinary build restores local-origin output at `http://127.0.0.1:4322`.
    - Source contains no `.env` read/create, `loadEnv`, dotenv dependency, request-host use, or page-level origin override.
  </acceptance_criteria>
  <done>One pure validator controls launch identity, ordinary builds remain deterministic, and native tests fail every unsafe canonical-host input.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the exact official sitemap and matching robots policy autonomously</name>
  <files>package.json, package-lock.json, astro.config.mjs, src/pages/robots.txt.ts</files>
  <read_first>
    - `.planning/phases/04-search-discovery-integrity/04-CONTEXT.md` — D-12, D-13, and D-14 forbid substitute dependencies, parallel route/filter policy, and robots-based draft security.
    - `.planning/phases/04-search-discovery-integrity/04-RESEARCH.md` — use the package audit facts and Patterns 3–4; slopcheck unavailability is documented and non-blocking under the owner's autonomous approval.
    - `.planning/phases/04-search-discovery-integrity/04-VALIDATION.md` — complete validation row 04-01-02.
    - `astro.config.mjs` — preserve Task 1's local site, MDX preflight, static output, and trailing-slash contract.
    - `src/lib/articles.ts` and `src/pages/[section]/[slug].astro` — observe but do not modify the public route boundary.
  </read_first>
  <action>Use the repository-selected Node 24.19.0/npm 11.17.0 toolchain. Before installation, query `npm view @astrojs/sitemap@3.7.3 name version repository.url dist.integrity scripts --json` and fail unless the scoped name and exact version match, the repository is `withastro/astro`, integrity is non-empty, and `preinstall`/`install`/`postinstall` scripts are absent. Slopcheck unavailability is non-blocking because the owner approved routine package decisions and the exact official dependency is locked by the project stack, research, validation contract, and D-12. Run exactly `npm install --save-exact @astrojs/sitemap@3.7.3`; accept no substitute. Assert afterward that `package.json` pins `3.7.3`, the root and `node_modules/@astrojs/sitemap` lockfile records resolve that exact package/version with non-empty integrity and no install-script flag, and the installed package names the official repository and declares no lifecycle install scripts. Import the official integration in `astro.config.mjs` and add plain `sitemap()` after `mdx()`; pass no `customPages`, `filter`, `serialize`, or route list. Create `src/pages/robots.txt.ts` as a static endpoint whose `GET satisfies APIRoute` requires `site`, derives `new URL("sitemap-index.xml", site).href`, and returns UTF-8 plain text with exactly `User-agent: *`, `Allow: /`, one blank line, and one `Sitemap:` line. Add no `Disallow`, draft identifier, hard-coded host, request-host source, or public HTML link. This autonomously implements D-12 and D-13 while preserving D-14's selector boundary.</action>
  <verify>
    <automated>$metadata = npm view @astrojs/sitemap@3.7.3 name version repository.url dist.integrity scripts --json | ConvertFrom-Json; if ($metadata.name -ne '@astrojs/sitemap' -or $metadata.version -ne '3.7.3' -or $metadata.'repository.url' -notmatch 'github\.com[/:]withastro/astro(?:\.git)?$' -or [string]::IsNullOrWhiteSpace($metadata.'dist.integrity') -or $metadata.scripts.preinstall -or $metadata.scripts.install -or $metadata.scripts.postinstall) { throw 'Unexpected sitemap package metadata' }; node -e "const p=require('./package.json');const l=require('./package-lock.json');const i=require('./node_modules/@astrojs/sitemap/package.json');const d=l.packages?.['node_modules/@astrojs/sitemap'];const repo=typeof i.repository==='string'?i.repository:i.repository?.url??'';if(p.dependencies?.['@astrojs/sitemap']!=='3.7.3'||l.packages?.['']?.dependencies?.['@astrojs/sitemap']!=='3.7.3'||d?.version!=='3.7.3'||d?.resolved!=='https://registry.npmjs.org/@astrojs/sitemap/-/sitemap-3.7.3.tgz'||!d?.integrity||d?.hasInstallScript===true||i.name!=='@astrojs/sitemap'||i.version!=='3.7.3'||!/(^|[/:])withastro\/astro(?:\.git)?$/.test(repo)||i.scripts?.preinstall||i.scripts?.install||i.scripts?.postinstall)process.exit(1)"; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm test; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run check; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run build; if ($LASTEXITCODE) { exit $LASTEXITCODE }; if (!(Test-Path -LiteralPath 'dist\sitemap-index.xml') -or !(Test-Path -LiteralPath 'dist\sitemap-0.xml') -or !(Test-Path -LiteralPath 'dist\robots.txt')) { throw 'Missing generated sitemap or robots output' }; $localCrawler = (Get-Content -Raw -LiteralPath 'dist\sitemap-index.xml') + (Get-Content -Raw -LiteralPath 'dist\sitemap-0.xml') + (Get-Content -Raw -LiteralPath 'dist\robots.txt'); if ($localCrawler -notmatch 'http://127\.0\.0\.1:4322') { throw 'Local crawler output uses the wrong origin' }; $env:SITE_ORIGIN='https://blog.ahmed-mangawy.org'; try { npm run launch:ready; if ($LASTEXITCODE) { throw 'Controlled launch build failed' }; $launchCrawler = (Get-Content -Raw -LiteralPath 'dist\sitemap-index.xml') + (Get-Content -Raw -LiteralPath 'dist\sitemap-0.xml') + (Get-Content -Raw -LiteralPath 'dist\robots.txt'); if ($launchCrawler -notmatch 'https://blog\.ahmed-mangawy\.org' -or $launchCrawler -match '127\.0\.0\.1') { throw 'Launch crawler output origin mismatch' } } finally { Remove-Item Env:SITE_ORIGIN -ErrorAction SilentlyContinue }; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Registry metadata, installed-package metadata, `package.json`, and `package-lock.json` all identify exact official `@astrojs/sitemap@3.7.3`, non-empty integrity, official repository, and no lifecycle install script.
    - `astro.config.mjs` uses plain `sitemap()` and contains no custom page set, filter, serializer, or second route registry.
    - Fresh local and controlled-launch builds emit `sitemap-index.xml`, `sitemap-0.xml`, and `robots.txt` from their respective configured origins before the final local rebuild.
    - Robots contains only the locked allow policy and exactly one absolute sitemap-index URL; it contains no `Disallow` or draft/proof identifier.
  </acceptance_criteria>
  <done>The official Astro build graph produces the sitemap, robots derives its only absolute URL from the same normalized `Astro.site`, and the exact dependency/lockfile facts are proven without a checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Explicit process input → launch build | Untrusted `SITE_ORIGIN` can affect every absolute public identity. |
| npm registry → repository dependency graph | The one external package enters the static build toolchain. |
| Generated routes → sitemap/robots output | Route inclusion and configured origin determine what crawlers are invited to index. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification |
|---|---|---|---|---|---|---|
| T-04-01 | Spoofing/Tampering | `productionSiteOrigin` and launch wrapper | High | mitigate | Validate one explicit root HTTPS origin; reject credentials, URL state, local/IP/reserved hosts; normalize once before `build({ site })`. | Native rejection matrix plus controlled launch output scan. |
| T-04-02 | Spoofing | Absolute crawler identity | High | mitigate | Use only configured `Astro.site`; never derive robots or sitemap identity from request/page input. | Source scan and local/launch host equality. |
| T-04-03 | Information Disclosure | Sitemap/public output | High | mitigate | Preserve the public route selector and use no custom sitemap pages/filter. | Fresh build now; final independent source/output oracle in Plan 04-03. |
| T-04-04 | Tampering/Repudiation | Sitemap and robots agreement | Medium | mitigate | Official route-derived sitemap plus robots URL from the same `site`. | Parse generated local/launch files and exact robots policy in Plan 04-02. |
| T-04-SC | Tampering | `@astrojs/sitemap` install | High | mitigate | Verify exact registry identity/version/repository/integrity/scripts and resulting package/lockfile facts; install no substitute. | Automated registry, installed-package, manifest, and lockfile assertions before build. |
</threat_model>

<verification>
- `npm test` passes the complete native content and origin suite.
- Missing/unsafe `SITE_ORIGIN` fails; controlled safe HTTPS launch builds pass; final ordinary builds restore the local origin.
- Exact package registry, repository, integrity, lifecycle-script, installed-package, and lockfile assertions pass.
- Local and controlled-launch builds emit both sitemap XML layers and matching robots text from the expected origin.
- `npm run verify` remains green after the wave.
</verification>

<success_criteria>
- SEO-03 has one fail-closed configured-origin boundary usable by local and launch builds.
- SEO-05 has the exact official route-derived sitemap and same-origin minimal robots policy with no parallel URL/filter registry.
- T-04-01, T-04-02, T-04-03, and T-04-SC have concrete implementation controls and runnable evidence paths.
- No checkpoint, substitute dependency, `.env` access, request-host identity, custom sitemap generator, or deferred feature enters the plan.
</success_criteria>

<decision_coverage>
- D-05, D-06, D-07, D-08: `Astro.site` origin source, fixed local verification origin, strict explicit production input, and no `.env` access.
- D-12, D-13, D-14: exact official route-derived sitemap, robots from the same site origin, and preserved public selector without a second list/filter.
- D-17, D-18, D-19: native/build parsing, fresh local/launch output, full gate preservation, and no new runtime/framework surface.
</decision_coverage>

## Artifacts this plan produces

| Kind | Symbol / CLI / field | File path | Contract |
|---|---|---|---|
| Constant/function | `LOCAL_SITE_ORIGIN`, `productionSiteOrigin` | `src/lib/site-origin.ts` | Deterministic local origin and fail-closed clean HTTPS production validator. |
| Native test | Origin acceptance/rejection matrix | `tests/site-origin.test.ts` | Covers normalization, missing input, and every D-07 rejection class. |
| CLI | `npm run launch:ready` | `scripts/launch-ready.mjs`, `package.json` | Validates explicit process input and calls `build({ site })` without `.env` access. |
| Dependency/integration | `@astrojs/sitemap@3.7.3`, `sitemap()` | `package.json`, `package-lock.json`, `astro.config.mjs` | Exact official route-derived sitemap with automated supply-chain facts. |
| Endpoint | `GET satisfies APIRoute` | `src/pages/robots.txt.ts` | Minimal same-origin crawler policy pointing to `sitemap-index.xml`. |

No page metadata, 404 surface, favicon, client runtime, custom route list, or external service is added by this plan.

<output>
After completion, create `.planning/phases/04-search-discovery-integrity/04-01-SUMMARY.md`.
</output>
