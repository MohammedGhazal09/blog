---
phase: 04-search-discovery-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/layouts/SiteLayout.astro
  - src/pages/index.astro
  - src/pages/[section]/index.astro
  - src/pages/[section]/[slug].astro
  - src/pages/عن-أحمد-المنجاوي.astro
  - src/pages/404.astro
  - public/favicon.svg
  - tests/search-discovery.spec.ts
  - playwright.config.ts
autonomous: true
requirements:
  - SITE-06
  - SEO-02
  - SEO-03
  - SEO-05

must_haves:
  truths:
    - "Every indexable public route has one unique maintained Arabic title/description, one visible h1, one self-canonical, and one matching text-only social identity per D-01 through D-11."
    - "A missing slash-form route returns HTTP 404 with the exact Arabic shell, noindex,follow, no canonical/social identity, and no sitemap membership per D-04 and D-15."
    - "Every HTML document references one inert local cream/green open-page SVG favicon with no active or remote content per D-16."
    - "Fresh-build parsing proves metadata, sitemap/robots, 404, favicon, no-JavaScript recovery, focus, reflow, and accessibility through the existing lifecycle per D-17 through D-19."
  artifacts:
    - path: "src/layouts/SiteLayout.astro"
      provides: "Sole title/description/canonical/Open Graph/Twitter/favicon/noindex rendering boundary"
      contains: "og:site_name"
    - path: "src/pages/404.astro"
      provides: "Arabic shared-shell missing-route recovery document"
      contains: "الصفحة غير موجودة"
    - path: "public/favicon.svg"
      provides: "Inert local cream/green open-page favicon"
      contains: "viewBox"
    - path: "tests/search-discovery.spec.ts"
      provides: "Fresh-build metadata, XML, text, 404, favicon, no-JS, focus, reflow, and axe proof"
  key_links:
    - from: "src/layouts/SiteLayout.astro"
      to: "Astro.site + Astro.url.pathname"
      via: "new URL pathname canonical derivation without page override"
      pattern: "new URL\\(Astro\\.url\\.pathname, Astro\\.site\\)"
    - from: "src/pages/[section]/[slug].astro"
      to: "src/layouts/SiteLayout.astro"
      via: "validated article title/description and article type props"
      pattern: "ogType=.?article"
    - from: "src/pages/404.astro"
      to: "src/layouts/SiteLayout.astro"
      via: "indexable=false shared-shell rendering"
      pattern: "indexable=.?false"
    - from: "src/layouts/SiteLayout.astro"
      to: "public/favicon.svg"
      via: "one root-relative SVG icon link"
      pattern: "href=.?/favicon\\.svg"
---

## Phase Goal

**As an** Arabic search reader, **I want** every public page and crawler discovery file to expose one accurate identity and published route graph, **so that** I can find and navigate the site's public content reliably.

<objective>
Complete the reader/browser slice: exact shared Arabic search identity on every public route, a useful non-indexable Arabic 404, and one inert local favicon, all proven against Plan 04-01's crawler outputs.

Purpose: Pages, error recovery, and crawler files must expose the same configured identity without changing the eight established bodies.
Output: Sole shared metadata boundary, four public page families wired to maintained facts, static 404, local SVG favicon, and one dedicated fresh-build browser suite.
</objective>

<execution_context>
@C:/Users/saieh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/saieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-search-discovery-integrity/04-SPEC.md
@.planning/phases/04-search-discovery-integrity/04-CONTEXT.md
@.planning/phases/04-search-discovery-integrity/04-RESEARCH.md
@.planning/phases/04-search-discovery-integrity/04-VALIDATION.md
@.planning/phases/04-search-discovery-integrity/04-UI-SPEC.md
@.planning/phases/04-search-discovery-integrity/04-01-SUMMARY.md
@playwright.config.ts
@src/layouts/SiteLayout.astro
@src/config/registries.ts
@src/pages/index.astro
@src/pages/[section]/index.astro
@src/pages/[section]/[slug].astro
@src/pages/عن-أحمد-المنجاوي.astro
@tests/discovery.spec.ts

<interfaces>
From Plan 04-01:
- `LOCAL_SITE_ORIGIN` is the ordinary `Astro.site` value and the launch wrapper supplies only a validated override.
- `dist/sitemap-index.xml`, `dist/sitemap-0.xml`, and `dist/robots.txt` derive from the same configured site and generated route graph.
- The exact official sitemap integration is installed and no custom route list/filter exists.

From current content and test boundaries:
- Section registry values and article frontmatter expose maintained title/description facts; the author/home pages already contain the maintained visible descriptions.
- `tests/discovery.spec.ts` supplies the independent raw-frontmatter approach but must not be replaced or made dependent on `getPublicArticles()`.
- `production-discovery` serves `dist` at `http://127.0.0.1:4322` and keeps reports/traces/screenshots/videos under `.artifacts/playwright/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the complete browser identity and recovery contract in RED</name>
  <files>tests/search-discovery.spec.ts, playwright.config.ts</files>
  <read_first>
    - `.planning/phases/04-search-discovery-integrity/04-VALIDATION.md` — create the Wave 0 dedicated suite for rows 04-02-01 and 04-02-02.
    - `.planning/phases/04-search-discovery-integrity/04-CONTEXT.md` — D-01 through D-19 define the exact contract and exclusions.
    - `.planning/phases/04-search-discovery-integrity/04-UI-SPEC.md` — encode the zero-visible-regression, 404, focus/reflow, and favicon evidence boundaries.
    - `playwright.config.ts` — reuse the production-discovery server at port 4322 and existing `.artifacts/` routing.
    - `tests/discovery.spec.ts` — reuse registry/raw-source techniques conceptually while keeping this dedicated crawler suite separate.
  </read_first>
  <behavior>
    - A fresh build exposes exactly eight indexable routes with one non-empty unique Arabic title, description, and visible H1 each.
    - Canonical equals local origin plus exact slash-form pathname; Open Graph/Twitter text exactly mirrors the maintained identity and omits invented optional fields.
    - Sitemap index/URL XML and robots text parse successfully, agree with the configured origin, and exclude 404/draft/proof identities.
    - Two slash-form missing routes require exact Arabic 404 semantics; every document requires one safe local favicon; no-JS/focus/reflow/axe behavior is covered.
  </behavior>
  <action>Create `tests/search-discovery.spec.ts` before production implementation. Build the expected route/title/description/type matrix from registries, exact locked homepage/author copy, and the existing raw-frontmatter approach rather than `getPublicArticles()` or generated/rendered output. Add exact tag-count, Arabic/uniqueness, canonical/social parity, prohibited-field, sitemap/robots parsing, 404 status/DOM/noindex, favicon reference/response/safety, no-JS, focus, responsive overflow, and axe cases; group the route head cases under a test title containing `metadata identity` so Task 2 can run that completed slice while the 404/favicon cases remain red. Keep expected membership independent from application selectors. Update `production-discovery.testMatch` to run both discovery suites through the existing one preview server; preserve port 4322, readiness URL, browser, and every `.artifacts/` path. Run the focused suite and require RED specifically because metadata, custom 404, or favicon output is absent, not because Plan 04-01 crawler files, server, routes, imports, or the oracle are broken. This establishes the complete D-01 through D-19 browser contract before visible/head implementation.</action>
  <verify>
    <automated>npm run build; if ($LASTEXITCODE) { exit $LASTEXITCODE }; $redOutput = & npx playwright test tests/search-discovery.spec.ts --project=production-discovery 2>&1; $redCode = $LASTEXITCODE; $redText = $redOutput -join "`n"; $redOutput; if ($redCode -eq 0) { throw 'Expected RED browser contract before implementation' }; if ($redText -notmatch 'title|description|canonical|og:|404|favicon') { throw 'RED failure was not caused by missing Phase 4 browser output' }</automated>
  </verify>
  <acceptance_criteria>
    - The suite starts against a fresh local build, discovers the complete eight-route set, and parses Plan 04-01 sitemap/robots output successfully.
    - RED names missing metadata, custom 404, or favicon behavior rather than infrastructure, route, import, crawler-file, or test-data errors.
    - Expectations contain exact locked identity/copy/type/safety fields and prohibited outputs, not loose presence-only checks.
    - No production source file changes in this task.
  </acceptance_criteria>
  <done>A reproducible failing browser contract defines the complete reader/crawler slice before metadata, 404, or favicon implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render one exact metadata identity for every public route</name>
  <files>src/layouts/SiteLayout.astro, src/pages/index.astro, src/pages/[section]/index.astro, src/pages/[section]/[slug].astro, src/pages/عن-أحمد-المنجاوي.astro</files>
  <read_first>
    - `.planning/phases/04-search-discovery-integrity/04-CONTEXT.md` — D-01 through D-11 define exact titles, descriptions, canonical behavior, and social fields.
    - `.planning/phases/04-search-discovery-integrity/04-VALIDATION.md` — implement row 04-02-01.
    - `.planning/phases/04-search-discovery-integrity/04-UI-SPEC.md` — preserve the eight existing bodies layout-for-layout and behavior-for-behavior.
    - `tests/search-discovery.spec.ts` — make the metadata portion of Task 1's RED contract green without weakening assertions.
    - `src/layouts/SiteLayout.astro` and all four page-family files — retain body markup/styles and consume maintained identity facts directly.
  </read_first>
  <behavior>
    - Homepage, section, article, and author titles follow the exact D-02 formulas.
    - Each description equals its maintained homepage copy, section registry value, article frontmatter value, or author explanatory copy.
    - Every canonical is `new URL(Astro.url.pathname, Astro.site).href`; no prop or content field can override it.
    - Article routes use `og:type=article`; other indexable routes use `website`; all text fields mirror one identity object.
  </behavior>
  <action>Add one local discriminated `Props` type in `SiteLayout.astro`: indexable identity requires `title`, `description`, and `ogType`; include the explicit `indexable: false` branch needed by Task 3, but expose no canonical/origin input. Require `Astro.site`, derive canonical only from `Astro.url.pathname` plus `Astro.site`, and render every head field exactly once through normal escaped Astro expressions. Render the D-09/D-10 Open Graph and Twitter fields and no explicit `index,follow`; never use `set:html`. Per D-11, omit optional article timestamps, JSON-LD, images, handles, accounts, keywords, credentials, review claims, and inferred video facts. Update all four page families to pass exact D-02 titles and D-03 descriptions from existing body copy, registries, or validated frontmatter, with article type only on articles. Preserve every body element, visible string, order, CSS rule, and one-H1 contract; use a local copy constant only where the same maintained sentence feeds head and body. Make the metadata portion of Task 1 GREEN without relaxing its exact assertions. This completes D-01 through D-11 and preserves D-17 through D-19 boundaries.</action>
  <verify>
    <automated>npm run check; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run build; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npx playwright test tests/search-discovery.spec.ts --project=production-discovery --grep "metadata identity"</automated>
  </verify>
  <acceptance_criteria>
    - Astro type checking requires complete indexable identity and permits the 404 to suppress canonical/social identity explicitly.
    - All eight routes satisfy exact titles, maintained unique descriptions, canonical equality, article/website type, and social parity.
    - `SiteLayout.astro` is the only metadata rendering boundary and page/content data exposes no canonical/origin override.
    - No optional timestamp, social image/handle/account, schema, analytics, client runtime, remote asset, or visible body change is added.
  </acceptance_criteria>
  <done>Every indexable public route renders one escaped self-consistent Arabic identity from maintained facts through the sole shared layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deliver exact Arabic 404 recovery and inert favicon with parsed proof</name>
  <files>src/layouts/SiteLayout.astro, src/pages/404.astro, public/favicon.svg, tests/search-discovery.spec.ts</files>
  <read_first>
    - `.planning/phases/04-search-discovery-integrity/04-CONTEXT.md` — D-04 and D-12 through D-19 define exact output and evidence.
    - `.planning/phases/04-search-discovery-integrity/04-VALIDATION.md` — complete row 04-02-02.
    - `.planning/phases/04-search-discovery-integrity/04-UI-SPEC.md` — implement the exact DOM, 16px/24px rhythm, 44px target, palette, focus, responsive, no-JS, and favicon contracts.
    - `.planning/phases/04-search-discovery-integrity/04-RESEARCH.md` — preserve the static preview slash-form caveat and parse XML/text/SVG directly.
    - `tests/search-discovery.spec.ts` — complete Task 1's dedicated contract rather than adding a framework/server.
  </read_first>
  <behavior>
    - Two unrelated unknown slash-form paths return status 404 and the exact custom Arabic document; a slashless unknown path still returns 404 without asserting host-specific custom body.
    - The custom 404 has Arabic/RTL semantics, one main/H1/paragraph, exactly two native `/` anchors total, `noindex,follow`, no canonical/social identity, and no sitemap membership.
    - JavaScript-disabled recovery, header-then-recovery focus order, visible focus, and 320/390/768/1024/1440 reflow work without horizontal overflow.
    - Every HTML document links `/favicon.svg` once; the response is valid inert SVG with square viewBox, allowed geometry/attributes/colors, and no active/remote features.
    - Sitemap/robots parsing still agrees exactly with the local configured origin and complete indexable route set.
  </behavior>
  <action>Add one root-relative `<link rel="icon" type="image/svg+xml" href="/favicon.svg">` in the shared layout so every document receives it. In the layout's non-indexable branch, render title/description plus exactly `meta[name=robots][content="noindex,follow"]` and omit canonical and all Open Graph/Twitter identity. Create `src/pages/404.astro` with `SiteLayout`, exact title `الصفحة غير موجودة | مدونة أحمد المنجاوي`, exact description/message, one H1, one paragraph, and one ordinary same-tab recovery anchor. Add only scoped logical CSS for the exact 16px/24px stack rhythm and 44px minimum target; reuse inherited tokens and add no card, code, icon, illustration, search, suggestion, client event, or visible URL. Create one small `public/favicon.svg` with a finite square viewBox and simple open-page paths using only `#FFFDF8` fill and `#166534` stroke; include no text, font, script, style URL, event, image, use, foreignObject, animation, filter, data/HTTP reference, or logo claim. Complete the dedicated suite: parse both sitemap XML layers, exact robots lines, and SVG allowlist/denylist; assert MIME/status/network behavior, two slash-form 404 documents, separate slashless 404 status, no-JS recovery, focus, responsive overflow, axe, and 16px/32px/responsive/focus artifacts only under `.artifacts/`. This completes D-04 and D-12 through D-19 without widening runtime or UI scope.</action>
  <verify>
    <automated>npm run build; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npx playwright test tests/search-discovery.spec.ts --project=production-discovery; if ($LASTEXITCODE) { exit $LASTEXITCODE }; npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - SITE-06 exact Arabic copy, DOM order, status, navigation, RTL, no-JS, focus, reflow, and zero serious/critical axe findings pass.
    - SEO-05 parsed sitemap URL set and robots sitemap URL agree with the configured origin, slash policy, and generated indexable route set.
    - The 404 is absent from sitemap output, emits no canonical/social identity, and is the only page with `noindex,follow`.
    - Every HTML document links `/favicon.svg` exactly once; SVG MIME, square viewBox, palette, allowlist, active-content, remote-request, and small-size checks pass.
    - Existing eight bodies remain unchanged and every browser artifact stays beneath `.artifacts/`.
  </acceptance_criteria>
  <done>Public routes have exact search identity, crawlers retain matching sitemap/robots output, missing readers receive the locked Arabic 404, and every document uses one safe local favicon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Registry/frontmatter/page facts → shared head | Maintained strings enter search/social metadata and must remain escaped and non-overriding. |
| Generated build output → browser/crawler parser | HTML, XML, text, status, and network observations must agree independently. |
| Local SVG source → browser chrome | An image document can carry active or external content if not constrained. |
| Missing pathname → 404 document | Preview/host must return a true non-indexable error rather than a soft 404. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification |
|---|---|---|---|---|---|---|
| T-04-01 | Spoofing/Tampering | Canonical/social consumers | High | mitigate | Consume only Plan 04-01's configured `Astro.site` and exact pathname. | Local canonical/OG/robots/sitemap equality; controlled launch proof in Plan 04-03. |
| T-04-02 | Spoofing | Canonical derivation | High | mitigate | Expose no origin/canonical prop and never use request-host identity. | Source ownership scan and exact canonical equality. |
| T-04-03 | Information Disclosure | Public HTML/sitemap | High | mitigate | Preserve route selection and compare draft/proof identifiers across separate output families. | Dedicated negative scan now; independent oracle in Plan 04-03. |
| T-04-04 | Tampering/Repudiation | Sitemap and robots | Medium | mitigate | Parse both official XML layers and exact same-origin robots lines. | Exact set/host/line equality. |
| T-04-05 | Tampering/Elevation | Metadata strings and 404 head | High | mitigate | Normal Astro escaping in one layout; no `set:html` or duplicate page tags. | Exact counts/value parity and source scan. |
| T-04-06 | Elevation/Information Disclosure | `public/favicon.svg` | High | mitigate | Fixed local allowlisted SVG with no active, external, font, image, animation, or event surface. | XML allowlist/denylist, MIME, network, and small-size checks. |
| T-04-07 | Spoofing | `src/pages/404.astro` | Medium | mitigate | Astro special 404 with true status, noindex, no canonical/social, and sitemap exclusion. | Two slash-form responses, separate slashless status, DOM/XML checks. |
| T-04-SC | Tampering | Sitemap dependency inherited from Plan 04-01 | High | mitigate | Consume only the exact autonomously verified `@astrojs/sitemap@3.7.3` manifest/lockfile state and introduce no additional dependency. | Plan 04-01 summary, unchanged package diff, and full build. |
</threat_model>

<verification>
- The dedicated suite begins RED for missing browser-facing output, then passes without weakened assertions after Tasks 2–3.
- `npm run check` and fresh local builds pass with one exact identity per public route.
- Parsed sitemap/robots output agrees with canonical public routes and contains no 404/draft/proof identity.
- Exact Arabic 404, no-JS recovery, focus/reflow/axe, SVG safety/network, and favicon small-size checks pass.
- `npm run verify` remains green after the wave.
</verification>

<success_criteria>
- SEO-02 and SEO-03: every indexable route has one unique maintained Arabic title/description/H1, self-canonical, and exact text-only social identity.
- SITE-06: representative missing routes return the exact Arabic 404 recovery document with true status and noindex policy.
- SEO-05: sitemap and robots remain parsed, same-origin, published-only, and 404-free after browser-surface implementation.
- T-04-01, T-04-02, T-04-03, T-04-05, T-04-06, and T-04-SC have concrete implementation controls and runnable evidence.
- Existing bodies, static/no-JS architecture, and `.artifacts/` boundary remain unchanged.
</success_criteria>

<decision_coverage>
- D-01, D-02, D-03, D-04, D-05: sole layout boundary, exact maintained identity, indexable canonical policy, and explicit 404 noindex branch.
- D-09, D-10, D-11: exact text-only Open Graph/Twitter parity with article/website types and no invented optional claims.
- D-12, D-13, D-14: parsed route-derived sitemap/robots agreement and independent publication-boundary checks.
- D-15, D-16: exact quiet Arabic 404 and one inert local cream/green SVG favicon.
- D-17, D-18, D-19: one dedicated fresh-build browser suite, existing preview/artifact lifecycle, full gate, and no new runtime/framework surface.
</decision_coverage>

## Artifacts this plan produces

| Kind | Symbol / CLI / field | File path | Contract |
|---|---|---|---|
| Layout fields | `title`, `description`, `ogType`, `indexable` | `src/layouts/SiteLayout.astro` | Sole escaped metadata/favicon boundary with no canonical override. |
| Static page | Arabic 404 route | `src/pages/404.astro` | True 404 shell with exact locked Arabic copy and recovery link. |
| Static asset | Cream/green open-page SVG | `public/favicon.svg` | One inert local favicon with no active or remote content. |
| Browser test | Search-discovery suite | `tests/search-discovery.spec.ts` | Fresh-build metadata, XML, text, 404, favicon, no-JS, reflow, focus, and axe proof. |

No custom crawler output, SEO copy registry, canonical registry, content field, client runtime, remote asset, or deferred feature is added by this plan.

<output>
After completion, create `.planning/phases/04-search-discovery-integrity/04-02-SUMMARY.md`.
</output>
