---
phase: 04-search-discovery-integrity
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - tests/discovery.spec.ts
  - tests/search-discovery.spec.ts
  - .artifacts/hercules-visual-qa/phase-04-final/**
autonomous: true
requirements:
  - SITE-06
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05

must_haves:
  truths:
    - "The independent raw-frontmatter/registry oracle equals generated HTML routes, ordinary internal links, canonical URLs, and sitemap URLs; every draft/proof identifier is absent everywhere per D-14."
    - "Every public internal anchor resolves, so a crawler can traverse homepage → section → article and article → author without JavaScript while non-public records remain structurally absent."
    - "The eight established route bodies have zero visible regression: body landmarks, text, headings, lists, links, computed visual tokens, dimensions, focus, reflow, and no-JS behavior remain unchanged; only browser title/favicon may differ per the UI-SPEC."
    - "D-18: Every browser verification follows a fresh ordinary or controlled launch build, parses metadata/XML/text before rendered 404/focus/reflow/accessibility checks, and stores any visual evidence only under .artifacts/."
    - "Every high-severity T-04 threat is verified closed; no unsafe origin, host override, draft leak, raw metadata injection, or active SVG is deferred."
  artifacts:
    - path: "tests/discovery.spec.ts"
      provides: "Independent source/build/links/canonical/sitemap equality and negative proof-leak oracle"
    - path: "tests/search-discovery.spec.ts"
      provides: "Complete metadata/discovery/404/favicon/body-invariance browser gate"
    - path: ".artifacts/hercules-visual-qa/phase-04-final/"
      provides: "Ignored identity evidence, responsive/focus/favicon captures, coverage ledger, console/network notes, and findings-first report"
  key_links:
    - from: "raw src/content/articles frontmatter"
      to: "dist/**/*.html + rendered anchors + canonicals + sitemap loc values"
      via: "independent expected set in tests/discovery.spec.ts"
      pattern: "expectedPublicCorpus"
    - from: "tests/search-discovery.spec.ts"
      to: "playwright.config.ts production-discovery"
      via: "fresh build followed by static preview at 127.0.0.1:4322"
      pattern: "production-discovery"
    - from: "04-UI-SPEC.md body invariance contract"
      to: "all eight existing routes"
      via: "DOM/text/token/box/scroll-width comparison plus visual evidence"
      pattern: "zero-visible-regression"
---

## Phase Goal

**As an** Arabic search reader, **I want** every public page and crawler discovery file to expose one accurate identity and published route graph, **so that** I can find and navigate the site's public content reliably.

<objective>
Close the phase with an independent published-route proof and evidence-first validation that catches shared-selector false greens and any visible regression.

Purpose: Route generation, sitemap output, metadata, and tests must not all agree on the same bug; the final oracle derives expected public membership independently and proves the complete reader/crawler graph.
Output: Expanded independent regression suite plus ignored Hercules visual/logic evidence with no unresolved required finding.
</objective>

<execution_context>
@C:/Users/saieh/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/saieh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-search-discovery-integrity/04-SPEC.md
@.planning/phases/04-search-discovery-integrity/04-CONTEXT.md
@.planning/phases/04-search-discovery-integrity/04-RESEARCH.md
@.planning/phases/04-search-discovery-integrity/04-VALIDATION.md
@.planning/phases/04-search-discovery-integrity/04-UI-SPEC.md
@.planning/phases/03-real-content-and-section-discovery/03-CONTEXT.md
@.planning/phases/03-real-content-and-section-discovery/03-03-SUMMARY.md
@.planning/phases/03-real-content-and-section-discovery/03-04-SUMMARY.md
@.planning/phases/04-search-discovery-integrity/04-01-SUMMARY.md
@.planning/phases/04-search-discovery-integrity/04-02-SUMMARY.md
@tests/discovery.spec.ts
@tests/search-discovery.spec.ts
@playwright.config.ts
@src/config/registries.ts
@src/lib/articles.ts
@src/lib/content-contract.ts

<interfaces>
From `tests/discovery.spec.ts`:
- `expectedPublicCorpus()` walks raw Markdown/MDX files, parses frontmatter itself, excludes literal drafts, validates registered sections, and never imports `getPublicArticles()`.
- `structuralPaths(corpus)` identifies homepage, section roots, author, and approved article routes.
- `readOutputTree("dist")` already supports negative proof/reviewer scans across generated text outputs.

From Phase 04-01 and 04-02:
- `tests/search-discovery.spec.ts` owns exact rendered head, XML/text, 404, favicon, no-JS, responsive, focus, and axe checks.
- `SiteLayout.astro` is the sole metadata/favicon render boundary.
- `dist/sitemap-index.xml` points to the generated numbered sitemap URL set.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the published route graph independently across source, HTML, links, canonicals, and sitemap</name>
  <files>tests/discovery.spec.ts</files>
  <read_first>
    - `.planning/phases/04-search-discovery-integrity/04-CONTEXT.md` — D-14 and D-17 through D-19 require an independent source/build/browser comparison.
    - `.planning/phases/04-search-discovery-integrity/04-VALIDATION.md` — implement validation row 04-03-01 and T-04-03 closure.
    - `tests/discovery.spec.ts` — extend its existing raw-frontmatter oracle; never replace it with application imports.
    - `tests/search-discovery.spec.ts` — reuse parsed sitemap/output helpers only when doing so does not make expected membership depend on rendered output.
    - `src/lib/articles.ts` and `src/lib/content-contract.ts` — inspect the production boundary but do not import it into the expectation oracle.
  </read_first>
  <behavior>
    - Raw approved source route set equals generated public HTML route set.
    - Raw approved source route set equals the route set reachable through ordinary HTML anchors from homepage/sections/articles/author.
    - Raw approved source route set plus structural routes equals canonical href set and sitemap loc set exactly, with no duplicate URLs.
    - Every internal public anchor resolves 200; missing/proof/draft routes resolve 404 and never enter canonical/social/sitemap/robots output.
    - Proof titles, slugs, URLs, video IDs, review/approval traces, and draft identifiers are absent across HTML, head, XML, and text outputs.
  </behavior>
  <action>Extend the existing `PublicCorpusEntry` raw-frontmatter oracle only with the maintained fields needed for identity comparison; continue reading source files directly and do not call/import `getPublicArticles`, generated route helpers for expected membership, `dist`, rendered links, canonicals, or sitemap to create expected results. Remove the temporary `.disableRules(["document-title"])` axe exception now that every route has a document title. After a fresh build, walk `dist` for public HTML, crawl ordinary anchors, collect each canonical/OG URL, parse both sitemap XML layers, and compare those independently observed sets against the raw approved source plus registry-derived structural route set. Assert exact set equality, no duplicates, correct trailing slashes, working internal status, and absence of `/404/`. Scan every output family separately for both draft proof paths/titles/video IDs and fabricated review/approval traces so a shared selector leak cannot pass. Preserve existing route sorting, source disclosure, no-JS, responsive, network, visual-token, and axe tests. This is the primary SEO-04 implementation proof and closes D-14/T-04-03 at the independent oracle boundary.</action>
  <verify>
    <automated>npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - SEO-04 source/build/link/canonical/sitemap sets are exactly equal and duplicate-free.
    - Every public internal link returns 200 and every proof/draft/missing route tested returns 404.
    - No expectation imports `getPublicArticles()` or derives approved membership from generated/rendered output.
    - Proof/draft identity and fabricated review traces are absent from HTML bodies, metadata, canonicals, social fields, sitemap XML, and robots text.
    - The complete existing discovery, article, native, check, build, accessibility, no-JS, network, and responsive gates remain green with `document-title` enabled.
  </acceptance_criteria>
  <done>The published graph is proven by independent observations, making draft leakage or shared-selector omission a deterministic full-gate failure.</done>
</task>

<task type="auto">
  <name>Task 2: Run final fresh-build launch, visual, logic, and zero-regression evidence</name>
  <files>tests/search-discovery.spec.ts, .artifacts/hercules-visual-qa/phase-04-final/**</files>
  <read_first>
    - `.planning/phases/04-search-discovery-integrity/04-UI-SPEC.md` — use the Evidence and Verification Contract, exact 404/favicon states, and eight-route body invariance budget.
    - `.planning/phases/04-search-discovery-integrity/04-VALIDATION.md` — implement row 04-03-02 and close every threat before sign-off.
    - `C:/Users/saieh/.agents/skills/hercules-visual-qa/SKILL.md` — follow visible-browser, target-identity, evidence, coverage-ledger, and findings-first requirements; this task is verification-only and does not execute a source-fix branch.
    - `tests/search-discovery.spec.ts` and `tests/discovery.spec.ts` — treat deterministic parsed assertions as the repeatable contract, not screenshots.
    - `playwright.config.ts` — keep the existing preview lifecycle and all generated artifacts under `.artifacts/`.
  </read_first>
  <action>Finish `tests/search-discovery.spec.ts` with a controlled existing-body invariance matrix for all eight indexable routes: assert unchanged landmark/heading/list/link order, exact visible text sources, one H1, computed font family/four-size/two-weight palette, 70ch measure, shared paddings/borders, link/focus rules, box containment, document width, and absence of visible metadata/favicon body nodes. Parameterize only the expected absolute identity as `process.env.EXPECTED_SITE_ORIGIN ?? "http://127.0.0.1:4322"`; keep Playwright requests and preview lifecycle on the local configured base URL. The same suite must therefore assert local canonical/OG/sitemap/robots origins during ordinary verification and a controlled HTTPS origin when it is run directly against launch-built `dist`. Run the ordinary full gate, set process-local `SITE_ORIGIN` and `EXPECTED_SITE_ORIGIN` to the same controlled safe HTTPS origin, run `npm run launch:ready`, remove `SITE_ORIGIN`, and immediately run `npx playwright test tests/search-discovery.spec.ts --project=production-discovery` against that untouched launch output before any ordinary rebuild. Clear both process variables in all success/failure paths; only then run the ordinary full verify again so it rebuilds and proves the local-origin contract. Apply the Hercules workflow without subagents as verification/evidence only: prefer visible Chrome DevTools MCP when available and label the Playwright fallback honestly otherwise; record resolved URL/title, port/process, branch/commit, no credentials/test data, and redaction status. Create only `.artifacts/hercules-visual-qa/phase-04-final/<run-id>/`; inventory all eight existing routes, two slash-form missing routes, slashless status-only case, JavaScript-off recovery, keyboard/focus states, widths 320/390/768/1024/1440, favicon at 16px/32px, XML/text endpoints, console, failed/unexpected network, and accessibility. Capture and inspect viewport/full-page/responsive/focus evidence, maintain a coverage ledger, and compare existing bodies against the locked Phase 3/UI contract using DOM/text/computed token/box invariance when a deterministic pixel baseline is unavailable; browser title/favicon are the only allowed visible/browser-chrome differences. Do not edit application, configuration, package, or production source in this task. Any failed, blocked, untested, or confirmed finding fails this task and must be repaired under the concrete producing task or an explicitly scoped execution deviation before this entire verification sequence is rerun. Do not claim native browser-chrome 200% zoom or production Core Web Vitals; those remain Phase 6.</action>
  <verify>
    <automated>npm run verify; if ($LASTEXITCODE) { exit $LASTEXITCODE }; $env:SITE_ORIGIN='https://blog.ahmed-mangawy.org'; $env:EXPECTED_SITE_ORIGIN=$env:SITE_ORIGIN; try { npm run launch:ready; if ($LASTEXITCODE) { throw 'Controlled launch build failed' }; Remove-Item Env:SITE_ORIGIN; npx playwright test tests/search-discovery.spec.ts --project=production-discovery; if ($LASTEXITCODE) { throw 'Launch-output discovery assertions failed' } } finally { Remove-Item Env:SITE_ORIGIN -ErrorAction SilentlyContinue; Remove-Item Env:EXPECTED_SITE_ORIGIN -ErrorAction SilentlyContinue }; npm run verify; if ($LASTEXITCODE) { exit $LASTEXITCODE }; git check-ignore .artifacts/hercules-visual-qa/phase-04-final/</automated>
    <human-check>Through the Hercules workflow, visually inspect the complete responsive/focus/favicon capture set and confirm the coverage ledger contains no required `failed`, `blocked`, or `untested` item and no unresolved Phase 4 finding.</human-check>
  </verify>
  <acceptance_criteria>
    - Full native, Astro diagnostic, fresh-build Playwright, controlled safe launch, launch-output browser assertions, and restored ordinary full gates all pass.
    - The direct production-discovery suite reads untouched launch-built `dist` before any ordinary rebuild and expects the controlled HTTPS origin while requests remain local.
    - Controlled launch output uses only the safe HTTPS host across canonical, Open Graph, sitemap, and robots; both process variables are cleared before the final ordinary verify restores `http://127.0.0.1:4322` output.
    - All eight existing bodies satisfy zero-visible-regression structural/computed evidence; browser title/favicon are the only intentional visible/browser-chrome differences.
    - Hercules evidence includes target identity, redaction note, coverage ledger, responsive/focus/favicon screenshots, console/network notes, findings-first report, and no required failed/blocked/untested/unresolved item.
    - Every artifact is ignored below `.artifacts/`; no evidence enters `src/`, `tests/`, or `.planning/` except the tracked deterministic test source and execution summary.
    - No application/configuration/package/production source is edited by this verification task; any finding is remediated under its producing task/deviation scope before every gate is rerun.
    - No optional article timestamps, JSON-LD/schema, social images/handles, analytics, remote fonts/assets, CMS/database/runtime, alternate test framework, or speculative abstraction appears.
  </acceptance_criteria>
  <done>All five requirements, D-01 through D-19, all high-severity threats, the published-route graph, and the zero-visible-regression UI contract have repeatable automated proof plus complete ignored visual/logic evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Raw approved content/registries → expected public graph | The oracle must remain independent from application selection and generated outputs. |
| Build outputs → crawler/browser evidence | HTML, head, XML, text, status, network, and visible behavior require separate observations. |
| Browser captures/logs → repository | Evidence can leak or destabilize watched paths unless kept ignored and redacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification |
|---|---|---|---|---|---|---|
| T-04-01 | Spoofing/Tampering | Launch origin | High | mitigate | Re-run native matrix and controlled launch crawl; require one normalized safe HTTPS host everywhere. | `npm test`, controlled `launch:ready`, parsed canonical/OG/XML/text host equality. |
| T-04-02 | Spoofing | Absolute page identity | High | mitigate | Source ownership scan and route pathname equality; no request/page override. | Exact canonical set and source scan. |
| T-04-03 | Information Disclosure | Draft/proof publication | High | mitigate | Independent raw-frontmatter oracle compared with files, links, canonicals, social metadata, sitemap, and negative identifiers. | Full `tests/discovery.spec.ts` equality/absence matrix. |
| T-04-04 | Tampering/Repudiation | Sitemap/robots agreement | Medium | mitigate | Parse sitemap index + URL file and exact robots lines against the same origin/set. | Dedicated browser parsing and controlled launch/local runs. |
| T-04-05 | Tampering/Elevation | Metadata injection/duplication | High | mitigate | Normal escaping in one layout and exact tag/source checks. | Head counts/value parity, no `set:html`, no duplicate page tags. |
| T-04-06 | Elevation/Information Disclosure | SVG favicon | High | mitigate | Static allowlist/denylist plus local-only network and visual proof. | SVG parse/MIME/network tests and inspected 16px/32px captures. |
| T-04-07 | Spoofing | Missing-route behavior | Medium | mitigate | True static 404 status, exact noindex identity, sitemap exclusion, and native recovery. | Two slash-form response/DOM tests plus slashless status-only check. |
| T-04-SC | Tampering | Package supply chain | High | mitigate | Recheck the exact official package, version, integrity, repository, lifecycle-script absence, and lockfile facts established autonomously in Plan 04-02. | Exact registry/package/lockfile assertions and full build. |
</threat_model>

<verification>
- `npm run verify` passes after independent oracle expansion and again after any finding is remediated under its producing task/deviation scope.
- Controlled safe `SITE_ORIGIN` launch build passes, then the direct production-discovery suite parses untouched launch output with `EXPECTED_SITE_ORIGIN` and proves host consistency.
- Both process variables are cleared before a final ordinary full verify restores local-origin output and keeps the production-discovery suite green.
- Hercules/visible-browser evidence contains no required failed, blocked, untested, or unresolved finding.
- `git check-ignore .artifacts/hercules-visual-qa/phase-04-final/` confirms evidence containment.
</verification>

<success_criteria>
- SITE-06 and SEO-02 through SEO-05 are each backed by independent runnable evidence.
- Every public route is reachable through ordinary HTML links and every non-public route/identifier is absent from all discovery outputs.
- Every page/discovery file exposes one accurate identity and one consistent origin policy.
- Existing eight route bodies remain visibly and semantically unchanged.
- All high-severity threats are mitigated and no excluded/deferred feature enters the implementation.
</success_criteria>

<decision_coverage>
- D-01 through D-04: sole metadata boundary, exact title/description reuse, indexable canonical policy, and exact 404 noindex branch are reverified.
- D-05 through D-08: one `Astro.site` origin, fixed local origin, strict launch input, and zero `.env` access are reverified locally and in controlled launch mode.
- D-09 through D-11: exact text-only OG/Twitter parity and omission of optional/invented article/social claims are reverified.
- D-12 through D-14: exact official route-derived sitemap, same-origin robots, independent source/build/link/canonical/sitemap comparison, and structural draft absence are reverified.
- D-15 and D-16: exact quiet Arabic 404 and inert cream/green favicon are reverified visually and semantically.
- D-17: smallest native and dedicated browser suites reuse the existing lifecycle and `.artifacts/` boundary.
- D-18: every browser verification follows a fresh ordinary or controlled launch build, parses metadata/XML/text before rendered 404/focus/reflow/accessibility checks, and never treats screenshots as machine-output proof.
- D-19: the full verification gate stays green with no new client runtime, remote dependency, or alternate framework.
</decision_coverage>

## Artifacts this plan produces

| Kind | Symbol / CLI / field | File path | Contract |
|---|---|---|---|
| Browser test | Search-discovery suite | `tests/search-discovery.spec.ts` | Fresh-build metadata, XML, text, 404, favicon, no-JS, reflow, focus, and axe proof. |
| Existing-test extension | Independent route/canonical/link/sitemap oracle | `tests/discovery.spec.ts` | Proves published-only set equality and negative draft/proof isolation. |
| QA evidence | Identity notes, captures, ledger, console/network notes, report | `.artifacts/hercules-visual-qa/phase-04-final/<run-id>/` | Ignored visual-first and logic-complete evidence; no sensitive data or watched-path output. |

No new content/frontmatter field, SEO copy registry, canonical registry, route registry, client runtime, JSON-LD type, analytics field, remote asset, CMS/database/runtime, or alternate test framework is produced.

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One accurate consistent identity and indexing policy on every public route | 01–03 | COVERED | Origin, shared head, crawler outputs, 404, and independent agreement are end-to-end. |
| REQ | SITE-06 | Useful Arabic 404 with a clear path back | 02–03 | COVERED | Exact copy/status/noindex/navigation plus final regression. |
| REQ | SEO-02 | Unique Arabic title, description, and one H1 | 01, 03 | COVERED | Shared layout identity plus complete route matrix. |
| REQ | SEO-03 | Safe configured origin, self-canonical, and accurate social identity | 01, 03 | COVERED | Native validation, local/launch builds, and launch-output browser proof. |
| REQ | SEO-04 | Crawlable published graph; drafts/non-public absent | 03 | COVERED | Independent source/build/link/canonical/sitemap equality and negative scans. |
| REQ | SEO-05 | Published-only sitemap and agreeing robots | 01–03 | COVERED | Exact official package, route-derived XML, same-origin robots, and independent proof. |
| RESEARCH | — | Programmatic launch wrapper plus WHATWG URL/`node:net.isIP` fail-closed policy | 01 | COVERED | One pure helper and explicit `build({ site })` wrapper. |
| RESEARCH | — | Discriminated shared metadata input with normal Astro escaping | 02 | COVERED | One layout boundary, no canonical override or raw head HTML. |
| RESEARCH | — | Plain official sitemap plus generated same-origin robots | 01–03 | COVERED | No custom pages/filter; exact XML/text parsing. |
| RESEARCH | — | Static slash-form 404 and inert local SVG | 02–03 | COVERED | Status/DOM/no-JS/reflow plus SVG allowlist/network/small-size proof. |
| RESEARCH | — | Independent raw-source oracle and fresh-build browser parsing | 02–03 | COVERED | Expected membership never derives from app selector or generated output. |
| CONTEXT | D-01, D-02, D-03, D-04 | Sole layout, exact maintained identity, canonical indexable pages, 404-only noindex | 02–03 | COVERED | Exact rendering/source ownership and tag-count/value checks. |
| CONTEXT | D-05, D-06, D-07, D-08 | One safe `Astro.site`, fixed local origin, strict explicit launch input, no `.env` | 01, 03 | COVERED | Native rejection matrix plus local and controlled-launch runs. |
| CONTEXT | D-09, D-10, D-11 | Exact text-only Open Graph/Twitter parity and no inferred claims | 02–03 | COVERED | Type/equality/absence matrix. |
| CONTEXT | D-12, D-13, D-14 | Official route-derived sitemap, same-origin robots, independent publication oracle | 01–03 | COVERED | Automated package facts, parsed outputs, source/build/link set equality. |
| CONTEXT | D-15, D-16 | Exact quiet Arabic 404 and inert cream/green favicon | 02–03 | COVERED | Deterministic DOM/asset checks plus visual evidence. |
| CONTEXT | D-17, D-18, D-19 | Small native/browser suites, fresh builds, full gate, no new runtime/framework | 01–03 | COVERED | Existing lifecycle/artifact boundary and final full verification. |
| UI-SPEC | — | Zero visible regression plus exact 404/focus/reflow/favicon contract | 02–03 | COVERED | DOM/token/box assertions and ignored responsive/focus/16px/32px evidence. |

### Explicit exclusions — not gaps

- Final hostname, provider deployment, redirects, DNS/TLS, Search Console, analytics, and outbound-click measurement remain Phase 5.
- Native browser-chrome 200% zoom, live YouTube playback, production crawl certification, and Core Web Vitals evidence remain Phase 6.
- JSON-LD/rich results, social images, handles, keywords, optional article OG timestamps, remote fonts/assets, search, CMS/database/runtime, and speculative abstractions remain deferred or out of scope.
</source_audit>

<output>
After completion, create `.planning/phases/04-search-discovery-integrity/04-03-SUMMARY.md`.
</output>
