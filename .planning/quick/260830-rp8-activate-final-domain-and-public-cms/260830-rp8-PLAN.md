---
quick_id: 260830-rp8
description: Activate the final free domain and least-privilege public-repository CMS deployment
mode: quick
status: human_needed
updated: 2026-08-31
must_haves:
  truths:
    - "Every production-facing configuration uses ahmed-almangawy.de5.net and the canonical public repository MohammedGhazal09/blog."
    - "Sveltia and its OAuth Worker request only public_repo access and remain fail closed until the owner-controlled OAuth secret is installed."
    - "Cloudflare Pages, DNS delegation, branch protection, Access, OAuth, and final production verification are activated or honestly recorded at their external boundary."
  artifacts:
    - path: "public/admin/config.yml"
      provides: "Exact public repository, Worker origin, and least-privilege OAuth scope"
    - path: "workers/sveltia-cms-auth/src/index.js"
      provides: "Exact public_repo-only OAuth enforcement"
    - path: "workers/sveltia-cms-auth/wrangler.toml"
      provides: "Exact final admin hostname allowlist"
    - path: "tests/cms-security.test.ts"
      provides: "Regression coverage for the production CMS trust boundary"
    - path: ".planning/STATE.md"
      provides: "Current external deployment and handoff state"
---

# Quick Task 260830-rp8: Activate Final Domain and Public CMS

## Task 1: Lock repository and hostname identity

Update the CMS, Worker, tests, runbook, and active planning state to use `MohammedGhazal09/blog`, `ahmed-almangawy.de5.net`, and the deployed Worker origin. Remove superseded production-host references from active artifacts.

## Task 2: Reduce OAuth permission

Change both the CMS request and Worker allowlist from private-repository `repo` scope to public-repository `public_repo`, preserving exact scope rejection, CSRF, origin, and secret boundaries. Run focused and full repository gates.

## Task 3: Activate providers and verify production

Connect Cloudflare Pages to public `main`, delegate the DNSHE domain to Cloudflare, attach the custom domain, configure the OAuth app/secret and Cloudflare Access, protect `main`, prove one draft-only CMS pull request, and run final-origin plus Hercules visual QA. Never claim provider states without direct evidence.

## Current Status — 2026-08-31

- Tasks 1 and 2 are complete and shipped.
- Task 3 has passed Cloudflare Pages, DNS/TLS, Access, OAuth configuration, protected-branch mechanics, final-origin verification, live media checks, native 200% zoom, and full Hercules QA.
- The task remains `human_needed` only because Ahmed's dedicated GitHub username is required for the real-owner hidden-draft Sveltia proof. The developer account is not Ahmed’s dedicated owner identity and must not substitute.
