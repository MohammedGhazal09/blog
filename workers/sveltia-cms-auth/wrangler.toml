name = "mangawy-sveltia-cms-auth"
main = "src/index.js"
compatibility_date = "2026-08-30"
workers_dev = true
preview_urls = false

# The exact admin hostname is public. The OAuth client ID remains fail-closed
# until the owner creates the GitHub OAuth app during one-time setup.
[vars]
ALLOWED_DOMAINS = "ahmed-almangawy.de5.net"
GITHUB_CLIENT_ID = "replace-before-deploy"
